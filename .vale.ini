StylesPath = docs/vale/styles
MinAlertLevel = error

; Prose lint for the user-facing docs site. Rules encode the repo's editorial
; standards (see CLAUDE.md "User Documentation Authoring"): no filler that
; carries no information, no generic link text. Invoked as
; `vale docs/site/docs` from `make lint`.
[*.md]
BasedOnStyles = Haptic
