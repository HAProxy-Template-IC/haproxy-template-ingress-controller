# Changelog

All notable changes to HAPTIC — the controller and its Helm chart — are
documented in this file. Controller changes are listed first; chart changes
(values, templates, chart defaults) follow under each release's "Helm chart"
subsection.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Field-level validation-test stripping: `validationTests` accept `requiresFields` (`<watchedResource>.<field.path>` entries); tests whose fields are absent from the resolved schema generation are stripped at config load. Fixes the controller crash-loop on clusters serving older Gateway API releases (v1.1/v1.4) whose schemas lack fields the chart's tests exercise (#59).
- Runtime API-version detection for watched resources: `watchedResources` entries accept an ordered `apiVersions` candidate list and an `optional` flag, resolved against live apiserver discovery at startup. `templateSnippets`/`validationTests` accept `requires` — features whose optional resources aren't served are stripped at config load instead of failing startup.
- A CRD watch reinitializes the controller when a watched resource's CRD is installed, upgraded in place, or removed — Gateway API support activates or re-resolves at runtime with no Helm operation and no pod restart. Reloads fire only when the resolution outcome actually changes.
- A required watched resource with no served candidate version now fails startup fast with an error naming the resource and candidates (previously: silent infinite informer-sync hang with `/healthz` stuck at 503).
- Templates can read the resolved version of any watched resource via `resources.<name>.APIVersion()`; the resolution is exposed at `/debug/vars/effectiveConfigResolution`.
- `controller validate` resolves `apiVersions` candidate lists against `--schema-dir` CRD manifests and strips unavailable optional features, making degraded cluster profiles testable offline.
- Bulk resource watchers register a watch-error handler, so a watch that starts failing mid-run (e.g. its API version stopped being served) is logged instead of being visible only inside client-go.
- The controller now emits Kubernetes Events on the `HAProxyTemplateConfig`: a `Warning`/`ValidationFailed` (carrying the first error) when a config change fails validation — template, JSONPath, or `haproxy -c` — and a `Normal`/`Validated` on recovery. Failures now surface in `kubectl describe haproxytemplateconfig` and `kubectl get events`, not only in CRD status and controller logs.
- HAProxy 3.4 support: the controller is now built, validated, and released for HAProxy 3.4 (which ships DataPlane API v3.3, served by the existing v3.3 client).
- The controller now runs the config's embedded `validationTests` before accepting a `HAProxyTemplateConfig` — at startup and on every change — so it never loads a config whose tests fail (previously only the `controller validate` CLI ran them). A failing change is rejected and the last-good config keeps serving; at startup the controller stays unready until a passing config is present. Failing test names and reasons are surfaced on the CR's `status.validationErrors`. Runs only on config load, never on the reconciliation hot path.
- The admission webhook now also runs the config's embedded `validationTests` (the same suite as the load gate), so a `HAProxyTemplateConfig` whose tests fail is denied at `kubectl apply` and never reaches etcd — closing the gap where a broken config could be stored and then crash-loop the next fresh controller pod. The run is time-bounded; if it can't finish within the admission budget, or the webhook is unreachable (`failurePolicy: Ignore`), the apply is admitted-with-warning and the load gate still enforces.
- **Typed watched resources** — chart templates can access watched resources through compile-time typed top-level globals (e.g. `gateways`, `httproutes`); field access is type-checked at controller startup. Schemas come from the live apiserver, or `--schema-dir` offline. See [ADR-0010](./docs/adr/0010-typed-watched-resources.md).
- `spec.k8sResources` declares full Kubernetes resources reconciled via Server-Side Apply, with an `OwnerReference` to the `HAProxyTemplateConfig` CR so `helm uninstall` GCs them. Replaces the imperative `renderResource()` template function.
- `resourceapplier` partial-ownership mode (`haproxy-haptic.org/ownership: partial`): chart-owned and haptic-owned entries coexist on one resource via field-manager-aware list-map merging; orphan cleanup never deletes a partial-mode resource.
- `spec.validators` declares pluggable validator sidecars consulted by the admission webhook (per-entry socket, file-glob routing, timeout, connection pool); `/healthz` reports each socket's reachability.
- The admission webhook hot-reloads its TLS certificate via a `GetCertificate` callback that re-parses the mounted Secret on change, so a cert-manager renewal is served within ~a minute without a controller restart. The `--webhook-cert-secret-name` flag is replaced by `--webhook-cert-dir` (env `WEBHOOK_CERT_DIR`), and the controller no longer reads the cert Secret over the API.
- Gateway API `HTTPRouteFilter` of type `RequestMirror`, including multiple and percentage/fraction mirrors, backed by the bundled mirror SPOA plugin.
- Ingress `spec.defaultBackend` support, both rules-less (catch-all `/`) and combined with `spec.rules` (per-host fallthrough, newest-Ingress-wins on collision).
- Ingress `pathType: Exact` now preserves trailing slashes (`/foo/` no longer matches `/foo`).
- HAProxy responses now carry a `Server: haptic` header.
- Prometheus metrics for every bundled SPOA plugin (mirror, coraza, external-auth, fingerprinting, maxmind, otel, sso-auth) via the hub's `/metrics` endpoint.
- Runtime fast-path metrics: `haptic_runtime_fast_path_fires_total`, `_applies_total`, `_failures_total`, `_server_updates_total`.
- Deployment operation metrics: `haptic_haproxy_reloads_total` (HAProxy reloads triggered by deployments — the canonical capacity/SLO signal, since a reload forks the process) and `haptic_dataplane_api_operations_total` (DataPlane API operations applied to HAProxy).
- `spec.dataplane.configPublishInterval`, `reloadVerificationTimeout`, and `syncTimeout` are now tunable via `HAProxyTemplateConfig`; `spec.watchedResources.*.debounceInterval` adds a per-resource override.
- New `spoa-hub` container image bundling the hub plus seven plugin libraries, Cosign-signed by digest with a CycloneDX SBOM. A `spoa-hub` template library wires HAProxy to the sidecar, auto-enabled when any plugin is on.
- External-auth flow wired end to end (nginx-ingress `auth-url` annotation → `auth-url.map` → per-frontend SPOE check + `deny_status 401`).

### Changed

- Leader-election timing defaults raised from 15s/10s/2s to 30s/20s/5s (leaseDuration/renewDeadline/retryPeriod), giving the leader a 20s budget to ride out apiserver or CPU starvation stalls without losing the lease. Trade-off: crash-failover (leader dies without releasing the lease) now takes up to ~35s instead of ~17s; voluntary handoffs (rolling update, graceful shutdown) still release the lease immediately. Tune via `spec.controller.leaderElection` (#57).
- The controller now runs the `HAProxyTemplateConfig`'s embedded `validationTests` at **startup/load**, not only on live changes, and **crash-loops** (stays un-`/healthz`, so the liveness probe restarts the pod) if they fail. Previously a fresh or restarted controller — every `helm upgrade` restarts the controllers — loaded the config after only structural validation and served it even if its `validationTests` failed; only a live edit to an already-running controller was gated. A rolling upgrade now stalls on the old, good pods instead of rolling out a config that fails its own tests. Both gates share one code path so they can't drift.
- Rapid successive `HAProxyTemplateConfig` edits now coalesce: when several config changes are queued, the controller validates only the latest, skipping superseded ones rather than running the (now multi-second) `validationTests` suite against each in turn. The operator's most recent config is what gets validated and applied.
- `store: on-demand` watched resources now keep far less memory resident: the underlying informer strips each object's body to just the `indexBy`, field-selector, and identity fields, dropping large payloads (e.g. Secret `data`, plus the configured ignore-fields such as `metadata.managedFields` and the `last-applied-configuration` annotation that otherwise duplicates the whole body) from the in-memory cache. Templates still read the full object — it is fetched live on access — so rendering behavior is unchanged. See [ADR-0012](./docs/adr/0012-on-demand-projection-and-access-gated-reconcile.md).
- All controller log output now shares one logfmt format. client-go's leader-election/informer logs (klog) and automemlimit's `GOMEMLIMIT is updated` line are routed through the structured slog handler instead of their own formats; every message starts with a capital letter; resource counts are emitted as individual `resource_counts.<type>=N` keys instead of a non-parseable Go `map[...]` literal; and startup labels all eight stages at INFO (previously only 5–8).
- Reduced steady-state INFO log noise: routine per-request/per-reconcile success and housekeeping lines now log at DEBUG instead of INFO — successful admission-webhook validations (denials and warning-bearing admissions stay INFO), runtime-eligible fast-path server applies, HTTP-content refresh/validation/promotion/eviction, validation-test no-ops and completions, unreferenced aux-file cleanup, and pod-reference/stale-status cleanup on pod termination. Failures, denials, config/credential changes, leadership transitions, and HAProxy-pod topology changes remain at INFO. Set `LOG_LEVEL=DEBUG` to restore the previous verbosity.
- Endpoint changes now propagate immediately while reload-inducing structural changes stay lenient. The reconciler-level refractory and `spec.controller.reconciliationDebounceInterval` were **removed** (the reconciler fires on every change); batching is per-watcher, with `spec.watchedResources.<name>.debounceInterval` default raised from 100ms to 2s and `"0"` firing on every change. Reload throttling remains the deployer's `minDeploymentInterval`.
- Runtime-eligible server changes (pod-IP rotation, port, admin state) now reach the live HAProxy workers in ~ms via a runtime fast path that bypasses the deployment interval, instead of waiting on the O(config-size) scheduled deploy; structural changes still take the rate-limited reload path. The change persists across reloads because the deploy re-renders current endpoints (ADR-0011).
- Map file content changes (routing host/path/weight maps) now apply to the live HAProxy workers via the runtime API without a reload, instead of forcing one. Only content updates to existing maps qualify; adding or removing a map file, or any other auxiliary-file change (crt-lists, general/error files), still reloads. A runtime apply failure falls back to a reload, so the result always converges.
- TLS certificate content changes (e.g. a cert-manager renewal of an existing certificate) now apply to the live HAProxy workers via the runtime API without a reload, on HAProxy 3.2+ (`set ssl cert` + `commit ssl cert`). Adding or removing a certificate, crt-list changes, and HAProxy <3.2 still reload. A runtime apply failure falls back to a reload.
- Frontend `maxconn` changes now apply to the live HAProxy workers via the runtime API (`set maxconn frontend`, carried as the `SetFrontendMaxConn` runtime action) without a reload. Previously a maxconn-only change forced a reload even though it was listed as runtime-eligible. Any other frontend attribute change in the same update, or unsetting maxconn, still reloads. Works on all supported HAProxy versions.
- SSL CA-file content changes — mTLS trust bundles for `BackendTLSPolicy` backend verification and Gateway frontend client-cert verification — now apply to the live HAProxy workers via the runtime API without a reload, on HAProxy 3.2+ (`add ssl ca-file` + `commit ssl ca-file`, which replaces the bundle content). This makes frequent trust-bundle rotation (e.g. SPIFFE/SPIRE SVID roots with hourly TTLs) reload-free. Adding or removing a CA file, and HAProxy <3.2, still reload. A runtime apply failure falls back to a reload.
- `controller validate` now requires `--schema-dir` (or `HAPTIC_SCHEMA_DIR`) for typed-access templates; the embedded built-in Gateway schema fallback was removed. `scripts/test-templates.sh` auto-wires `tests/schemas`. Production (live apiserver) is unaffected. See [ADR-0010](./docs/adr/0010-typed-watched-resources.md).

### Fixed

- Deleting an unreferenced TLS certificate (or crt-list) from HAProxy storage no longer lets the DataPlane API schedule a second, uncoordinated reload after the deploy's own reload. That stray reload blacked out the master CLI socket ~0.5–1s after every certificate-removing deploy, so runtime fast-path `set server` updates landing in that window failed and retried while the outgoing worker drained with a stale server list — observed as a single 503 during a single-replica rolling restart when a backend rollout coincided with the window (#67). Storage deletes now send `skip_reload=true`, matching the existing create/update behavior; the deploy pipeline's config push remains the only reload trigger.
- Config validation no longer blocks the config-change event loop: the validation scatter-gather (dominated by the embedded `validationTests` suite, 13-15s+ under load) now runs asynchronously with single-flight/latest-wins ordering. Previously a leadership acquisition during an in-flight validation (e.g. right after a CRD-triggered reinitialization) left the leader-only config publisher without a template config for the whole validation — no `HAProxyCfg` spec update could be written in that window (#55).
- The pure-runtime deploy lane now verifies runtime map updates by read-back after apply and falls back to a reload when the live map did not converge. Previously a runtime map write acknowledged by the DataPlane API but lost on the master socket (observed on the haproxytech 3.1 image under reload churn) latched stale routing — the on-disk file already matched the desired state, so no later deploy re-synced the map and requests kept hitting `default_backend` until an unrelated reload (#48).
- A replica that genuinely loses the leader-election lease (renewal missed under apiserver/CPU starvation) now reinitializes and re-enters the election loop instead of running as a permanent follower with a dead elector — client-go's election loop exits for good on a lost lease, so previously the replica could never lead again until a config change or pod restart; on single-replica deployments that meant a permanent deployment stall (#57).
- A structural deploy no longer trusts an empty config diff against a headerless on-disk config (one whose last write was a `skip_version` runtime push): it now forces one reload to activate the pushed body. Previously the runtime fast path could park a render carrying structural changes on the pod's disk without a reload — the dataplane writes the body even when the accompanying runtime actions fail — and the next deploy would fetch that disk state, see "no changes", report success, and mark Gateways `Programmed` while the running worker still served the old config; convergence then waited for an unrelated structural change (observed as 90s of refused connections on new TCP listeners in gateway-conformance shard 4, issue #56). A successful authoritative runtime-raw apply now also re-stamps the config's `# _version` header (one versioned skip_reload push) so the pure runtime lane stays reload-free across later syncs.
- The `transitionTime()` template helper now preserves condition `lastTransitionTime` for typed (schema-loaded) watched resources — previously it only handled map-shaped conditions, so in production every render re-stamped the timestamp, and each unchanged GatewayClass/Gateway/route/policy status was SSA-rewritten ~1/s under load, feeding watch events back into reconciliation; BackendTLSPolicy additionally now matches its existing `status.ancestors` entry by controllerName + ancestorRef instead of navigating the nonexistent `.status.parents` path (#63).
- The load-path `validationTests` gate now runs with its own 120s suite budget instead of inheriting the live change gate's 25s scatter-gather budget. On slow or contended nodes the engine compile plus the `haproxy -c` sweep legitimately exceed 25s at startup, which crash-looped fresh pods with "validationTests did not complete within the suite timeout" despite a valid config.
- The CRD watch re-checks an inconclusive re-resolution on a bounded 5s cadence (until stable-equal for 3 consecutive checks) instead of accepting a single "no change" answer — a re-resolution racing the apiserver's discovery-propagation lag after a CRD apply no longer stalls the reload permanently (#59).
- Resource status conditions no longer race the infrastructure they describe: the "rendered" status variant (e.g. a Gateway's `Accepted=True`) is now applied only after the same render's `spec.k8sResources` (e.g. per-Gateway Services carrying the `gateway.networking.k8s.io/gateway-name` label) have been applied. Previously the status applier and resource applier consumed sibling events in parallel, so a consumer reacting to `Accepted=True` could list the Gateway's generated infrastructure before it existed.
- Components with latest-wins event semantics (status applier, resource applier, deployer) now drain their event subscription through an unbounded coalescing mailbox: an intake goroutine empties the bus-side buffer immediately and collapses uninterrupted runs of coalescible events to their latest element, so a slow handler (e.g. status applies taking seconds under churn) can no longer cause the event bus to overflow the subscription buffer and drop events. Each component declares which event types are latest-wins for it (the status applier coalesces rendered/deployment events; the deployer deliberately not deployment-completed, which carries its per-deploy bookkeeping), and status applies fan out wider so the apply keeps pace with the deploy cadence. Previously a sustained burst could drop the final events of the burst — including deployment-completed events — leaving resource status stale until the next config change or drift check.
- A pod's `deployedToPods` status entry is no longer silently lost when its deployment completes before the initial `HAProxyCfg` publish lands (a startup race): the per-pod status write previously swallowed the NotFound as success and — with subsequent no-op deploys deduped on the unchanged checksum — never retried, leaving the pod unreported until the next config change or drift check. The update is now requeued and retried until the resource exists.
- Event coalescing no longer starves the coalesced event type under sustained load: the drain previously held the latest coalescible event back until the subscription channel emptied, so during long bursts (e.g. continuous resource churn) rendered status patches could stall for the whole burst while deployment-completed events kept being handled — and later events of other types were dispatched ahead of the older held event. Coalescing now flushes the held event at every run boundary, preserving arrival order across event types.
- Reload-free map-only and certificate-only updates no longer fail on HAProxy 3.4: a skip-reload config push with no runtime server actions previously sent an empty `X-Runtime-Actions` header, which HAProxy 3.4's DataPlane API rejects (`empty value is not allowed`); the header is now omitted when there are no actions to apply.
- A failed deployment to a pod no longer records the new config checksum on `HAProxyCfg.status.deployedToPods[].checksum`, which previously made a partial/failed rollout read as fully converged (the checksum matched `spec.checksum` even though the pod was serving stale config). The pod now keeps its last successfully-deployed checksum plus a `lastError`, so checksum-equality consumers correctly see it as not-yet-converged.
- A partial or failed deployment is no longer cached as the "last deployed" config hash, so the controller re-attempts it on the next reconcile instead of suppressing the retry until the config changes or the drift-prevention timer fires (previously up to `driftPreventionInterval` of delayed self-heal).
- DataPlane API minor versions newer than the newest bundled client (v3.3) now clamp down to it instead of selecting the oldest, most-restrictive v3.0 client; the client-selection and schema-validator version-resolution paths now agree.
- The `_global` shared-fixtures entry in `validationTests` is no longer executed as a standalone test — it inflated the reported test count and ran its assertions against an empty-fixture render. Its fixtures still merge into every real test.
- Watched-resource admission (e.g. Ingress) now uses the same ~9 s internal validation deadline as `HAProxyTemplateConfig` admission (was 5 s), so a large-config dry-run is not prematurely cut off and admitted-without-validation under the webhook's 10 s budget.
- Config-publisher Kubernetes API calls (CRD publish, pod status updates, pod-reference cleanup) now derive from the controller's lifecycle context, so they cancel promptly on shutdown or config-triggered iteration restart instead of running against a background context; the final pre-shutdown flush still completes via a detached context.
- Hardened controller component lifecycle: leader-only components (DeploymentScheduler, DriftPreventionMonitor, ConfigPublisher) now unsubscribe from the event bus on shutdown, so a pod that re-acquires leadership no longer stacks orphaned subscriptions that fill up and log "critical drops" indefinitely; the StatusUpdater's `Stop` is now idempotent (no panic on a double stop); and the commentator, debug event buffer, metrics, and HTTP-store event loops now recover from a handler panic instead of silently killing the goroutine.
- `controller validate` and production now build identical template render contexts. The `capabilities` map is exposed in validation/benchmark renders too (it was production-only, so capability-gated templates could validate differently than they ran), and production now always populates `extraContext` (an empty map when unset, avoiding nil panics in `extraContext | dig(...)`) and promotes its top-level keys (e.g. `{{ debug.enabled }}`) the same way validation already did. Previously a template could pass `controller validate` yet render differently in production.
- Fixed a potential permanent config drift after runtime fast-path applies: a skip_version push leaves the pod's config without a `# _version` header (it reads as version 1 for any body), and the version cache could falsely treat a later "version 1" reading as unchanged and skip a needed sync. Version 1 is now treated as unknown state — never a cache hit, never cached.
- HAProxy 3.3 configurations are now schema-validated against the bundled v3.3 DataPlane API schema; previously they were validated against the v3.2 schema even though the v3.3 validators shipped with 3.3 support.
- HAProxy target paths (map/cert/general-file references in rendered configs and DataPlane API identifiers) are now handled with slash-only path semantics, so they no longer pick up the host OS path separator when the controller binary runs on Windows (no-op on Linux).
- Eliminated intermittent 503s during rolling restarts of single-replica backends: a newly-Ready pod's endpoint change now reaches the live workers within `option redispatch`'s rescue window even when it coalesces with another tenant's in-flight or interval-gated structural deploy, and runtime applies retry across a concurrent reload (master socket briefly down).
- Deployment scheduler now bounds reloads to ≤1 per `minDeploymentInterval` under concurrent churn, eliminating a reload storm that could surface as `<BADREQ>` 400s or dropped connections on the h2c loopback.
- Auxiliary-file UPDATEs no longer trigger a separate dataplane auto-reload; all aux changes batch into the main config sync's single reload, fixing SPOE-group and crt-list shrink races.
- Credentials-Secret rotation now triggers a controller iteration restart, and `SingleWatcher` no longer drops content updates on resources (Secrets/ConfigMaps) that keep `generation` at 0 (the generation-unchanged skip now applies only when a status subresource exists).
- `default_backend` now returns a gRPC trailers-only response (`grpc-status: 12`) for `application/grpc` requests instead of a plain 404 that tore down the HTTP/2 connection.
- GRPCRoute method matching now routes correctly on TLS+ALPN-h2 and plaintext h2c, and gRPC over h2c is multiplexed with HTTP/1.1 on the same bind.
- Gateway TLS listeners on non-default ports now bind (Terminate via crt-list + SNI routing, Passthrough via `req_ssl_sni`); TLS listeners in `ProtocolConflict` report `supportedKinds: []`; TLSRoute mixed-termination listeners report `Accepted=False/ProtocolConflict`.
- Gateway frontend/backend client-certificate validation: hostname-less listeners verify against the configured CA, backend `clientCertificateRef` emits `crt`, and per-listener mTLS / ListenerSet hostname-protocol conflict statuses now land correctly.
- Per-Gateway marker / static-address Service names stay within the 63-char limit and land in the Gateway's own namespace; fixed a Scriggo register-allocation bug that corrupted their discovery labels.
- Renderer now fails fast when the config references a map file it didn't register, instead of deleting it as unreferenced and breaking every subsequent reload.
- Render-time view of the watched-resource stores is pinned per render across `List()`/`Fetch()`/`GetSingle()`, so one snippet can no longer iterate a different resource set than another within a single render.
- `StoreWrapper` no longer triggers a full `Store.List()` on per-key reads against `store: on-demand` resources (was one API fetch per cached reference on every drift cycle).
- Debug introspection endpoints (`/debug/vars/resources`, `/debug/vars/state`, `/debug/vars/all`) no longer trigger one kube-apiserver lookup per resource for `store: on-demand` types; resource counts are read from the in-memory reference index instead.
- Deployer's per-pod version cache now stores each pod's actual post-sync state, so cross-pod drift is detected and fixed up instead of silently serving a stale config.
- Deployment scheduler no longer skips a deployment after a render race left a stale `lastDeployedConfigHash`, which could keep a newer render from ever reaching HAProxy.
- HTTP resource store no longer misclassifies an empty `200 OK` (no ETag) as `304 Not Modified`, which kept serving stale content.
- SSL-certificate Secret publishing now retries on write conflict and `AlreadyExists`, matching the other auxiliary-file publish paths.
- Backend comparator now detects `default-server` parameter changes, so a backend whose only delta is on its `default-server` line reloads instead of silently staying on stale settings.
- `dig()` now returns nil for absent optional fields on typed globals, so `dig(obj, "field") | fallback(default)` behaves identically for typed and untyped resources (#52).
- Gateway/Ingress resources whose addition produces no config diff (e.g. a Gateway with no attached routes) now get their `Programmed` status written instead of sticking at `Programmed=Unknown`; status patches travel on the deploy event, fixing a stale-status race under render churn.
- Status patches, admission overlays, and owned-resource apply now resolve a watched resource's plural via the cluster RESTMapper instead of a built-in pluralization guess, so a CRD with an irregular served plural no longer targets a nonexistent GroupVersionResource.
- Controller no longer schedules a spurious iteration restart ~1s after every startup.
- Bundled SPOA hub updated to v0.7.3: reloads now quiesce in-flight SPOE dispatches before retiring the old plugin set (fixing mirror losses around config reloads, #47), unhandled SPOE messages are surfaced via WARN log and `spoa_messages_unhandled_total`, reload drains in-flight mirror/OTLP work before swapping, and earlier hub fixes are included (use-after-free segfaults on plugin reload, config-reload conformance flake).

### Removed

- `renderResource()` template function — use `spec.k8sResources` instead.
- The dormant event-driven webhook-cert-rotation pipeline (the `certloader` component, its cert events, the webhook-cert watcher, and the `haptic_webhook_cert_expiry_timestamp_seconds` / `haptic_webhook_cert_rotations_total` metrics) — it was never wired in and never rotated certs; webhook-cert hot-reload (see Added) replaces it.
- `namespaceSelector` on `watchedResources` entries (was never wired up). Scope at the template level, via `labelSelector:`, or with separate controller instances.
- Management of HAProxy `program` sections — `client-native` v6.4.0 dropped the `program` model (HAProxy removed the section in 3.3). Rendered configs may still contain `program` sections on older HAProxy versions, but the controller no longer parses, diffs, or normalizes them.

### Helm chart

#### Added

- The ingress library now surfaces long-degraded by-name backends (#66): when an Ingress references its backend Service port by **name** and that Service is absent, the backend deliberately renders placeholder-only slots (503, see the eventual-consistency fix below) — but a permanent Service-name typo was indistinguishable from a propagation race and stayed silent. The controller now emits a `Warning` Event (reason `BackendUnresolved`) on each affected Ingress, visible in `kubectl describe ingress`, naming every unresolvable Service/port-name reference. The Event is precise — backends that resolved real endpoints through an already-present EndpointSlice don't alarm — and is deleted automatically once the Service appears. Gateway API routes already carry the equivalent spec-correct signal (`ResolvedRefs: False`/`BackendNotFound` on the route's parent status). The ClusterRole gains cluster-wide Event write verbs when the ingress library is enabled.

- Template libraries can now declare default `templatingSettings.extraContext` values (top-level `templatingSettings.extraContext` in the library file / `_index.yaml`). They merge into the rendered config at the lowest precedence, so an operator override via `controller.config.templatingSettings.extraContext.<key>` still wins. Used for library-global tunables that mirror an upstream controller's global ConfigMap setting.
- nginx-ingress library: `nginxHttpRedirectCode` extraContext var (default `308`) sets the status code for `ssl-redirect` / `force-ssl-redirect` HTTP→HTTPS redirects — the equivalent of ingress-nginx's global `http-redirect-code`. Override via `controller.config.templatingSettings.extraContext.nginxHttpRedirectCode` (valid: 301, 302, 303, 307, 308).
- `monitoring.prometheusRule.defaultRules` now ships four more default alerts (each individually toggleable): `configRejected` (a config change refused by the validation gate — the latest change is not live), `haproxyPodsRejected` (discovered HAProxy pods refused admission, e.g. a version mismatch), `noHAProxyPods` (no HAProxy pods to manage — no config reaches the data plane), and `criticalEventsDropped` (event-bus drops — reconciliation work lost). The underlying metrics already existed and the docs already recommended alerting on them; the chart now enables the rules by default.
- nginx-ingress library: `mirror-target` — fire-and-forget request mirroring via the spoa-hub mirror plugin (reuses the Gateway RequestMirror machinery; each mirror-target Ingress gets its own mirror slot — capped at `spoaHub.mirrorStaticMinSlots` — host-gated, with `option http-buffer-request` so request bodies reach the plugin). Only the URL authority of the nginx template is used as the mirror target; `mirror-host` and `mirror-request-body: off` need a mirror-plugin change and are unsupported. Requires `spoaHub.plugins.mirror` enabled (else the render fails with an actionable message).
- nginx-ingress library: `session-cookie-domain` (→ cookie `domain`), and `session-cookie-max-age` / `session-cookie-expires` (→ cookie `attr Max-Age=<s>` — the browser-facing cookie lifetime, not HAProxy's internal `maxlife` stickiness window; HAProxy can't compute an absolute `Expires` date so both map to `Max-Age`; `max-age` takes precedence). (`affinity-mode`, `conditional-samesite-none`, and `change-on-failure` remain unimplemented — no faithful HAProxy CE mapping.)
- nginx-ingress library: `auth-secret-type: auth-map` (Secret data keys are usernames, values are password hashes — alongside the existing `auth-file` htpasswd layout), `proxy-next-upstream`/`-tries` (retry conditions → HAProxy `retry-on` + `retries`; `non_idempotent` and `proxy-next-upstream-timeout` have no CE equivalent and are ignored), `limit-rate`/`limit-rate-after` (per-connection download throttle via `filter bwlim-out` + `set-bandwidth-limit`), and `proxy-redirect-from`/`proxy-redirect-to` (rewrite upstream `Location`/`Refresh` headers via `http-response replace-header`; the `default` mode is unsupported and warns).
- Gateway API `TCPRoute` (L4): a Gateway listener with `protocol: TCP` now forwards raw TCP on its port to the attached TCPRoute's backend Service via a dedicated `mode tcp` frontend → `mode tcp` backend (single-backendRef; weighted splitting and TCPRoute status conditions not yet emitted). The `tcproutes` watch is added only when the (Experimental-channel) TCPRoute CRD is present on the cluster, so Standard-channel installs are unaffected.
- Gateway API HTTPRoute/GRPCRoute session persistence (GEP-1619, `type: Cookie`): `spec.rules[].sessionPersistence` maps to HAProxy `cookie <sessionName|SESSION> insert indirect nocache dynamic [maxlife][maxidle]` + `dynamic-cookie-key` on the referenced backend (per-pod affinity). `type: Header` deferred.
- Gateway API HTTPRoute `retry` (`spec.rules[].retry` → backend `retries` + `retry-on <codes>`; exponential backoff not expressible), per-listener TLS options (GEP-2907 `listeners[].tls.options` → per-bind `ssl-min-ver`/`ssl-max-ver`/`ciphers`/`ciphersuites`), and BackendTLSPolicy `validation.subjectAltNames` (first Hostname-type SAN drives `verifyhost`; URI SANs deferred).
- haproxy-ingress library: `oauth: oauth2_proxy` — a convenience that desugars onto the existing external-auth machinery (no new SPOA plugin). The oauth2-proxy address is derived from the Ingress's own `oauth-uri-prefix` path (default `/oauth2`); it sets `auth-url` (`/oauth2/auth`), `auth-signin` (`/oauth2/start?rd=%[path]`), `auth-method HEAD`, and forwards `oauth-headers` (default `X-Auth-Request-Email`) on success. `auth-url` takes precedence when both are set. Emits an `http://` auth URL, so the spoa-hub external-auth plugin must run with `allow_plaintext` for a plain-HTTP in-cluster oauth2-proxy.
- haproxy-ingress library: `rewrite-target` (path rewriting with nginx-style `$N`→`\N` capture translation), rate limiting (`limit-rps`, `limit-rpm`, `limit-whitelist` — advertised but previously emitting no config), `proxy-body-size` (413 over limit), `ssl-ciphers-backend`/`ssl-cipher-suites-backend` (per-backend TLS cipher control), `agent-check-port`/`-addr`/`-interval`/`-send` (auxiliary agent health check), `default-backend-redirect`/`-code`, `server-alias`/`server-alias-regex` (extra hostnames → primary host's backends), and `config-frontend`/`config-global`/`config-defaults` (raw directive injection into those sections).
- nginx-ingress library: `upstream-vhost`, `x-forwarded-prefix`, `connection-proxy-header` (upstream request-header rewrites), `limit-rpm`/`limit-whitelist` (rate limiting), `session-cookie-secure`/`session-cookie-samesite` (cookie attributes), `permanent-redirect-code`/`temporal-redirect-code` (redirect status override), `from-to-www-redirect`, backend TLS to upstream (`proxy-ssl-secret`/`proxy-ssl-verify`/`proxy-ssl-name`/`proxy-ssl-server-name`/`proxy-ssl-ciphers`/`proxy-ssl-protocols`), `proxy-cookie-domain`/`proxy-cookie-path` (downstream Set-Cookie rewrite), `server-alias`, `default-backend` (per-Ingress catch-all Service), and `satisfy` (`any` OR-combines IP allow-list with auth).
- haproxytech library (`haproxy.org/`): `rate-limit-whitelist` (CIDRs exempt from the rate-limit deny), `ssl-redirect-port` (HTTP→HTTPS redirect to an explicit port), and the previously silent-no-op `check`/`check-interval`/`scale-server-slots` are now honored.
- `spoaHub` values block and a conditionally-rendered SPOA hub sidecar (hub plus seven plugin libraries: mirror, coraza, external-auth, fingerprinting, maxmind, otel, sso-auth), auto-rendered when any plugin is enabled. A `spoa-hub` template library wires the HAProxy-side SPOE config (`backend spoa-hub`, frontend filter, `spoe.conf`), and the controller delivers the hub's runtime `config.toml` via the dataplane API so the file-watching hub reloads without a restart.
- The `HAProxyTemplateConfig` admission webhook now also runs the config's embedded `validationTests` (the same suite the controller's load gate runs), so a config whose tests fail is denied at `kubectl apply`; its `timeoutSeconds` is `10` to give the suite headroom on top of render + `haproxy -c`. `failurePolicy: Ignore` is unchanged, so an unreachable or slow webhook admits-with-warning and the load gate still enforces.
- `controller.validators` block and a validator sidecar (same image, `--validate-socket` mode) consulted by the admission webhook, with readiness/liveness probes guarding the socket. Auto-enabled with the spoa-hub sidecar.
- Gateway API support for `RequestMirror` (via `spoaHub.plugins.mirror`, auto-enabled with the gateway library), `SupportTLSRoute`, `SupportGatewayStaticAddresses` (per-Gateway LoadBalancer Service, multi-IP via MetalLB annotation), `SupportGatewayInfrastructurePropagation` (per-Gateway marker Service carrying `spec.infrastructure` metadata), `SupportListenerSet` routing, and GEP-91 frontend client-cert validation (verify mode, per-reason status, `InsecureFrontendValidationMode` condition).
- nginx-ingress and haproxy-ingress libraries wire the external-auth annotation family (`auth-url`, `auth-signin`, `auth-method`, `auth-headers-request`, `auth-headers-succeed`/`-fail`, client-mTLS `auth-tls-*`) to the SPOA hub's external-auth plugin, plus the Coraza WAF annotations (`/waf`, `modsecurity-snippet`) with per-resource opt-in and a `default-on` dispatch mode.
- `nginx-ingress` template library for `nginx.ingress.kubernetes.io/*` annotation compatibility (disabled by default), and a new `ingress-annotations-compat` scaffold library (level 2.5) providing shared Ingress vendor-annotation macros (SSL passthrough, CIDR access control).
- `https-bind-extra-*` extension point for Gateway HTTPS listeners on non-default ports; SSL-passthrough SNI matcher now supports wildcard listener hostnames.
- HTTPRoute `RequestRedirect` with scheme and port both unset now preserves the inbound listener port in `Location`; per-listener-port routing isolation via a `:<port>` host-map fallback.
- The chart-static `haptic-haproxy` LoadBalancer Service is now rendered via the controller's `spec.k8sResources` (SSA with an `OwnerReference`), folding non-default Gateway listener ports into the same Service.
- `haproxy.initialConfig` values key for the HAProxy bootstrap ConfigMap; `controller.config.dataplane.{configPublishInterval,reloadVerificationTimeout,syncTimeout}` tunables (commented out by default).
- `controller.config.extraContext.hardStopAfter` (default `10s`) emits `hard-stop-after` so old workers don't accumulate across reloads.
- Always-on local `peers localinstance` section (`localpeer local`, listening on a unix socket so it can never collide with a frontend/Gateway listener bind) so stick-tables that opt in survive reloads; `haproxy.org/rate-limit-*` and `nginx.ingress.kubernetes.io/limit-rps`/`limit-connections` counters now persist across config reloads (replicated old-worker→new-worker, per-pod, no cross-replica sync) instead of resetting and granting every client a fresh budget.
- `haproxy.dataplane.aclFormat` to override the dataplane access-log format (adds per-request μs timing); test profiles opt in.
- ClusterRole now grants `customresourcedefinitions` get/list/watch (typebootstrap schema resolution) and, when the gateway library is enabled, cluster-wide `services` write verbs; a namespace-scoped Role grants `services` for same-namespace per-Gateway Services. New `extraContext` keys `controllerNamespace` and `haproxyPodSelector`.
- HAProxy NetworkPolicy opens to all TCP ports when `haproxy.networkPolicy.allowExternal: true` so dynamic Gateway listener ports work; restrictive mode unchanged.

#### Fixed

- An Ingress that references its backend Service by port **name** no longer fails rendering when that Service isn't in the controller's store yet. Kubernetes is eventually consistent — an Ingress may legally be created before its Service, and the admission webhook's dry-run render can run before a just-created Service reaches the watch cache — but the old `ResolveServicePort: Service … not found` hard `fail()` denied such Ingresses at admission (flaked ingress-conformance, #50) and blocked the entire haproxy.cfg render in the reconcile loop until the Service appeared. The backend now renders in a degraded shape (placeholder-only server slots → 503 for that route; the named port is resolved from an already-present EndpointSlice when possible) and converges on a later reconcile. A Service that exists but lacks the named port still fails loudly — that's a config error, not a propagation race.
- The gateway library's Gateway status patch no longer fails on pre-v1.5 Gateway API schemas: the rendered variant emits `attachedListenerSets` only when the ListenerSets watch is active (older Gateway CRDs don't declare the field, which failed the whole patch at apply), and the deployed/deployFailed listener entries now carry `supportedKinds` (required by v1.1/v1.4 schemas) so any phase-apply order is valid (#59).
- The gateway library now supports every Gateway API release, resolved at RUNTIME: each gateway kind is an optional watched resource with an ordered `apiVersions` candidate list (core kinds v1/v1beta1; GRPCRoute v1/v1alpha2; TLSRoute v1/v1alpha3/v1alpha2; TCPRoute v1/v1alpha2; ReferenceGrant v1/v1beta1; BackendTLSPolicy v1/v1alpha3 — v1alpha2 excluded as an incompatible shape). The Helm-render-time `.Capabilities` gate and the TCPRoute version-adaptive `inject`/`unset` machinery are removed: installing or upgrading Gateway API CRDs activates support without `helm upgrade`. Features whose fields don't exist in an older release's schemas stay inactive there; everything else works (pinned per release by `tests/schemas-ga-*` and `scripts/test-templates.sh`).
- The GatewayClass object is now created at runtime by the controller (Server-Side Apply, owned by the HAProxyTemplateConfig) instead of by a Helm template, so it exists exactly when the `gatewayclasses` CRD is served. **Upgrade note:** `helm upgrade` removes the previously Helm-owned GatewayClass and the controller recreates it within one reconcile; existing Gateways are unaffected (the class is re-established immediately).
- ClusterRole and ValidatingWebhookConfiguration rules cover every declared API-version candidate of each watched resource.

- The gateway library no longer crash-loops the controller on clusters whose Gateway API install doesn't serve the TCPRoute CRD (every Standard-channel release ≤ v1.5). The `noTCPRoute` strip was incomplete: shared status snippets referenced the typed `resources.tcproutes` store directly (a compile error once the watch is stripped), two TCPRoute status snippets and three TCPRoute validationTests survived the strip and failed the fatal load gate. TCPRoute status registration now lives in its own `status-patches-*` extension-point snippet, the Gateway attachedRoutes count reads TCPRoutes through a compile-safe `render … default ""` seam, and the strip list covers every TCPRoute snippet and test (pinned by a helm-unittest invariant).

- Per-Gateway pod-port allocation is now a pure hash of the (Gateway, port) key instead of read-back from committed Services. The read-back design could permanently bind two Gateways to one pod port (plaintext requests answered with TLS alerts by the neighbour's ssl bind) or oscillate Service targetPorts indefinitely under Gateway churn, because the cluster cache lags the allocator's own output. Ports now come from a deterministic sha256 slot in [18000, 19000) (`perGatewayPodPortBase`/`perGatewayPodPortRange`), so allocations are stable across renders, replicas, and restarts; existing installs may see per-Gateway target ports move once on upgrade (internal pod ports only — LB IPs and listener ports are unaffected).

- The controller's startup probe is now enabled by default. Controller startup includes running the config's embedded `validationTests` (which grow with the chart), so time-to-first-healthy can exceed the liveness budget on slow or contended nodes — the liveness probe then kills the controller mid-initialization and the pod crash-loops forever ("Liveness probe failed: 503"). With the startup probe on (30 × 10s budget), liveness and readiness only engage after initialization completes.
- The default Coraza WAF directives were silently detection-only: `SecRuleEngine On` was placed BEFORE `Include @coraza.conf-recommended`, and that include itself sets `SecRuleEngine DetectionOnly`, overriding the earlier `On` (directives are processed sequentially). Rules — including per-Ingress `modsecurity-snippet` rules — matched and logged but never blocked (no 403). The default now puts `SecRuleEngine On` after the includes. Operators with custom `spoaHub.plugins.coraza.directives` should check their own ordering.
- Backend-level annotation directives (e.g. haproxytech/nginx/haproxy-ingress rate-limiting `stick-table`/`track-sc0`/`deny` and their `# library/…` provenance comments) now render indented to 2 spaces like the rest of the backend body, instead of flush-left at column 0. The `backend-directives-*` extension point was the only backend-body injection point missing `| indent(2, true)` (the other section-body globs already had it; map/global/backends globs correctly emit at column 0). Cosmetic only — HAProxy is whitespace-insensitive.
- The `# Path matching: …` and `# Extension point: advanced matchers …` comments in the frontend routing logic no longer render fused onto an adjacent directive/comment line (a whitespace-control artifact at the snippet-render boundaries). Cosmetic only.
- `host.map` no longer contains dead regex entries and a stray `# gateway/map-host-regex-gateway` provenance marker. The exact-host `render_glob "map-host-*"` was over-matching the wildcard-regex snippets (which share the `map-host-` prefix), duplicating their `^…$` entries (never matched by an exact `map()` lookup) and their comment into `host.map`. The regex snippets are renamed to the `map-hostregex-*` extension point so the two no longer collide; `host-regex.map` is unchanged. Operator-defined `map-host-*` snippets are unaffected.
- Several map-file headers (`host.map`, `host-regex.map`, `path-regex.map`, `weighted-multi-backend.map`) no longer render their leading comment fused onto the first provenance marker (leading-strip `{{-` artifact).
- Removed an inaccurate `# Note: dynamic-cookie-key would be set by base template …` comment from haproxytech cookie-persistence backends — no `dynamic-cookie-key` is ever emitted, so each HAProxy instance derives its own cookie key (the docs example and note are corrected to match).

- Gateway API `validationTests` that assert experimental-channel HTTPRoute features (`sessionPersistence`, `retry`) are now skipped unless the new `controller.templateLibraries.gateway.experimentalChannel` value is set true. As of Gateway API v1.6 the Standard and Experimental installs ship an identical CRD set (TCPRoute, ListenerSet, UDPRoute et al. graduated to Standard), so the difference is field-level additions to HTTPRoute that Helm `.Capabilities` cannot detect — a GVK-presence check (e.g. TCPRoute) no longer distinguishes the channels and wrongly enabled these tests on Standard-channel clusters, where the asserted directives are correctly absent; with the fatal load gate that crash-looped the controller. Operators on the Experimental channel set the flag to keep the self-tests; offline CI sets it to exercise them against the experimental schemas.
- The Gateway API TCPRoute watch is now version-adaptive (`gateway.networking.k8s.io/v1` on v1.6+, `v1alpha2` on older releases), selected from the served CRD version; the feature is wired whenever the TCPRoute CRD is served at either version and stripped when it is served at neither.
- The bundled `validationTests` now pass in **any** release namespace, not only `haproxy-haptic`/`default`. The `_global` SSL fixture's `default-ssl-cert` Secret is now rewritten to the same namespace `default.pem` looks it up in (`default_ssl_cert_namespace`, i.e. the release namespace); previously it hardcoded `haproxy-haptic`/`default`, so in any other namespace the default-cert render failed and most tests failed. Previously latent (the suite was only run offline in the `default` namespace and non-fatally at controller startup); it matters now that the controller runs `validationTests` fatally on load.
- spoa-hub/haproxy-ingress coraza WAF `validationTests` that consume `nginx.ingress.kubernetes.io/*` annotations are now skipped when the `nginxIngress` library is disabled, instead of shipping and failing at runtime. The coraza plugin auto-enables whenever `haproxyIngress` (or `nginxIngress`) is on, so a common profile — `haproxyIngress` enabled, `nginxIngress` disabled — loaded the spoa-hub library and its coraza tests but not the nginx-ingress scan that produces the maps those tests assert on, surfacing 11 spurious `validationTests failed` errors on every config load. Gated via a new `haptic.nginxIngress.disabled` helper.
- nginx-ingress library: `ssl-redirect` now emits a **308** redirect (was `301`), matching ingress-nginx — which sends both `ssl-redirect` and `force-ssl-redirect` via its `http-redirect-code` (default 308). 308 preserves the request method on POST where 301 may downgrade it to GET. Override the code with the new `nginxHttpRedirectCode` extraContext var.
- The chart now auto-wires the `spoa-hub` validator: when the spoa-hub sidecar and the validator sidecar are both enabled, `spec.validators` gains a `spoa-hub` entry routing the rendered `general/spoa-hub-config.toml` to the `--validate-socket` sidecar, so admission-time WAF/TOML validation (ADR-0007) works without hand-adding `controller.validators.entries`. An operator-provided `spoa-hub` entry still takes precedence.
- The chart now fails fast at install with actionable guidance when `webhook.certManager.enabled=true` but the cert-manager CRDs are absent on the target cluster, instead of leaving the controller pod stuck in `ContainerCreating` waiting for a webhook-cert Secret that is never created. The check is liveness-gated, so `helm template` and offline linting are unaffected.
- `haproxy.org/check`, `haproxy.org/check-interval`, and `haproxy.org/scale-server-slots` are now honored instead of being silently ignored: `check: "false"` emits `default-server no-check`, `check-interval` emits `inter <duration>`, and `scale-server-slots` now drives the pre-allocated server-slot count (the value was computed but never passed to the backend renderer).
- haproxy-ingress `maxconn-server`, `maxqueue-server`, `initial-weight`, `backend-check-interval`, `health-check-port`, `health-check-fall-count`, and `health-check-rise-count` are now honored: each emits its `default-server` keyword (`maxconn`/`maxqueue`/`weight`/`inter`/`port`/`fall`/`rise`). The values were validated and stored but never rendered onto the server line.
- haproxytech IP access control is now keyed on the canonical upstream annotations `haproxy.org/allow-list` / `haproxy.org/deny-list` (with the deprecated `haproxy.org/whitelist` / `haproxy.org/blacklist` aliases honored as a fallback). The library previously bound the non-existent one-word `allowlist`/`denylist` keys, so the real annotations emitted no ACL.
- haproxy-ingress `limit-rps`/`limit-rpm` rate-limit stick-tables now carry `peers localinstance`, so per-source counters survive HAProxy reloads instead of resetting to zero (matching the haproxytech and nginx-ingress libraries).
- haproxy-ingress honours the deprecated `whitelist-source-range` annotation as an alias of `allowlist-source-range` (ignored when the canonical key is also set, matching upstream); it was previously read by no snippet.
- nginx-ingress `session-cookie-hash` now emits a warning comment when set instead of being silently ignored — HAProxy uses `dynamic` cookies whose hash is not operator-selectable, so `md5`/`sha1`/`index` cannot be honoured. The `_index` "supported" claim is corrected accordingly.
- `defaults` block now sets `option redispatch` and `base.yaml` filters out not-ready / terminating endpoints, eliminating the single-replica rolling-restart 503 windows.
- Reserved-slot server addresses are now config-driven across reloads (removed the HAProxy server-state-file machinery); placeholders moved to the unroutable `192.0.2.1:1` sentinel. See [ADR-0011](./docs/adr/0011-no-haproxy-server-state-file.md).
- `haproxy.dataplane.validateConfig: false` now actually skips the dataplane's `haproxy -c` (the flag was misplaced under `haproxy:` instead of `haproxy.reload.*`), cutting raw-config push time ~130ms → ~18ms.
- Named Service-port references (`port.name`) in Ingress / HTTPRoute / GRPCRoute and SSL-passthrough backends now resolve to the correct numeric port via a shared `ResolveServicePort` helper that fails loud on unresolvable input (was silent 503s / empty backends).
- `haproxy-ingress.github.io/path-type` annotation now actually routes (fixed missing `BACKEND:` qualifier, missing macro call on the regex renderer, and missing wildcard-host normalization).
- Gateway TLS-cert registration treats unspecified `tls.mode` as the spec default `Terminate` (was silently skipping the listener); BackendTLSPolicy with no resolvable CA now returns 503 instead of downgrading to plaintext; TLSRoute status patches target `gateway.networking.k8s.io/v1`.
- Per-resource feature maps (`auth-*`, `coraza-app`, `waf`) are keyed by resource identity, fixing silent auth/WAF skips on regex, prefix, and wildcard-host paths; `modsecurity-snippet` rules are path-scoped per Ingress.
- HAProxy-Ingress / nginx-ingress `auth-headers-*` directives now emit into both the HTTP/1.1 and HTTP/2 cleartext frontends.
- Basic-auth snippets no longer panic when a referenced auth Secret is briefly absent from the render snapshot.
- PrometheusRule default alerts `HAProxyControllerHighQueueDepth` / `HAProxyControllerNoLeader` repointed to metrics the controller actually emits (the old names never fired).
- `networkPolicy.ingress.webhook.from` and `networkPolicy.egress.kubernetesApi` defaults switched to `ipBlock` `0.0.0.0/0` + `::/0`, so the host-network apiserver can reach the webhook on clusters enforcing NetworkPolicy (was silent 502 admission blocks).
- The validating-webhook configuration now sources `watchedResources` from the merged libraries, so library-declared resources are actually validated.
- `features-160-ssl-redirect-map` uses a per-snippet dedup so the map registration and the rule referencing it stay in lockstep under churn.
- `base.yaml` no longer references gateway-library state, restoring the Level-0 resource-agnostic architecture on clusters without Gateway API.
- SPOA mirror plugin no longer flakes the `HTTPRouteRequestMirror` / `MultipleMirrors` / `PercentageMirror` conformance tests (mirror message slots floored at a static minimum; reload drains in-flight dispatches).
- NOTES.txt no longer claims the Gateway API library is enabled when its CRDs are absent (severity downgraded to ℹ️).
- `haproxy.org/pod-maxconn` counts only Running + Ready HAProxy pods.

#### Changed

- `controller.config.controller.leaderElection` timing defaults raised from 15s/10s/2s to 30s/20s/5s (`leaseDuration`/`renewDeadline`/`retryPeriod`), matching the new controller defaults: more headroom against apiserver/CPU starvation stalls, at the cost of slower crash-failover (up to ~35s when a leader dies without releasing the lease; voluntary handoffs are unaffected) (#57).
- The generated `haproxy.cfg` comments are now terse: the multi-paragraph rationale that was inlined into the routing logic, `timeout connect`/`option redispatch`, h2c detection, and the gRPC default-backend is condensed to one or two lines plus a link to the [Base library docs](https://haproxy-haptic.org/helm-chart/latest/libraries/base/). This shrinks the rendered config (the routing-logic comment block was previously emitted in full in each of the three frontends) without changing any directive. The redundant second `path_match_qualifier` re-parse is now emitted only when advanced matchers (gateway library) are present.
- Verbose comment headers across the generated `.map` files (spoa-hub external-auth / Coraza maps, base `body-size`/`reqhdr-*`/`path-rewrite` maps) and several in-config rationale blocks (gateway SNI dispatch / 421 misdirected-request, SSL-passthrough SNI routing, BackendTLSPolicy fail-closed) are condensed to 2-3 lines plus a docs link. Implementation/conformance detail moved to non-rendered template comments or the library docs; no rendered directive changed.
- Runs of blank / whitespace-only lines in the generated `haproxy.cfg` (left behind by empty extension-point snippets and render boundaries) are collapsed to a single blank line via a post-processing pass. Purely cosmetic — HAProxy ignores blank lines.
- The deployed DataPlane API now uses relative storage dirs (`maps`/`ssl`/`general`) resolved against the dataplane container's `workingDir` (the HAProxy base dir), instead of absolute paths. This makes it address runtime maps/certs by the same relative identifier HAProxy uses (from `default-path origin`), so map content changes apply via the runtime API without a reload. Storage files still land in the same absolute dirs; the `dataplane.mapsDir`/`sslCertsDir`/`generalStorageDir` values are unchanged and remain authoritative for the controller's validation path derivation.
- The auto-generated DataPlane API password (`credentials.dataplane.password` left empty) is now a **random** 32-char value instead of a deterministic hash of the release name and namespace. The Dataplane API is HAProxy's runtime control plane over plain HTTP, so a credential derived from public, guessable inputs was a weakness. Existing installs are unaffected — the password is preserved from the existing Secret via `lookup` on `helm upgrade`. **GitOps note:** under ArgoCD/Flux (rendering without cluster lookup), an empty password now regenerates on every sync; set `credentials.dataplane.password` explicitly (e.g. via a SealedSecret) for those setups.
- The validating admission webhook now provisions its own **self-signed TLS certificate by default** (no cert-manager required): `webhook.certManager.enabled` defaults to `false`, and the chart generates a long-lived self-signed cert (`webhook.selfSigned.certValidityDays`, default 10y) that is reused across upgrades (no churn). cert-manager stays opt-in (`webhook.certManager.enabled=true`, recommended for production / automatic rotation); the manual `webhook.caBundle` path is unchanged. Admission validation now works out of the box instead of requiring cert-manager. The self-signed cert is **not auto-rotated** — rotate via cert-manager, or manually by deleting the Secret and running `helm upgrade`. **Upgrade note:** a release that used the previous cert-manager default holds a cert-manager-created webhook Secret (`<fullname>-webhook-cert`) that lacks Helm ownership metadata; because the self-signed default reuses the same Secret name, `helm upgrade` will refuse to adopt it. Before upgrading, either keep cert-manager (`--set webhook.certManager.enabled=true`) or delete the old Secret so the chart can recreate it (`kubectl delete secret <release>-webhook-cert -n <namespace>`).
- `proxy-body-size` (nginx-ingress and haproxy-ingress) is now enforced by a shared resource-agnostic frontend rule reading per-backend limits from a new `body-size.map`, instead of an `http-request deny` inside each backend. Adding or changing a body-size limit is now a map-only, reload-free update; the annotation and the 413 behaviour are unchanged.
- Upstream request-header overrides — `haproxy.org/set-host`, and nginx-ingress `upstream-vhost` / `x-forwarded-prefix` / `connection-proxy-header` — are now applied by shared resource-agnostic frontend rules reading per-backend values from new `reqhdr-host.map` / `reqhdr-xfwd-prefix.map` / `reqhdr-connection.map`, instead of per-backend `http-request set-header`. Changing a value is a map-only, reload-free update; behaviour and the config-injection guard are unchanged.
- `rewrite-target` (nginx-ingress and haproxy-ingress), when the value is a literal path with no `$N`/`\N` capture, is now applied by a shared frontend `set-path` rule reading `path-rewrite.map`, instead of a per-backend `http-request replace-path`. Capture/regex rewrites still render as `replace-path` in the backend. Literal-rewrite changes are now map-only and reload-free.
- `ssl-redirect` / `force-ssl-redirect` (nginx-ingress and haproxy-ingress) now register their hosts into the shared host-keyed `ssl-redirect-<code>.map` (the same map haproxytech already uses), instead of emitting a per-ingress inline-ACL `http-request redirect`. ssl.yaml emits the single map-based redirect rule, so adding/removing a redirected host is a map-only, reload-free update. Behaviour and status codes are unchanged.
- Host-keyed redirects — `redirect-to` (haproxy-ingress), `request-redirect` (haproxytech), and `permanent-redirect`/`temporal-redirect` (nginx-ingress) — are now applied by a shared resource-agnostic frontend rule reading per-host target+code from new `redirect-loc-<code>.map` files, instead of a per-ingress inline-ACL `http-request redirect`. Adding or changing a host redirect is now a map-only, reload-free update; the annotations, status codes, and config-injection guards are unchanged.
- HSTS (`hsts` / `hsts-max-age` / `hsts-include-subdomains` / `hsts-preload`, nginx + haproxy-ingress) is now applied by a single shared resource-agnostic rule reading the per-host `Strict-Transport-Security` value from a new `hsts.map`, instead of a per-ingress `http-response set-header`. Changing an HSTS value is now a map-only, reload-free update; the header value and `ssl_fc` gating are unchanged, and `hsts-max-age` gains a config-injection guard.
- The remaining conditional redirects are now map-driven and reload-free too: `app-root` (nginx + haproxy-ingress → shared `app-root.map`), `auth-tls-error-page` mTLS error redirect (nginx + haproxy-ingress → shared `mtls-error.map`; the cert-passthrough set-headers stay inline), `from-to-www-redirect` (nginx → `from-to-www.map`), `default-backend-redirect` (haproxy-ingress → per-code `default-backend-redirect-<code>.map`), and `ssl-redirect-port` (haproxytech → per-code `ssl-redirect-port-<code>.map`). Each replaces a per-ingress inline-ACL redirect with a single shared rule reading the map; the gates (`{ path / }`, `ssl_c_verify`, scheme split, `!backend_name`, explicit port) and status codes are unchanged. `app-root` and `auth-tls-error-page` values now also get a config-injection guard they previously lacked.
- Default `haproxyVersion` is now `3.4` (was `3.2`); the controller and HAProxy pod images default to the HAProxy 3.4 series. Added `3.4` to `haproxyPatchVersions` (and a pending `haproxyEnterprisePatchVersions` entry). Override `haproxyVersion` to stay on an older series.
- gateway library: the cluster-wide `configmaps` watch now defaults to `store: on-demand` (CachedStore) instead of `full`. It is only read by name for BackendTLSPolicy CA bundles and never iterated, so keeping every cluster ConfigMap body resident was pure overhead; on-demand keeps just references resident and fetches bodies live on access (matching the `secrets` watch).
- Template libraries (`gateway`, `nginx-ingress`, `haproxy-ingress`, `haproxytech`) are now packaged as conditional **subcharts** instead of files bundled into the parent. A release stores only the source of the libraries it enables — disabled libraries are pruned from the Helm release Secret — so an install with all libraries enabled no longer exceeds the Kubernetes 1 MiB Secret limit (it previously did, breaking `helm install`). No values changes: the same `controller.templateLibraries.<x>.enabled` flags gate subchart inclusion, and the rendered `HAProxyTemplateConfig` is byte-identical. `base`/`ssl`/`ingress`/`ingress-annotations-compat`/`spoa-hub` remain in the parent.

- **BREAKING**: path matching order is now selected by `controller.config.routing.regexMatchOrder` (`default`/`last`), replacing the removed `path-regex-last` template library. Operators with `templateLibraries.pathRegexLast.enabled: true` must switch to `routing.regexMatchOrder: last`. See [ADR-0005](./docs/adr/0005-path-matching-order-as-values-flag.md).
- **BREAKING**: `ingressClass.name` and `gatewayClass.name` default from `haproxy` to `haptic`. Operators replacing an incumbent controller set them back to `haproxy` (or update their manifests to `ingressClassName: haptic` / `gatewayClassName: haptic`).
- **BREAKING**: pod-spec scheduling, runtime, and metadata fields moved under namespaced `podSpec:` blocks on both Deployments. Container-, Deployment-, and chart-wide fields are unchanged. Rename the following keys in custom values files:

  | Previous | New |
  |---|---|
  | `imagePullSecrets` | `controller.podSpec.imagePullSecrets` |
  | `podAnnotations` | `controller.podSpec.podAnnotations` |
  | `podLabels` | `controller.podSpec.podLabels` |
  | `priorityClassName` | `controller.podSpec.priorityClassName` |
  | `topologySpreadConstraints` | `controller.podSpec.topologySpreadConstraints` |
  | `podSecurityContext` | `controller.podSpec.podSecurityContext` |
  | `nodeSelector` | `controller.podSpec.nodeSelector` |
  | `tolerations` | `controller.podSpec.tolerations` |
  | `affinity` | `controller.podSpec.affinity` |
  | `terminationGracePeriodSeconds` | `controller.podSpec.terminationGracePeriodSeconds` |
  | `dnsPolicy` | `controller.podSpec.dnsPolicy` |
  | `dnsConfig` | `controller.podSpec.dnsConfig` |
  | `hostAliases` | `controller.podSpec.hostAliases` |
  | `runtimeClassName` | `controller.podSpec.runtimeClassName` |
  | `haproxy.priorityClassName` | `haproxy.podSpec.priorityClassName` |
  | `haproxy.topologySpreadConstraints` | `haproxy.podSpec.topologySpreadConstraints` |
  | `haproxy.nodeSelector` | `haproxy.podSpec.nodeSelector` |
  | `haproxy.tolerations` | `haproxy.podSpec.tolerations` |
  | `haproxy.affinity` | `haproxy.podSpec.affinity` |
  | `haproxy.dnsPolicy` | `haproxy.podSpec.dnsPolicy` |
  | `haproxy.dnsConfig` | `haproxy.podSpec.dnsConfig` |
  | `haproxy.hostAliases` | `haproxy.podSpec.hostAliases` |
  | `haproxy.runtimeClassName` | `haproxy.podSpec.runtimeClassName` |
  | `haproxy.podAnnotations` | `haproxy.podSpec.podAnnotations` |
  | `haproxy.shareProcessNamespace` | `haproxy.podSpec.shareProcessNamespace` |
  | `haproxy.terminationGracePeriodSeconds` | `haproxy.podSpec.terminationGracePeriodSeconds` |
  | `haproxy.podSecurityContext` | `haproxy.podSpec.podSecurityContext` |

- `haproxy.ports.http`/`https` defaults shift from `8080`/`8443` to `80`/`443` so `dst_port` equals the Gateway listener port; operators who set them explicitly keep their override.
- Dataplane `minDeploymentInterval` default `0s` → `5s` (matches haproxytech `--sync-period`), throttling reload-inducing structural deploys; endpoint changes still apply instantly via the runtime fast path.
- Default per-watcher `debounceInterval` raised from 100ms to 2s; EndpointSlice keeps `"0"` for instant rolling-restart reaction. Removed the `reconciliationDebounceInterval` knob.
- **HAProxy `defaults` `timeout connect` lowered from `5000` to `100` (100 ms).** Backends are pod IPs over the CNI; 100 ms fails fast on a SYN to a just-terminated pod so `option redispatch` retries. Operators on slow networks restore `5000` via `extraContext.timeout_connect`.
- Bundled SPOA image updated to hub `v0.7.3` with the v3-consumer plugin releases (mirror `v0.5.0`, coraza `v0.5.0`, external-auth `v0.5.0`, fingerprinting `v0.3.0`, maxmind `v0.4.0`, otel `v0.5.0`, sso-auth `v0.3.0`); reloads now quiesce in-flight SPOE dispatches before retiring the old plugin set (#47), unhandled SPOE messages surface via WARN + `spoa_messages_unhandled_total`, and reload still drains in-flight work before swapping.
- The mirror `messages` list is sized dynamically per render (floored at `spoaHub.mirrorStaticMinSlots`, default 4), with per-dispatch timeout/retries derived from `timeout server`/`retries`; HTTPS mirror targets dispatch over TLS, and `worker_threads` / `pool_max_idle_per_host` params are tunable.
- `spoaHub.plugins.coraza.enabled` default broadened to also follow `haproxyIngress.enabled` (coraza wired on chart defaults); `coraza.directives` is now its own values field — operators upgrading must move directives out of `params`.
- SPOA hub config now delivered via a shared `emptyDir` the controller overwrites via the dataplane API (was a direct ConfigMap mount); the validator sidecar gets a memory-backed `/tmp` for Coraza's pre-compile probe.
- `extraDeploy` now accepts both list and dict formats.
- `haproxy.org/pod-maxconn` quantizes the pod count to the next power of 2 to avoid reload cascades on scaling.
- Compound resource names (derived from `haptic.fullname`) now truncate to the 63-char label limit; releases with very long names will see affected resources renamed on upgrade (run `helm diff upgrade` first).
- Removed the "TLS Certificate Expiry" Grafana dashboard panel and its `haptic_webhook_cert_expiry_timestamp_seconds` query — the controller no longer emits that metric (the webhook cert is now hot-reloaded; see controller CHANGELOG).

#### Security

- CIDR-list annotations (allowlist/denylist source ranges, rate-limit/connection whitelists, `satisfy` whitelist) are now validated against a strict IP/CIDR charset before being interpolated into HAProxy `src` ACLs. Previously a crafted annotation value containing a newline or space could inject arbitrary HAProxy directives into the rendered config (config injection passing `haproxy -c`). The nginx `satisfy` auth realm is likewise rejected if it contains a double-quote or newline.
- Single-value annotations interpolated onto an HAProxy config line — nginx `upstream-vhost`/`x-forwarded-prefix`/`connection-proxy-header` (header values), `proxy-ssl-name`/`proxy-ssl-ciphers`/`proxy-ssl-protocols`, and `proxy-cookie-domain`/`-path` — now reject control characters (and embedded spaces for single-token fields) via a shared `ValidateConfigValue` guard, closing the same newline-injection vector.

#### Removed

- Dead `spoaHub.podSecurityContext` values key (never consumed — use `haproxy.podSpec.podSecurityContext`).

## [0.1.0] - 2026-03-09

### Added

- **Template-driven HAProxy configuration**: Generate HAProxy configs using Scriggo templates (Go-based, Jinja2-like syntax) with full access to Kubernetes resources, built-in utility functions, and modular template snippets
- **Embedded validation tests**: Declarative test fixtures and assertions for testing HAProxy configurations within template libraries; run via `haptic-controller validate --test <name>`
- **Dry-run validation webhook**: Admission webhook that intercepts CREATE/UPDATE on opted-in watched resources (Ingress, HTTPRoute, GRPCRoute by default), renders templates with the proposed object overlaid on the live store, and rejects the write if rendering or HAProxy validation fails
- **Multi-architecture container images**: `linux/amd64`, `linux/arm64`, `linux/arm/v7`
- **HAProxy version support**: 3.0, 3.1, 3.2, 3.3 — version-specific images tagged accordingly
- **Supply chain security**: Container images, binaries, and Helm charts signed with Cosign (keyless OIDC); SBOM attestations in SPDX format
- **Prometheus metrics**: Reconciliation timing, template rendering duration, validation results, and Kubernetes API latencies
- **Leader election for high availability**: Multiple controller replicas with automatic leader election; hot-standby replicas continue watching and validating; configurable failover timing
- **Stall detection**: Components detect when blocked and report unhealthy via `/healthz`, enabling automatic pod restart via Kubernetes liveness probes
- **Configurable deployment timeout**: `deploymentTimeout` in dataplane config (default: 30s) to recover from stuck deployments
- **Server slot preservation**: Preserve HAProxy server slots during rolling deployments to enable zero-reload runtime API updates via `currentConfig` template context
- **HAProxy Ingress annotation compatibility**: 56 `haproxy-ingress.github.io/*` annotations via the haproxy-ingress template library
- **Dataplane API concurrency limiting**: `maxParallel` config option to limit concurrent API operations, preventing timeouts for large configurations
- **CRD content compression**: HAProxyCfg content compressed with zstd when exceeding `configPublishing.compressionThreshold` (default 1 MiB), reducing etcd storage
- **HAProxyGeneralFile CRD**: Publish general files (error pages, etc.) as Kubernetes custom resources with compression support
- **HAProxyCRTListFile CRD**: Publish crt-list files as Kubernetes custom resources with compression support
- **`semver_gte` template filter**: Version comparison for gating features on HAProxy version (e.g., `semver_gte(haproxyVersion, "3.3")`)
- **Template-driven status patches**: Templates can register status patches for any Kubernetes resource via `statusPatch()` function, with outcome-keyed variants (`rendered`, `deployed`, `renderFailed`, `deployFailed`) applied automatically based on pipeline phase
- **Backend diff field diagnostics**: Reconciliation log now includes which BackendBase fields caused backend updates, aiding diagnosis of false diffs from parser round-trip asymmetries
- **Status patch helper functions**: `condition()`, `transitionTime()`, and `toJSON()` template functions for building Kubernetes status conditions with stable transition timestamps

### Changed

- **Reconciliation triggering**: Leading-edge triggering with a 5s refractory period; no latency for isolated changes, bursts during that window are batched into a single reconciliation
- **Parallel Dataplane API operations**: Operations execute in parallel within each priority group, reducing sync time for large configurations
- **Balance directive**: `balance roundrobin` moved to `defaults` section to prevent silent behavior change when upgrading to HAProxy 3.3 (which changed the default balance algorithm from `roundrobin` to `random`)
- **Go runtime 1.26.1**: Green Tea GC replaces manual GOGC tuning

### Helm chart

#### Added

- Initial Helm chart deploying the controller and HAProxy pods (2 replicas by default)
- Separate controller Service (ClusterIP for operational endpoints) and HAProxy Service (configurable LoadBalancer/ClusterIP)
- Default NetworkPolicy for HAProxy instances
- Leader election support with configurable replica count
- Default SSL certificate configuration via `controller.defaultSSLCertificate`
- Modular template library system with composable libraries merged at Helm render time (enable/disable via `controller.templateLibraries.<name>.enabled`):
  - `base.yaml`: Core HAProxy template structure with extension points
  - `ingress.yaml`: Kubernetes Ingress support (path types: Exact, Prefix, ImplementationSpecific; TLS termination; default backend)
  - `gateway.yaml`: Gateway API support — HTTPRoute and GRPCRoute are watched and routed; traffic splitting, request/response header modification, URL rewrites, and Gateway/Route status patches are emitted. TLS/TCP/UDP listeners are reflected in each Gateway's `supportedKinds` status but TLSRoute/TCPRoute/UDPRoute resources are not watched or routed
  - `haproxytech.yaml`: `haproxy.org/*` annotation compatibility (backend config snippets, SSL passthrough, CORS, basic auth)
  - `ssl.yaml`: TLS/SSL features
  - `haproxy-ingress.yaml`: 56 `haproxy-ingress.github.io/*` annotation compatibility (enabled by default)
- Gateway API status reporting: Gateway conditions (Accepted, Programmed), listener status, HTTPRoute/GRPCRoute parent status with Accepted and ResolvedRefs conditions
- Ingress status reporting: LoadBalancer addresses propagated to Ingress `.status.loadBalancer`
- HAProxy built-in Prometheus exporter enabled by default on the status frontend (`/metrics` on port 8404)
- Grafana dashboard annotations for leader transitions and controller pod starts
- Auto-generated Dataplane API credentials stored in a Secret (deterministic 32-char SHA256 of release-name + namespace; preserved across upgrades from the existing Secret)
- `haproxy.sysctls` for setting kernel parameters on HAProxy pods via pod-level securityContext
- `haproxy.podAnnotations` for custom pod annotations on HAProxy pods (supports Helm template expressions)
- `haproxy.shareProcessNamespace` to enable process namespace sharing between containers (required for signal-based sidecar reload, e.g., SPIFFE/SPIRE mTLS agents)
- `haproxy.shmStats.enabled` to persist stats counters across HAProxy reloads via shared memory (requires HAProxy 3.3+); automatically provisions `/dev/shm` emptyDir volume with auto-calculated size
- `haproxy.nbthread` to control HAProxy thread count (auto-calculated from CPU requests by default)
- `haproxy.dataplane.validateConfig` to control server-side config validation
- `haproxy.dataplane.debugSocketPath` to enable Unix socket for runtime profiling of the Dataplane API sidecar
- `controller.config.dataplane.maxParallel` to limit concurrent Dataplane API operations
- `controller.statusPatches.enabled` to disable status patch writes during migration from another ingress controller
- `extraDeploy` for deploying arbitrary Kubernetes resources alongside the chart (supports Helm templating)
- `extraEnv`, `haproxy.extraEnv`, `haproxy.dataplane.extraEnv` for custom environment variables on all containers
- `global-settings-*`, `defaults-settings-*`, and `frontend-extra-*` extension points for customizing HAProxy global/defaults sections and early frontend directives via template snippets
- `status-patches-*` and `status-extra-*` extension points for custom status and Prometheus endpoint configuration
- `template` post-processor type for declarative output transformations in `postProcessing`
- `guid` directives on all frontends, backends, and servers for stable object identification

#### Changed

- Dataplane API credentials consolidated into `credentials.dataplane` section; auto-generated if not provided
- Basic auth userlists are named `auth_<secretNs>_<secretName>` and deduplicated per Secret; each Ingress references its userlist via `http_auth()`. Differs from the official HAProxy Ingress Controller's per-Ingress `{namespace}-{ingressName}` naming so multiple Ingresses sharing the same Secret produce a single userlist (significant speedup for bcrypt hashes)
- Production-ready default resource requests and limits: controller (100m CPU / 512Mi memory), HAProxy (250m CPU / 1Gi memory), dataplane sidecar (50m CPU / 256Mi memory)
- `sidecars`, `extraVolumes`, `extraVolumeMounts` and their `haproxy.*` counterparts support Helm template expressions

#### Removed

- `image.appendHaproxyVersion` value (HAProxy version suffix is now always included in controller image tag)
- `haproxy.dataplane.credentials` section (use `credentials.dataplane` instead)
