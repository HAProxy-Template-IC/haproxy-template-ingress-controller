# Tests expected to FAIL against this Gateway API release's standard-channel
# schemas: they assert features whose FIELDS do not exist in this schema
# generation (the typed fixture wrap prunes unknown fields, so the feature
# never activates). This is feature-absence, not a bug — the controller
# still becomes Ready and everything else routes. The harness in
# scripts/test-templates.sh requires the actual failure set to match this
# file EXACTLY (a new failure or a stale entry both fail CI).
#
# Empty: this bundle is the NO-GATEWAY-API profile (no gateway CRDs at all —
# exactly what a plain-Ingress cluster looks like). Every gateway feature
# strips atomically together with its tests; the base/ingress suite must
# pass untouched. This profile is what caught the fileRegistry-edge gap
# (a surviving extension-point snippet referencing a map registered by a
# stripped features- snippet).
