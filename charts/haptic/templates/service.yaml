apiVersion: v1
kind: Service
metadata:
  name: {{ include "haptic.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "haptic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.controller.ports.healthz }}
      targetPort: healthz
      protocol: TCP
      name: healthz
    - port: {{ .Values.controller.ports.metrics }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "haptic.selectorLabels" . | nindent 4 }}
