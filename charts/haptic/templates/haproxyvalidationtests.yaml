{{/*
The configuration's validationTests, in their own object.

They are ~36% of the merged spec and are needed only when the configuration is
VALIDATED, never when it is rendered — carrying them inline is what pushed a
migration profile past etcd's per-object limit, which is the whole reason this
file exists. The controller runs the union of this object and any inline
spec.validationTests, so a hand-written configuration that keeps them inline is
unaffected.

This is a separate file rather than a second document in
haproxytemplateconfig.yaml so that template still renders exactly one document —
several chart test suites address it by document index.
*/}}
{{- $controller := .Values.controller }}
{{- $mergedLibraries := include "haptic.mergeLibraries" . | fromYaml }}
{{- $tests := merge (deepCopy ($mergedLibraries.validationTests | default dict)) ($controller.config.validationTests | default dict) }}
{{- /* extraContext as the operator set it: the WAF policy refs below are
       operator configuration, not library-declared defaults. */ -}}
{{- $extraContext := (dig "templatingSettings" "extraContext" (dict) ($controller.config | default dict)) }}
  {{- $wafRefs := dig "waf" "policies" "configMapRefs" (list) $extraContext }}
  {{- if gt (len $wafRefs) 0 }}
    {{- /* Policy names must be unique across the whole catalog
           (registerPolicy rejects duplicates), so each injected source
           carries a distinct per-source placeholder, and only the FIRST
           source additionally carries the values-declared defaultPolicy —
           unless an inline policy already defines it. */ -}}
    {{- $defaultPolicy := (dig "waf" "policies" "defaultPolicy" "" $extraContext) | toString }}
    {{- $inlinePolicies := dig "waf" "policies" "inline" (dict) $extraContext }}
    {{- $defaultPolicyNeeded := and (ne $defaultPolicy "") (not (hasKey $inlinePolicies $defaultPolicy)) }}
    {{- $byIdentity := dict }}
    {{- $sourceIndex := 0 }}
    {{- range $ref := $wafRefs }}
      {{- $refNs := ((dig "namespace" "" $ref) | default $.Release.Namespace | toString) }}
      {{- $refName := ((dig "name" "" $ref) | toString) }}
      {{- $refKey := ((dig "key" "" $ref) | default "policies.yaml" | toString) }}
      {{- $id := printf "%s/%s" $refNs $refName }}
      {{- $catalog := printf "haptic-validation-fixture-%d:\n  description: Chart-injected validationTests catalog fixture\n" $sourceIndex }}
      {{- if and $defaultPolicyNeeded (eq $sourceIndex 0) }}
        {{- $catalog = printf "%s%s:\n  description: Chart-injected validationTests catalog fixture\n" $catalog $defaultPolicy }}
      {{- end }}
      {{- $entry := index $byIdentity $id | default (dict "apiVersion" "v1" "kind" "ConfigMap" "metadata" (dict "namespace" $refNs "name" $refName) "data" (dict)) }}
      {{- $_ := set (index $entry "data") $refKey $catalog }}
      {{- $_ := set $byIdentity $id $entry }}
      {{- $sourceIndex = add1 $sourceIndex }}
    {{- end }}
    {{- $vt := $tests }}
    {{- $vtGlobal := (index $vt "_global") | default dict }}
    {{- $vtFixtures := (index $vtGlobal "fixtures") | default dict }}
    {{- $vtCms := (index $vtFixtures "configmaps") | default list }}
    {{- range $id, $entry := $byIdentity }}
      {{- $vtCms = append $vtCms $entry }}
    {{- end }}
    {{- $_ := set $vtFixtures "configmaps" $vtCms }}
    {{- $_ := set $vtGlobal "fixtures" $vtFixtures }}
    {{- $_ := set $vt "_global" $vtGlobal }}
    {{- $tests = $vt }}
  {{- end }}

{{- if $tests }}
apiVersion: haproxy-haptic.org/v1alpha1
kind: HAProxyValidationTests
metadata:
  name: {{ $controller.configName }}-tests
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "haptic.labels.withCommon" . | nindent 4 }}
    app.kubernetes.io/component: validation-tests
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  validationTests:
    {{- toYaml $tests | nindent 4 }}
{{- end }}
