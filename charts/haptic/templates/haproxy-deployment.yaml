{{- $haproxy := .Values.haproxy }}
{{- if $haproxy.enabled }}
{{- $haproxyFullname := include "haptic.haproxyFullname" . }}
{{- $credentialsSecretName := include "haptic.dataplane.credentialsSecretName" . }}
{{- $haproxyImage := include "haptic.haproxy.image" . }}
{{- $haproxyBin := include "haptic.haproxy.bin" . }}
{{- $haproxyUid := include "haptic.haproxy.uid" . }}
{{- $spoaHubEnabled := include "haptic.spoaHub.enabled" . }}
{{- $dataplaneConfig := .Values.controller.config.dataplane }}
{{- $podSpec := $haproxy.podSpec }}
{{- $ports := $haproxy.ports }}
{{- $dataplane := $haproxy.dataplane }}
{{- $selectorLabels := include "haptic.selectorLabels" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $haproxyFullname }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "haptic.labels.withCommon" . | nindent 4 }}
    app.kubernetes.io/component: loadbalancer
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if not $haproxy.keda.enabled }}
  replicas: {{ $haproxy.replicaCount }}
  {{- end }}
  {{- with $haproxy.updateStrategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  minReadySeconds: {{ $haproxy.minReadySeconds }}
  revisionHistoryLimit: {{ $haproxy.revisionHistoryLimit }}
  selector:
    matchLabels:
      {{- $selectorLabels | nindent 6 }}
      app.kubernetes.io/component: loadbalancer
  template:
    metadata:
      annotations:
        checksum/secret: {{ include "haptic.dataplane.credentialsChecksum" . }}
        checksum/config: {{ include "haptic.haproxy.bootstrapConfigChecksum" . }}
        {{- with $podSpec.podAnnotations }}
        {{- tpl (toYaml .) $ | nindent 8 }}
        {{- end }}
      labels:
        {{- $selectorLabels | nindent 8 }}
        app.kubernetes.io/component: loadbalancer
    spec:
      {{- include "haptic.podSpec" $podSpec | nindent 6 }}
      {{- if $podSpec.shareProcessNamespace }}
      shareProcessNamespace: true
      {{- end }}
      {{- if or $haproxy.initContainers $spoaHubEnabled }}
      initContainers:
        {{- if $spoaHubEnabled }}
        # Bootstrap copy for the spoa-hub config. Kubernetes guarantees
        # init containers run to completion before regular containers
        # start, so by the time the haproxy + spoa-hub containers come
        # up, /etc/haproxy/general/spoa-hub-config.toml exists. Without
        # this ordering the spoa-hub sidecar can race the haproxy
        # container's init shell and crash on a missing config file —
        # CrashLoopBackOff resolves on retry once the cp lands, but
        # produces noisy startup events. HAPTIC's controller will
        # later overwrite this file via the dataplane API; the hub's
        # file-watch detects the change and gracefully reloads.
        - name: spoa-hub-bootstrap-copy
          image: {{ $haproxyImage }}
          imagePullPolicy: {{ $haproxy.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            runAsNonRoot: true
            runAsUser: {{ $haproxyUid }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              cp /etc/spoa-hub-bootstrap/config.toml /etc/haproxy/general/spoa-hub-config.toml
          volumeMounts:
            # general-storage (not haproxy-runtime) so the bootstrap
            # placeholder lands in the same volume the spoa-hub
            # sidecar mounts and the dataplane API later overwrites.
            # See volumes section below for why general/ is split off.
            - name: general-storage
              mountPath: /etc/haproxy/general
            - name: spoa-hub-bootstrap
              mountPath: /etc/spoa-hub-bootstrap
              readOnly: true
        {{- end }}
        {{- with $haproxy.initContainers }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: {{ $haproxyUid }}
        runAsGroup: {{ $haproxyUid }}
        fsGroup: {{ $haproxyUid }}
        {{- with $podSpec.podSecurityContext.seccompProfile }}
        seccompProfile:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with $podSpec.podSecurityContext.sysctls }}
        sysctls:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      containers:
        # HAProxy container
        - name: haproxy
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
              {{- if $haproxy.enterprise.enabled }}
              # Enterprise binaries have file capabilities requiring NET_BIND_SERVICE
              # PSS Restricted profile allows this capability
              add: [NET_BIND_SERVICE]
              {{- end }}
            runAsNonRoot: true
            runAsUser: {{ $haproxyUid }}
          image: {{ $haproxyImage }}
          imagePullPolicy: {{ $haproxy.image.pullPolicy }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              # Create required directories
              # Note: fsGroup handles ownership - no chown needed (and CAP_CHOWN is dropped)
              mkdir -p /etc/haproxy/maps /etc/haproxy/certs /etc/haproxy/errors /etc/haproxy/general

              # Copy initial config
              cp /config/haproxy.cfg /etc/haproxy/haproxy.cfg

              # Start HAProxy in master-worker mode with master socket
              # -dr: Skip DNS resolution failures - servers start in DOWN state instead of failing startup
              # -m: Memory limit in MB (derived from container memory requests, when set)
              {{- $memReq := dig "requests" "memory" "" $haproxy.resources }}
              exec {{ $haproxyBin }} -dr -W -db{{ if $memReq }} -m {{ include "haptic.memoryToMB" $memReq }}{{ end }} -S "/etc/haproxy/haproxy-master.sock,level,admin" -- /etc/haproxy/haproxy.cfg
          {{- with $haproxy.extraEnv }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            {{- range $name := list "http" "https" "stats" }}
            - name: {{ $name }}
              containerPort: {{ index $ports $name }}
              protocol: TCP
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /config
            - name: haproxy-runtime
              mountPath: /etc/haproxy
            # Overlay the dataplane API's general-storage dir on top of
            # haproxy-runtime/general/ with a SEPARATE emptyDir. The
            # spoa-hub sidecar (when enabled) mounts only this narrower
            # volume so it can't read SSL private keys or other
            # haproxy-runtime contents — see volumes section comment.
            - name: general-storage
              mountPath: /etc/haproxy/general
            {{- if $haproxy.shmStats.enabled }}
            - name: devshm
              mountPath: /dev/shm
            {{- end }}
            {{- if $spoaHubEnabled }}
            # Shared with the spoa-hub sidecar; HAProxy connects via a
            # unix socket the hub creates here at startup.
            - name: spoa-socket
              mountPath: /run/spoa
            {{- end }}
            {{- with $haproxy.extraVolumeMounts }}
            {{- tpl (toYaml .) $ | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: stats
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: stats
            initialDelaySeconds: 5
            periodSeconds: 5
          {{- with $haproxy.lifecycle }}
          lifecycle:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml $haproxy.resources | nindent 12 }}

        # Dataplane API sidecar
        - name: dataplane
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
              {{- if $haproxy.enterprise.enabled }}
              # Enterprise: dataplaneapi calls HAProxy binary which has file capabilities
              # PSS Restricted profile allows this capability
              add: [NET_BIND_SERVICE]
              {{- end }}
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: {{ $haproxyUid }}
          image: {{ $haproxyImage }}
          imagePullPolicy: {{ $haproxy.image.pullPolicy }}
          env:
            {{- range $env := list (dict "name" "DATAPLANE_USERNAME" "key" "dataplane_username") (dict "name" "DATAPLANE_PASSWORD" "key" "dataplane_password") }}
            - name: {{ $env.name }}
              valueFrom:
                secretKeyRef:
                  name: {{ $credentialsSecretName }}
                  key: {{ $env.key }}
            {{- end }}
            {{- include "haptic.dataplane.autoGomaxprocs" $dataplane | nindent 12 }}
            {{- with $dataplane.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              # Wait for HAProxy master socket file to exist
              echo "Waiting for HAProxy master socket file..."
              while [ ! -S /etc/haproxy/haproxy-master.sock ]; do
                sleep 0.5
              done

              # Wait for HAProxy to accept connections on the socket
              echo "Waiting for HAProxy master socket to accept connections..."
              MAX_RETRIES=30
              RETRY_COUNT=0
              while ! echo "show info" | socat -T2 stdio unix-connect:/etc/haproxy/haproxy-master.sock >/dev/null 2>&1; do
                RETRY_COUNT=$((RETRY_COUNT + 1))
                if [ $RETRY_COUNT -ge $MAX_RETRIES ]; then
                  echo "Warning: HAProxy master socket not responding after ${MAX_RETRIES} retries, proceeding anyway"
                  break
                fi
                sleep 0.5
              done
              echo "HAProxy master socket is ready"

              # Create required directories
              # Note: Directories owned by user 99 via pod fsGroup, no chown needed
              mkdir -p /var/lib/dataplaneapi/transactions /var/lib/dataplaneapi/backups
              mkdir -p /etc/haproxy/maps /etc/haproxy/certs /etc/haproxy/general

              # Create Dataplane API config using credentials from Secret
              cat > /etc/haproxy/dataplaneapi.yaml <<EOF
              config_version: 2
              name: haproxy-dataplaneapi
              dataplaneapi:
                host: 0.0.0.0
                port: {{ $ports.dataplane }}
                disable_inotify: true
                {{- with $dataplane.debugSocketPath }}
                debug_socket_path: {{ . }}
                {{- end }}
                user:
                  - name: ${DATAPLANE_USERNAME}
                    password: ${DATAPLANE_PASSWORD}
                    insecure: true
                userlist:
                  userlist: ""
                  userlist_file: ""
                transaction:
                  transaction_dir: /var/lib/dataplaneapi/transactions
                  backups_number: 10
                  backups_dir: /var/lib/dataplaneapi/backups
                resources:
                  maps_dir: {{ $dataplaneConfig.mapsDir }}
                  ssl_certs_dir: {{ $dataplaneConfig.sslCertsDir }}
                  general_storage_dir: {{ $dataplaneConfig.generalStorageDir }}
              haproxy:
                config_file: {{ $dataplaneConfig.configFile }}
                haproxy_bin: {{ $haproxyBin }}
                master_worker_mode: true
                master_runtime: /etc/haproxy/haproxy-master.sock
                reload:
                  reload_delay: 1
                  reload_cmd: /bin/sh -c "echo 'reload' | socat stdio unix-connect:/etc/haproxy/haproxy-master.sock"
                  restart_cmd: /bin/sh -c "echo 'reload' | socat stdio unix-connect:/etc/haproxy/haproxy-master.sock"
                  reload_strategy: custom
                  # The dataplane API reads validate_cmd from haproxy.reload, NOT
                  # from haproxy directly. Placing it at the top-level haproxy
                  # block parses into an unused struct field with omitempty and
                  # silently drops on re-serialize — making
                  # haproxy.dataplane.validateConfig=false a no-op and leaving
                  # the default haproxy -c running on every endpoint push.
                  {{- if $dataplane.validateConfig }}
                  # -dr skips DNS resolution failures during validation so the
                  # config isn't rejected when DNS is temporarily unavailable.
                  validate_cmd: {{ $haproxyBin }} -dr -c -f %s
                  {{- else }}
                  validate_cmd: /bin/true
                  {{- end }}
              log_targets:
                - log_to: stdout
                  log_level: trace
                  log_format: text
                  {{- with $dataplane.aclFormat }}
                  acl_format: {{ . | quote }}
                  {{- end }}
                  log_types:
                  - access
                  - app
              EOF

              # Start Dataplane API
              exec {{ include "haptic.haproxy.dataplanebin" . }} -f /etc/haproxy/dataplaneapi.yaml
          ports:
            - name: dataplane
              containerPort: {{ $ports.dataplane }}
              protocol: TCP
          volumeMounts:
            - name: haproxy-runtime
              mountPath: /etc/haproxy
            # general-storage overlay — same shape as the haproxy
            # container's mount. The dataplane API's
            # general_storage_dir resolves through this overlay so
            # writes to /etc/haproxy/general/<file> land in the
            # narrow general-storage volume that the spoa-hub
            # sidecar can also read.
            - name: general-storage
              mountPath: /etc/haproxy/general
            - name: dataplane-data
              mountPath: /var/lib/dataplaneapi
            - name: dataplane-tmp
              mountPath: /tmp
          livenessProbe:
            tcpSocket:
              port: dataplane
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: dataplane
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            {{- toYaml $dataplane.resources | nindent 12 }}

        {{- if $spoaHubEnabled }}
        {{- $spoaHub := .Values.spoaHub }}
        # SPOA hub sidecar — bundles the haproxy-spoa-hub binary plus the
        # plugin .so files. Conditional on at least one plugin being
        # enabled. Communicates with HAProxy over /run/spoa/hub.sock.
        - name: spoa-hub
          image: {{ include "haptic.spoaHub.image" . }}
          imagePullPolicy: {{ $spoaHub.image.pullPolicy }}
          args:
            # Hub config lives in HAProxy's general-storage dir so the
            # dataplane API can write to it. Mounted into this container
            # at /etc/haproxy/ (the haproxy-runtime emptyDir, no
            # subPath — see the volumeMounts comment below for why
            # subPath is incompatible with the hub's file-watch).
            # The hub binary's file-watch (introduced in
            # haproxy-spoa-hub v0.5.x via the file-watch-and-reload-
            # metrics MR) attaches to the parent of this --config
            # path, so HAPTIC overwriting the file via the dataplane
            # API triggers a graceful reload without needing a SIGHUP.
            # See docs/adr/0007 for the full architecture.
            - --config
            - /etc/haproxy/general/spoa-hub-config.toml
          securityContext:
            {{- toYaml $spoaHub.securityContext | nindent 12 }}
          resources:
            {{- toYaml $spoaHub.resources | nindent 12 }}
          volumeMounts:
            # ONLY the narrow general-storage volume — NOT
            # haproxy-runtime. The hub needs to see the rendered
            # spoa-hub-config.toml that the dataplane API writes
            # (and the bootstrap initContainer seeded), but it has
            # no business reading SSL private keys or backend maps
            # from haproxy-runtime/{ssl,maps,errors,…}. By splitting
            # general/ into its own emptyDir and mounting only that
            # here, the spoa-hub container's blast radius is the
            # config file alone.
            #
            # We deliberately do NOT use `subPath: general` on a
            # haproxy-runtime mount — kubelet implements subPath as
            # an isolated bind-mount that does NOT propagate inotify
            # events from the source side. The hub binary relies on
            # file-watch on the parent of `--config` to survive
            # atomic-rename writes; through a subPath mount that
            # watch never fires and the hub stays on the bootstrap
            # config forever, silently breaking per-Ingress
            # modsecurity-snippet enforcement. A separate-volume
            # overlay (mounted on haproxy + dataplane at the same
            # path) gives both narrow scope AND working inotify.
            - name: general-storage
              mountPath: /etc/haproxy/general
              readOnly: true
            - name: spoa-socket
              mountPath: /run/spoa
            # The spoa-hub container runs with readOnlyRootFilesystem:true.
            # Some plugins (e.g. coraza's filesystem-access verifier) need a
            # writable scratch directory at startup; the hub also keeps a
            # short-lived audit-log spool there for some plugin
            # configurations. A small emptyDir mounted at /tmp gives them a
            # place to write without weakening the container's read-only
            # root filesystem. Memory-backed because the contents are
            # ephemeral and tiny — no need for disk-backed storage.
            - name: spoa-hub-tmp
              mountPath: /tmp
            {{- with $spoaHub.extraVolumeMounts }}
            {{- tpl (toYaml .) $ | nindent 12 }}
            {{- end }}
        {{- end }}

        {{- with $haproxy.sidecars }}
        {{- tpl (toYaml .) $ | nindent 8 }}
        {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ $haproxyFullname }}-config
        - name: haproxy-runtime
          # tmpfs-backed emptyDir. Disk-backed emptyDir lands on the
          # node's container-runtime filesystem (overlayfs on most
          # clusters), where inotify events occasionally fail to
          # propagate to bind-mount consumers in other containers.
          # tmpfs sidesteps that — emptyDir contents never survive a
          # pod restart anyway, so tmpfs vs disk has no semantic
          # impact. 64 MiB ceiling — rendered maps + SSL files + …
          # are well under that for any realistic ingress workload.
          emptyDir:
            medium: Memory
            sizeLimit: 64Mi
        # Narrow overlay volume scoped to the dataplane API's
        # general-storage dir. Mounted at /etc/haproxy/general/ on
        # the haproxy + dataplane containers (overlaying the same
        # subdirectory of haproxy-runtime), AND on the spoa-hub
        # sidecar (its only volume from the haproxy-runtime tree).
        # The split exists so the hub container can read its
        # config file (and pick up file-watch reloads) without
        # also gaining read access to ssl/ private keys, maps/,
        # or other haproxy-runtime contents — least-privilege via
        # the kubelet mount layout rather than via subPath, which
        # would block inotify event propagation.
        - name: general-storage
          emptyDir:
            medium: Memory
            sizeLimit: 16Mi
        {{- if $haproxy.shmStats.enabled }}
        - name: devshm
          emptyDir:
            medium: Memory
            sizeLimit: {{ include "haptic.haproxy.shmSizeLimit" . }}
        {{- end }}
        - name: dataplane-data
          emptyDir: {}
        - name: dataplane-tmp
          emptyDir: {}
        {{- if $spoaHubEnabled }}
        # Memory-backed scratch directory for the spoa-hub container's
        # /tmp. Some plugins (notably coraza) write probe files here to
        # verify filesystem access at plugin init; without this the
        # container's readOnlyRootFilesystem makes the writes EROFS
        # and the plugin fails to load. tmpfs keeps the I/O off-disk.
        - name: spoa-hub-tmp
          emptyDir:
            medium: Memory
            sizeLimit: 16Mi
        # Helm-rendered bootstrap config for the spoa-hub. Mounted
        # read-only into the haproxy container, which copies it into
        # /etc/haproxy/general/spoa-hub-config.toml during init so the
        # spoa-hub sidecar finds a valid config at its --config path
        # on first start. HAPTIC's controller overwrites the
        # destination via the dataplane API on first reconcile.
        - name: spoa-hub-bootstrap
          configMap:
            name: {{ include "haptic.spoaHub.configMapName" . }}
        - name: spoa-socket
          emptyDir: {}
        {{- end }}
        {{- with $haproxy.extraVolumes }}
        {{- tpl (toYaml .) $ | nindent 8 }}
        {{- end }}
{{- end }}
