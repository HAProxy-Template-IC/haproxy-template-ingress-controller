# Changelog

The Helm chart is released together with the controller and shares its version.
All changes — including chart changes (values, templates, chart defaults) — are
documented in the root [CHANGELOG.md](../../CHANGELOG.md); chart changes appear
under each release's "Helm chart" subsection. The chart release history from
before the unified release (0.1.0 and earlier) has been merged there as well.
