apiVersion: v2
name: gateway
description: HAPTIC template library (gateway) — split-library fragments merged into the HAProxyTemplateConfig by the parent. Carries no templates of its own.
version: 0.1.0
