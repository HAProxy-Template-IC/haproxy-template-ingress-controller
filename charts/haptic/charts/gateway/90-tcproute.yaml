# Gateway API TCPRoute (L4 TCP routing)
#
# A Gateway listener with `protocol: TCP` forwards raw TCP on its port to
# the backend Service(s) referenced by a TCPRoute attached to that
# listener. Unlike TLSRoute, there is NO SNI / TLS inspection — the
# connection is a straight `default_backend` forward in `mode tcp`.
#
# Shape (one TCP listener -> one tcp frontend -> its TCPRoute's backend):
#
#   frontend gateway-tcp-port-<port>
#       mode tcp
#       bind *:<port>
#       default_backend gtw_tcp_<routeNs>_<routeName>_<ruleIdx>
#
#   backend gtw_tcp_<routeNs>_<routeName>_<ruleIdx>
#       mode tcp
#       server SRV_1 <pod-ip>:<pod-port> ...
#
# Layout mirrors the TLSRoute passthrough path (22-ssl-passthrough.yaml +
# backends-501-gateway-ssl-passthrough + frontends-600-gateway-tls-
# listener) but without the SNI allowlist / use_backend dispatch.
#
# Attachment (which TCPRoute attaches to which TCP listener) reuses the
# same listener-attachment filters as the passthrough path: protocol
# (TCP), allowedRoutes.kinds (default TCPRoute), allowedRoutes.namespaces
# (Same / All / Selector), parentRef sectionName + port. TCP has no
# hostname, so there is no hostname-intersection step.
#
# Single- and weighted multi-backendRef routing are implemented: one
# routable backendRef forwards the whole port; several become a `balance
# roundrobin` mode-tcp backend, each Service in a disjoint slot range
# (SRV<i>_) carrying its weight (weight 0 → no traffic). When several
# TCPRoutes claim the same port, the oldest (creationTimestamp) wins.
templateSnippets:
  # util-gateway-tcproute-analysis
  #
  # Caches the per-port TCP routing plan in
  # shared["gateway_tcproute"].byPort. Race-free via ComputeIfAbsent so
  # the parallel backends-/frontends- renderers see a consistent view.
  #
  # Schema (shared["gateway_tcproute"]):
  #   {
  #     "byPort": map[int]any{                 # keyed by listener port
  #        <port>: map[string]any{
  #          "backendName": "gtw_tcp_<ns>_<name>_<ruleIdx>",
  #          "routeNs":     <route namespace>,
  #          "routeName":   <route name>,
  #          "ruleIdx":     <int>,
  #          "refName":     <first Service name>,
  #          "refPort":     <first Service port int>,
  #          "refNs":       <first Service namespace>,
  #          "refs":        [ {name, port, ns, weight}, … ],  # all valid refs
  #        }
  #     }
  #   }
  #
  # A port is claimed by the FIRST attached (TCPRoute, rule, valid
  # backendRef) tuple encountered (TCP can't multiplex a single port by
  # any L7 selector). Routes are walked in resources.tcproutes.List()
  # order, which the store keeps stable.
  util-gateway-tcproute-analysis:
    requires: [gateways, tcproutes]
    template: |
      {%- import "util-resource-helpers" for ListenerName, ListenerProtocol %}
      {%%
        var _, _ = shared.ComputeIfAbsent("gateway_tcproute", func() any {
          var byPort = map[int]any{}

          // Gateway API conflict resolution: when several TCPRoutes claim
          // the same listener port, the OLDEST (earliest creationTimestamp,
          // then namespace/name) is bound; the port can't be multiplexed by
          // any L7 key. Rather than pre-sort the routes (a hand-rolled
          // bubble-sort over the typed slice mis-lowered under Scriggo and
          // silently dropped later routes when an orphaned route sorted
          // first), we walk List() in store order and resolve the winner AT
          // CLAIM TIME: a contested port keeps whichever claimant is older
          // (ts asc, empty ts last, then namespace/name asc). Each byPort
          // entry carries "ts"/"key" so a later claimant can compare.

          for _, route := range resources.tcproutes.List() {
            var nsStr = route.Metadata.Namespace
            var nameStr = route.Metadata.Name
            var routeTs = route.Metadata.CreationTimestamp
            var routeKey = nsStr + "/" + nameStr

            for ruleIdx, rule := range route.Spec.Rules {
              // First valid backendRef of the rule wins. Per Gateway API
              // spec only core/v1 Service refs are routable; anything
              // else is skipped (an all-invalid rule simply contributes
              // no backend — the listener port stays unclaimed and its
              // frontend rejects, mirroring the TLSRoute invalid-ref
              // contract).
              // Collect EVERY valid backendRef with its weight. A single
              // routable ref forwards the whole port; several weighted refs
              // become a `balance roundrobin` mode-tcp backend (weighted
              // TCPRoute). Weight defaults to 1 (Gateway API); weight 0 stays
              // in the backend but is emitted with `weight 0` so it takes no
              // traffic. refName/refPort/refNs keep the FIRST ref for the
              // existing byPort fields.
              var refName = ""
              var refPort = 0
              var refNs = ""
              var refs = []any{}
              for _, br := range rule.BackendRefs {
                var brGroup = br.Group
                var brKind = br.Kind
                if brKind == "" { brKind = "Service" }
                if (brGroup != "" && brGroup != "core") || brKind != "Service" {
                  continue
                }
                var brNs = br.Namespace
                if brNs == "" { brNs = nsStr }
                if resources.services.GetSingle(brNs, br.Name) == nil {
                  continue
                }
                var brPort = toint(br.Port)
                if brPort == 0 { brPort = 80 }
                var brWeight = 1
                if br.Weight != nil { brWeight = toint(br.Weight) }
                refs = append(refs, map[string]any{
                  "name": br.Name, "port": brPort, "ns": brNs, "weight": brWeight,
                })
                if refName == "" {
                  refName = br.Name
                  refPort = brPort
                  refNs = brNs
                }
              }
              if refName == "" {
                continue
              }

              // Walk parentRefs and find every accepted TCP listener.
              for _, parentRef := range route.Spec.ParentRefs {
                var prGroup = parentRef.Group
                if prGroup == "" { prGroup = "gateway.networking.k8s.io" }
                var prKind = parentRef.Kind
                if prKind == "" { prKind = "Gateway" }
                if prGroup != "gateway.networking.k8s.io" || prKind != "Gateway" {
                  continue
                }
                var prNs = parentRef.Namespace
                if prNs == "" { prNs = nsStr }
                var prName = parentRef.Name
                var prSection = parentRef.SectionName
                var prPort = toint(parentRef.Port)

                var gw = resources.gateways.GetSingle(prNs, prName)
                // NOTE: a nested-if guard (NOT `if gw == nil { continue }`).
                // Scriggo mis-lowers a `continue` inside this deep nested-if/
                // for structure so it terminates the OUTER route loop, not
                // just this parentRef iteration (same footgun documented in
                // 20-route-analysis.yaml's conflict detection). An orphaned
                // TCPRoute whose parentRef Gateway was deleted hits the nil
                // branch; a `continue` there dropped EVERY later TCPRoute
                // from routing (TCPRouteWeightedRouting / ParentRefPortAnd-
                // SectionName / ReferenceGrant all fail once a stale route
                // co-resides). Guard with `if gw != nil { … }` instead.
                if gw != nil {
                for _, listener := range gw.Spec.Listeners {
                  var lName = ListenerName(listener)
                  var lProto = ListenerProtocol(listener)
                  var lPort = toint(listener.Port)

                  if lProto != "TCP" || lPort <= 0 {
                    continue
                  }
                  if prSection != "" && prSection != lName {
                    continue
                  }
                  if prPort != 0 && prPort != lPort {
                    continue
                  }

                  // allowedRoutes.kinds intersection: when omitted, the
                  // protocol-default kind applies (TCP -> TCPRoute).
                  var kindOk = true
                  if len(listener.AllowedRoutes.Kinds) > 0 {
                    kindOk = false
                    for _, ak := range listener.AllowedRoutes.Kinds {
                      var akG = ak.Group
                      if akG == "" { akG = "gateway.networking.k8s.io" }
                      if akG == "gateway.networking.k8s.io" && ak.Kind == "TCPRoute" {
                        kindOk = true
                        break
                      }
                    }
                  }
                  if !kindOk {
                    continue
                  }

                  // allowedRoutes.namespaces gating.
                  var nsOk = false
                  var fromMode = listener.AllowedRoutes.Namespaces.From
                  if fromMode == "" { fromMode = "Same" }
                  switch fromMode {
                  case "Same":
                    nsOk = (nsStr == prNs)
                  case "All":
                    nsOk = true
                  case "Selector":
                    var selectorMap = listener.AllowedRoutes.Namespaces.Selector.MatchLabels
                    if selectorMap != nil {
                      var nsObj = resources.namespaces.GetSingle(nsStr)
                      if nsObj != nil {
                        var allMatch = true
                        for k, v := range selectorMap {
                          var actual, ok = nsObj.Metadata.Labels[k]
                          if !ok || actual != v { allMatch = false; break }
                        }
                        nsOk = allMatch
                      }
                    }
                  }
                  if !nsOk {
                    continue
                  }

                  // Oldest-wins at claim time: TCP forwards a whole port to
                  // one backend, so a contested port keeps the older route
                  // (ts asc, empty-ts last, then namespace/name asc). No
                  // `continue` here — a skipped claim must not disturb the
                  // outer route walk.
                  var winThisPort = true
                  var existing, taken = byPort[lPort]
                  if taken {
                    var em = existing.(map[string]any)
                    var eTs = tostring(em["ts"])
                    var eKey = tostring(em["key"])
                    if eTs == routeTs {
                      winThisPort = routeKey < eKey
                    } else if routeTs == "" {
                      winThisPort = false
                    } else if eTs != "" && routeTs > eTs {
                      winThisPort = false
                    }
                  }
                  if winThisPort {
                    byPort[lPort] = map[string]any{
                      "backendName": "gtw_tcp_" + nsStr + "_" + nameStr + "_" + sprintf("%d", ruleIdx),
                      "routeNs":     nsStr,
                      "routeName":   nameStr,
                      "ruleIdx":     ruleIdx,
                      "refName":     refName,
                      "refPort":     refPort,
                      "refNs":       refNs,
                      "refs":        refs,
                      "ts":          routeTs,
                      "key":         routeKey,
                    }
                  }
                }
                }
              }
            }
          }

          return map[string]any{"byPort": byPort}
        })
      %%}

  # backends-502-gateway-tcproute
  #
  # Emits one `mode tcp` backend per claimed TCP listener port. Hooks
  # into base.yaml's `render_glob "backends-*"` extension point. The
  # `502` ordinal keeps it after the ssl-passthrough TCP backends (501)
  # for stable output ordering.
  backends-502-gateway-tcproute:
    requires: [gateways, tcproutes]
    template: |
      {%- import "util-backend-servers" for BackendServers %}
      {{ render "util-gateway-tcproute-analysis" }}
      {%- var dataRaw = shared.Get("gateway_tcproute") %}
      {%- if dataRaw != nil %}
        {%- var byPort = dataRaw.(map[string]any)["byPort"].(map[int]any) %}
        {#- Deterministic output: sort the claimed ports numerically. -#}
        {%- var portKeysRaw = []any{} %}
        {%- for k, _ := range byPort %}
          {%- portKeysRaw = append(portKeysRaw, k) %}
        {%- end %}
        {%- var portKeys = sort_ints(portKeysRaw) %}
        {#- A TCPRoute attaching to multiple listener ports (e.g. one that
            attaches to every listener on a Gateway) claims several ports
            that all resolve to the SAME (route, rule) backend name
            `gtw_tcp_<ns>_<name>_<ruleIdx>` — the name is keyed by route+rule,
            not port. Emitting the backend once per port would declare the
            same `backend` name twice, which HAProxy 3.3+ rejects as a fatal
            parse error (crash-looping config load). Emit each unique backend
            exactly once; the per-port frontends (frontends-700) all forward
            to the shared backend, which is correct — the same Service:port
            is simply reachable on multiple listener ports. -#}
        {%- var seenBackends = map[string]bool{} %}
        {%- for _, port := range portKeys %}
          {%- var info = byPort[port].(map[string]any) %}
          {%- var beName = tostring(info["backendName"]) %}
          {%- if !seenBackends[beName] %}
            {%- seenBackends[beName] = true %}
          {%- var refName = tostring(info["refName"]) %}
          {%- var refPort = toint(info["refPort"]) %}
          {%- var refNs = tostring(info["refNs"]) %}
          {%- var refs = info["refs"].([]any) %}
      # gateway/backends-502-gateway-tcproute: {{ info["routeNs"] }}/{{ info["routeName"] }} rule {{ info["ruleIdx"] }} (port {{ port }})

      backend {{ beName }}
          guid {{ make_guid("be", beName) }}
          mode tcp
          {%- if len(refs) <= 1 %}
          {{ BackendServers(refName, 10, refPort, nil, nil, beName, refNs) | indent(4, false) }}
          {%- else %}
          {#- Weighted TCPRoute: several routable backendRefs. `balance
              roundrobin` honours per-server weights; each Service gets its
              own slotPrefix so the reload-free slot preservation tracks each
              independently within the one backend. A weight-0 ref stays in
              the config (emitted `weight 0`) so it takes no traffic. -#}
          balance roundrobin
          {%- for ri, r := range refs %}
          {%- var rm = r.(map[string]any) %}
          {{ BackendServers(tostring(rm["name"]), 10, toint(rm["port"]), map[string]any{"slotPrefix": "SRV" + tostring(ri) + "_", "weight": toint(rm["weight"])}, nil, beName, tostring(rm["ns"])) | indent(4, false) }}
          {%- end %}
          {%- end %}
          {%- end %}
        {%- end %}
      {%- end %}

  # frontends-700-gateway-tcp-listener
  #
  # Emits one `mode tcp` frontend per claimed TCP listener port, bound to
  # that port and forwarding straight to the route's backend. Hooks into
  # base.yaml's `render_glob "frontends-*"` extension point (same point
  # frontends-600-gateway-tls-listener uses).
  #
  # Port-collision guard: a TCP listener declared on a port the chart-
  # static HTTP / HTTPS frontends already bind (httpPort / httpsPort)
  # would produce a duplicate `bind *:<port>` and a config parse error.
  # We drop such listeners silently here (the spec surfaces the conflict
  # via the Gateway listener status; the data plane just stays
  # parseable). Ports claimed by the per-port TLS frontends
  # (frontends-600) cannot collide because a port is single-protocol in
  # a Gateway, so a TCP listener and a TLS listener never share a port.
  frontends-700-gateway-tcp-listener:
    requires: [gateways, tcproutes]
    template: |
      {{ render "util-gateway-tcproute-analysis" }}
      {%- var dataRaw = shared.Get("gateway_tcproute") %}
      {%- if dataRaw != nil %}
        {%- var byPort = dataRaw.(map[string]any)["byPort"].(map[int]any) %}
        {%- var httpPort = toint(extraContext | dig("httpPort") | fallback(8080)) %}
        {%- var httpsPort = toint(extraContext | dig("httpsPort") | fallback(8443)) %}
        {%- var portKeysRaw = []any{} %}
        {%- for k, _ := range byPort %}
          {%- portKeysRaw = append(portKeysRaw, k) %}
        {%- end %}
        {%- var portKeys = sort_ints(portKeysRaw) %}
        {%- for _, port := range portKeys %}
          {%- if port != httpPort && port != httpsPort %}
            {%- var info = byPort[port].(map[string]any) %}
            {%- var beName = tostring(info["backendName"]) %}

      # gateway/frontends-700-gateway-tcp-listener: port {{ port }}

      frontend gateway-tcp-port-{{ port }}
          guid fe:gateway-tcp-port-{{ port }}
          mode tcp
          bind *:{{ port }}
          default_backend {{ beName }}
          {%- end %}
        {%- end %}
      {%- end %}

  # util-gateway-tcproute-count-contrib
  #
  # TCPRoute contribution to the Gateway listener attachedRoutes count.
  #
  # Compile-safe seam: util-generate-gateway-status-patches (a snippet that
  # SURVIVES when the TCPRoute feature is stripped) renders this snippet
  # with `default ""` and reads the entries back from the shared key below.
  # The typed resources.tcproutes access must live HERE — in a snippet
  # stripped together with the tcproutes watch — because on a cluster where
  # the TCPRoute CRD is not served the resources struct has no tcproutes
  # field and a direct reference fails to COMPILE (crash-looping the
  # controller), regardless of any runtime `if`.
  #
  # needsTCPProtocol tags the entries so the listener loop counts them only
  # toward `protocol: TCP` listeners.
  util-gateway-tcproute-count-contrib:
    requires: [tcproutes]
    template: |
      {%%
        var _, _ = shared.ComputeIfAbsent("tcprouteCountEntries", func() any {
          var entries = []any{}
          for _, r := range resources.tcproutes.List() {
            entries = append(entries, map[string]any{
              "kind":  "TCPRoute",
              "route": r,
              "needsTLSProtocol": false,
              "needsTCPProtocol": true,
            })
          }
          return entries
        })
      %%}

  # status-patches-210-gateway-tcproute
  #
  # TCPRoute status patch registration. Hooks into base.yaml's
  # `render_glob "status-patches-*"` extension point (same point
  # status-patches-200-gateway uses).
  #
  # Lives apart from status-patches-200-gateway ONLY because the import of
  # the typed TCPRoute macros must not appear in a snippet that survives
  # when the TCPRoute feature is stripped — `import` has no `default`
  # clause, so the whole registration sits in this strippable snippet.
  # render_glob tolerates its absence.
  status-patches-210-gateway-tcproute:
    requires: [gateways, referencegrants, tcproutes]
    template: |
      {%- import "util-sharded-tcproute-status-patches" for ShardedTCPRouteStatusPatches -%}
      {%- var allTCPRoutes = resources.tcproutes.List() -%}
      {%- if len(allTCPRoutes) > 0 -%}
        {{- ShardedTCPRouteStatusPatches(allTCPRoutes) -}}
      {%- end -%}

validationTests:
  test-tcproute-basic-l4-forward:
    requires: [gateways, tcproutes]
    description: "Gateway API TCPRoute (L4): a Gateway listener with protocol TCP on port 9100 has a TCPRoute attached whose single backendRef points at a Service. The chart must emit a mode-tcp frontend bound to 9100 with default_backend to a mode-tcp backend carrying the Service's endpoint as a server line."
    extraContext:
      controllerName: "haproxy-haptic.org/controller"
    fixtures:
      gateways:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: Gateway
          metadata:
            name: gw-tcp
            namespace: default
            generation: 1
          spec:
            gatewayClassName: haptic
            listeners:
              - name: tcp
                protocol: TCP
                port: 9100
                allowedRoutes:
                  namespaces:
                    from: Same
                  kinds:
                    - kind: TCPRoute
      tcproutes:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: TCPRoute
          metadata:
            name: tcp-app
            namespace: default
            generation: 1
          spec:
            parentRefs:
              - name: gw-tcp
            rules:
              - backendRefs:
                  - name: tcp-backend
                    port: 9100
      services:
        - apiVersion: v1
          kind: Service
          metadata:
            name: tcp-backend
            namespace: default
          spec:
            ports:
              - port: 9100
                targetPort: 9100
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: tcp-backend-abc
            namespace: default
            labels:
              kubernetes.io/service-name: tcp-backend
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.42"]
              targetRef:
                kind: Pod
                name: tcp-backend-pod-1
          ports:
            - port: 9100
              protocol: TCP
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^frontend gateway-tcp-port-9100$'
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^\s+bind \*:9100$'
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^\s+default_backend gtw_tcp_default_tcp-app_0$'
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^backend gtw_tcp_default_tcp-app_0$'
      - type: contains
        target: haproxy.cfg
        pattern: '(?ms)^backend gtw_tcp_default_tcp-app_0$.*?^\s+mode tcp$'
      - type: contains
        target: haproxy.cfg
        pattern: 'server SRV_1 10\.0\.0\.42:9100'

  test-tcproute-oldest-route-wins-port:
    requires: [gateways, tcproutes]
    description: >
      When two TCPRoutes claim the same listener port, Gateway API conflict
      resolution binds the OLDEST (earliest creationTimestamp) route. The
      newer route is named alphabetically first (tcp-a-newer) so a naive
      List()-order first-claim would pick it; the sort must instead bind
      tcp-b-older. Mirrors the TCPRouteMultipleRoutesAttachment conformance
      test's traffic-binding assertion.
    extraContext:
      controllerName: "haproxy-haptic.org/controller"
    fixtures:
      gateways:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: Gateway
          metadata:
            name: gw-tcp
            namespace: default
            generation: 1
          spec:
            gatewayClassName: haptic
            listeners:
              - name: tcp
                protocol: TCP
                port: 9100
                allowedRoutes:
                  namespaces:
                    from: Same
                  kinds:
                    - kind: TCPRoute
      tcproutes:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: TCPRoute
          metadata:
            name: tcp-a-newer
            namespace: default
            generation: 1
            creationTimestamp: "2026-02-01T00:00:00Z"
          spec:
            parentRefs:
              - name: gw-tcp
            rules:
              - backendRefs:
                  - name: tcp-backend
                    port: 9100
        - apiVersion: gateway.networking.k8s.io/v1
          kind: TCPRoute
          metadata:
            name: tcp-b-older
            namespace: default
            generation: 1
            creationTimestamp: "2026-01-01T00:00:00Z"
          spec:
            parentRefs:
              - name: gw-tcp
            rules:
              - backendRefs:
                  - name: tcp-backend
                    port: 9100
      services:
        - apiVersion: v1
          kind: Service
          metadata:
            name: tcp-backend
            namespace: default
          spec:
            ports:
              - port: 9100
                targetPort: 9100
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: tcp-backend-abc
            namespace: default
            labels:
              kubernetes.io/service-name: tcp-backend
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.42"]
              targetRef:
                kind: Pod
                name: tcp-backend-pod-1
          ports:
            - port: 9100
              protocol: TCP
    assertions:
      - type: haproxy_valid
      # The older route is bound as the port's default_backend.
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^\s+default_backend gtw_tcp_default_tcp-b-older_0$'
      # The newer route is NOT bound to the port.
      - type: not_contains
        target: haproxy.cfg
        pattern: 'default_backend gtw_tcp_default_tcp-a-newer_0'

  test-tcproute-weighted-routing:
    requires: [gateways, tcproutes]
    description: >
      A TCPRoute rule with multiple weighted backendRefs becomes one
      mode-tcp backend with `balance roundrobin`, each Service's servers in
      a disjoint slot range (SRV0_/SRV1_/SRV2_) carrying its weight; a
      weight-0 backend is emitted with `weight 0` (no traffic). Mirrors the
      TCPRouteWeightedRouting conformance test.
    extraContext:
      controllerName: "haproxy-haptic.org/controller"
    fixtures:
      gateways:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: Gateway
          metadata:
            name: gw-tcp
            namespace: default
          spec:
            gatewayClassName: haptic
            listeners:
              - name: tcp
                protocol: TCP
                port: 9100
                allowedRoutes:
                  namespaces:
                    from: Same
                  kinds:
                    - kind: TCPRoute
      tcproutes:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: TCPRoute
          metadata:
            name: tcp-weighted
            namespace: default
          spec:
            parentRefs:
              - name: gw-tcp
            rules:
              - backendRefs:
                  - name: tcp-v1
                    port: 9100
                    weight: 70
                  - name: tcp-v2
                    port: 9100
                    weight: 30
                  - name: tcp-v3
                    port: 9100
                    weight: 0
      services:
        - apiVersion: v1
          kind: Service
          metadata: { name: tcp-v1, namespace: default }
          spec:
            ports: [ { port: 9100, targetPort: 9100 } ]
        - apiVersion: v1
          kind: Service
          metadata: { name: tcp-v2, namespace: default }
          spec:
            ports: [ { port: 9100, targetPort: 9100 } ]
        - apiVersion: v1
          kind: Service
          metadata: { name: tcp-v3, namespace: default }
          spec:
            ports: [ { port: 9100, targetPort: 9100 } ]
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: tcp-v1-a
            namespace: default
            labels: { kubernetes.io/service-name: tcp-v1 }
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.1"]
              targetRef: { kind: Pod, name: tcp-v1-pod }
          ports: [ { port: 9100, protocol: TCP } ]
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: tcp-v2-a
            namespace: default
            labels: { kubernetes.io/service-name: tcp-v2 }
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.2"]
              targetRef: { kind: Pod, name: tcp-v2-pod }
          ports: [ { port: 9100, protocol: TCP } ]
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: tcp-v3-a
            namespace: default
            labels: { kubernetes.io/service-name: tcp-v3 }
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.3"]
              targetRef: { kind: Pod, name: tcp-v3-pod }
          ports: [ { port: 9100, protocol: TCP } ]
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^\s+balance roundrobin$'
      # v1 weight 70 in slot range SRV0_
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^\s+server SRV0_1 10\.0\.0\.1:9100 .*enabled weight 70'
      # v2 weight 30 in slot range SRV1_
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^\s+server SRV1_1 10\.0\.0\.2:9100 .*enabled weight 30'
      # v3 weight 0 in slot range SRV2_
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^\s+server SRV2_1 10\.0\.0\.3:9100 .*enabled weight 0'

  test-tcproute-multi-listener-single-backend:
    requires: [gateways, tcproutes]
    description: "A TCPRoute with a no-sectionName parentRef attaches to EVERY TCP listener on the Gateway (here ports 9100 and 9101). Both listener ports resolve to the same (route, rule) backend name gtw_tcp_default_tcp-multi_0. The chart must emit that backend exactly ONCE (a per-port frontend each, both forwarding to the shared backend) — emitting it per-port would declare a duplicate `backend` name, which HAProxy 3.3+ rejects as a fatal parse error."
    extraContext:
      controllerName: "haproxy-haptic.org/controller"
    fixtures:
      gateways:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: Gateway
          metadata:
            name: gw-tcp-multi
            namespace: default
            generation: 1
          spec:
            gatewayClassName: haptic
            listeners:
              - name: tcp1
                protocol: TCP
                port: 9100
                allowedRoutes:
                  namespaces:
                    from: Same
                  kinds:
                    - kind: TCPRoute
              - name: tcp2
                protocol: TCP
                port: 9101
                allowedRoutes:
                  namespaces:
                    from: Same
                  kinds:
                    - kind: TCPRoute
      tcproutes:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: TCPRoute
          metadata:
            name: tcp-multi
            namespace: default
            generation: 1
          spec:
            parentRefs:
              - name: gw-tcp-multi
            rules:
              - backendRefs:
                  - name: tcp-backend
                    port: 9100
      services:
        - apiVersion: v1
          kind: Service
          metadata:
            name: tcp-backend
            namespace: default
          spec:
            ports:
              - port: 9100
                targetPort: 9100
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: tcp-backend-abc
            namespace: default
            labels:
              kubernetes.io/service-name: tcp-backend
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.42"]
              targetRef:
                kind: Pod
                name: tcp-backend-pod-1
          ports:
            - port: 9100
              protocol: TCP
    assertions:
      # haproxy -c is the decisive check: a duplicate `backend` name is a
      # fatal parse error, so this fails if the dedup regresses.
      - type: haproxy_valid
      # Both listener ports get their own frontend...
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^frontend gateway-tcp-port-9100$'
      - type: contains
        target: haproxy.cfg
        pattern: '(?m)^frontend gateway-tcp-port-9101$'
      # ...forwarding to the one shared backend.
      - type: match_count
        target: haproxy.cfg
        pattern: '(?m)^backend gtw_tcp_default_tcp-multi_0$'
        expected: "1"

  test-tcproute-unattached-no-frontend:
    requires: [gateways, tcproutes]
    description: "A TCPRoute whose parentRef points at a Gateway with no matching TCP listener (wrong port) produces no tcp frontend/backend — attachment gating must hold."
    extraContext:
      controllerName: "haproxy-haptic.org/controller"
    fixtures:
      gateways:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: Gateway
          metadata:
            name: gw-tcp-mismatch
            namespace: default
            generation: 1
          spec:
            gatewayClassName: haptic
            listeners:
              - name: tcp
                protocol: TCP
                port: 9200
                allowedRoutes:
                  namespaces:
                    from: Same
                  kinds:
                    - kind: TCPRoute
      tcproutes:
        - apiVersion: gateway.networking.k8s.io/v1
          kind: TCPRoute
          metadata:
            name: tcp-app-mismatch
            namespace: default
            generation: 1
          spec:
            parentRefs:
              # sectionName targets a listener name that does not exist
              - name: gw-tcp-mismatch
                sectionName: nonexistent
            rules:
              - backendRefs:
                  - name: tcp-backend
                    port: 9200
      services:
        - apiVersion: v1
          kind: Service
          metadata:
            name: tcp-backend
            namespace: default
          spec:
            ports:
              - port: 9200
                targetPort: 9200
    assertions:
      - type: haproxy_valid
      - type: not_contains
        target: haproxy.cfg
        pattern: '(?m)^frontend gateway-tcp-port-9200$'
      - type: not_contains
        target: haproxy.cfg
        pattern: '(?m)^backend gtw_tcp_default_tcp-app-mismatch_0$'
