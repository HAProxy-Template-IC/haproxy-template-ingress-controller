apiVersion: v2
name: ssl
description: SSL/TLS library — HTTPS frontend, certificate wiring, SSL passthrough infrastructure. Carries only library YAML; the parent chart merges it into the HAProxyTemplateConfig.
version: 0.1.0
