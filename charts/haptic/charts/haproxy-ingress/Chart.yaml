apiVersion: v2
name: haproxy-ingress
description: HAPTIC template library (haproxy-ingress) — split-library fragments merged into the HAProxyTemplateConfig by the parent. Carries no templates of its own.
version: 0.1.0
