apiVersion: v2
name: ingress-annotations-compat
description: Shared scaffold and macros for the Ingress vendor annotation libraries. Carries only library YAML; the parent chart merges it into the HAProxyTemplateConfig.
version: 0.1.0
