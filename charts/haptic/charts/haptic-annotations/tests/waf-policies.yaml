# HAPTIC-native reusable Coraza WAF policy tests.
#
# Every test is self-contained: it pins the exact extraContext.waf values its
# fixtures and assertions depend on (per-test extraContext deep-merges into the
# global context — map leaves win, list/scalar leaves replace — so a pinned
# `configMapRefs: { __replace__: true }` removes values-configured catalogs for that test while
# sibling keys survive). The tests therefore run identically under any Helm
# values, with one structural caveat: map-valued registries (policies.inline)
# deep-merge, so deployment-defined sibling keys join the pinned ones. A test
# that needs the EXACT key set adds `__replace__: true` inside the map — the
# runner then replaces the deployment subtree wholesale and strips the
# sentinel (see the invalid-policy test). Without the sentinel, assertions
# must not depend on the full key set, and test policy names must be unlikely
# to collide with deployment policy names. The only `_helm_skip_test` gates left are chart-render-time
# infrastructure requirements that no per-test extraContext can satisfy:
#   - the SPOA hub + coraza plugin must be in the bundle (the coraza maps and
#     spoa-hub-config.toml only exist when the spoa-hub library is merged), and
#   - the nginx-ingress / haproxy-ingress vendor library must be loaded when a
#     fixture's vendor annotation is scanned by that library's snippets.
_fx:
  spoahubCoraza: &spoahubCoraza
    enabled: true
    plugins:
      - name: coraza
        library: libcoraza_plugin.so
        messages: [check-request]
        timeoutMs: 15
        params: ""
        dependsOn: []
        directives: |
          Include @coraza.conf-recommended
          Include @crs-setup.conf.example
          Include @owasp_crs/*.conf
          SecRuleEngine On
  wafIngresspermissions: &wafIngresspermissions
    ingressPermissions: {allowPolicySelection: true}
    policies:
      inline: {body-inspection: {requestBody: {mode: any}}, headers-only: {requestBody: {mode: none}}}
      configMapRefs: { __replace__: true }
  wafSelfserviceCatalogs: &wafSelfserviceCatalogs
    - apiVersion: v1
      kind: ConfigMap
      metadata: { namespace: team-a, name: waf-policies }
      data:
        policies.yaml: |
          app-baseline:
            requestBody:
              mode: none
  specRulesHost: &specRulesHost
    ingressClassName: haptic
    rules:
      - host: app.team-a.example.com
        http:
          paths:
            - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
  wafPoliciesRoot: &wafPoliciesRoot
    policies: {defaultPolicy: "", configMapRefs: { __replace__: true }, selfService: {enabled: true}}
  httpRoot: &httpRoot
    paths:
      - path: /
        pathType: Prefix
        backend: { service: { name: echo, port: { number: 80 } } }
  policiesRootInline: &policiesRootInline
    defaultPolicy: ""
    inline: {baseline: {}}
    configMapRefs: { __replace__: true }

validationTests:
  test-haptic-waf-policy-cross-namespace-shared:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: exact cross-namespace catalog source is trusted; one policy compiles once and is shared by multiple Ingresses
    extraContext:
      waf:
        ingressPermissions: {allowPolicySelection: true, allowEnforcementOverride: true}
        policies:
          defaultPolicy: ""
          requestBody: {defaultMaxBytes: 4096}
          # Replace (not merge) the deployment's inline catalog: dev-values
          # defines an inline 'streaming-search' that would otherwise
          # collide with this test's trusted-ConfigMap policy of the same
          # name (duplicate-name hard fail at the load gate).
          inline: {__replace__: true}
          configMapRefs:
            __replace__: true
            security: {namespace: security, name: haptic-waf-policies, key: policies.yaml}
    fixtures:
      configmaps:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: security, name: haptic-waf-policies }
          data:
            policies.yaml: |
              streaming-search:
                description: Metadata inspection with narrow search exclusions and no body buffering
                requestBody:
                  mode: none
                enforcement: deny
                ruleExclusions:
                  - tags: [attack-sqli, attack-xss]
                    excludeTarget: "ARGS:q"
        # An Ingress author may create a same-named ConfigMap in their own
        # namespace, but it is not a configured source and must be ignored.
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: default, name: haptic-waf-policies }
          data:
            policies.yaml: |
              streaming-search:
                requestBody:
                  mode: json
                secLang: |
                  SecRuleEngine Off
                  # UNTRUSTED_INGRESS_NAMESPACE_POLICY
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: streaming-one
            annotations:
              haproxy-haptic.org/waf-policy: streaming-search
          spec:
            ingressClassName: haptic
            rules:
              - host: streaming-one.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: { name: app, port: { number: 3000 } }}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: streaming-two
            annotations:
              haproxy-haptic.org/waf-policy: streaming-search
              haproxy-haptic.org/waf-mode: detect
          spec:
            ingressClassName: haptic
            rules:
              - host: streaming-two.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: { name: app, port: { number: 3000 } }}
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-app.map", pattern: "default/streaming-one policy:streaming-search" }
      - { type: contains, target: "map:coraza-app.map", pattern: "default/streaming-two policy:streaming-search" }
      - { type: contains, target: "map:coraza-body.map", pattern: "default/streaming-one none any 4096" }
      - { type: contains, target: "map:haproxy-ingress-waf.map", pattern: "default/streaming-two detect" }
      - { type: match_count, target: "file:spoa-hub-config.toml", pattern: '\[plugins\.params\.applications\."policy:streaming-search"\]', expected: "1" }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: 'SecRuleUpdateTargetByTag "attack-sqli" "!ARGS:q"' }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: 'SecRuleUpdateTargetByTag "attack-xss" "!ARGS:q"' }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: "SecRequestBodyAccess Off" }
      - { type: not_contains, target: "file:spoa-hub-config.toml", pattern: "UNTRUSTED_INGRESS_NAMESPACE_POLICY" }

  test-haptic-waf-policy-buffered-contracts:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: policy authors opt into bounded complete body inspection and optional JSON media-type enforcement
    extraContext:
      requestBodyInspection: {haproxyBuffer: {sizeBytes: 16384}}
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          requestBody: {waitTimeout: "100ms", defaultMaxBytes: 4096}
          inline:
            form-body-inspection: {requestBody: {mode: any}}
            json-api-inspection: {requestBody: {mode: json, maxBytes: 2048}}
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: web
            annotations: { haproxy-haptic.org/waf-policy: form-body-inspection }
          spec:
            ingressClassName: haptic
            rules:
              - host: web.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: { service: { name: web, port: { number: 80 } } }
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: api
            annotations: { haproxy-haptic.org/waf-policy: json-api-inspection }
          spec:
            ingressClassName: haptic
            rules:
              - host: api.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: { service: { name: api, port: { number: 80 } } }
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-body.map", pattern: "default/web buffered any 4096" }
      - { type: contains, target: "map:coraza-body.map", pattern: "default/api buffered json 2048" }
      - { type: contains, target: haproxy.cfg, pattern: "tune.bufsize 16384" }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: '(?s)\[plugins\.params\.applications\."policy:form-body-inspection"\].*request_body_limit = 4096.*request_body_in_memory_limit = 4096' }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: '(?s)\[plugins\.params\.applications\."policy:json-api-inspection"\].*request_body_limit = 2048.*request_body_in_memory_limit = 2048' }
      # Body-contract rejections skip detect (shadow) mode and never fire on a
      # bodyless request; the 411 rejects only a chunked body with no length.
      - { type: contains, target: haproxy.cfg, pattern: "return status 415 if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\} !\\{ var\\(txn.coraza_detect_only\\) -m bool \\} \\{ var\\(txn.coraza_content_policy\\) -m str json \\} \\{ req.hdr\\(Content-Length\\) -m found \\}" }
      - { type: contains, target: haproxy.cfg, pattern: "return status 411 if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\} !\\{ var\\(txn.coraza_detect_only\\) -m bool \\} \\{ req.hdr\\(Transfer-Encoding\\) -m found \\} !\\{ req.hdr\\(Content-Length\\) -m found \\}" }
      # The wait is split by enforcement mode: detect (shadow) must never block
      # live traffic, so it waits only for a DECLARED body — an HTTP/2 streaming
      # request (gRPC bidi: no Content-Length, body open until the peer is done)
      # would otherwise run the wait to its timeout and get a 408 the backend
      # never saw. Enforce still waits unconditionally: a body it cannot bound
      # must not reach the backend uninspected.
      - { type: contains, target: haproxy.cfg, pattern: "wait-for-body time 100ms if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\} \\{ var\\(txn.coraza_detect_only\\) -m bool \\} \\{ req.hdr\\(Content-Length\\) -m found \\}" }
      - { type: contains, target: haproxy.cfg, pattern: "wait-for-body time 100ms if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\} !\\{ var\\(txn.coraza_detect_only\\) -m bool \\}" }
      # A detect-mode wait that is not gated on Content-Length is the bug: it
      # blocks streaming traffic that shadow mode promises never to block.
      - { type: not_contains, target: haproxy.cfg, pattern: "wait-for-body time 100ms if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\}\n" }
      # body-complete mirrors the wait split: the SPOE contract only allows it
      # to be asserted where a wait actually ran, so detect mode may not claim a
      # complete body for a request whose length was never declared.
      - { type: contains, target: haproxy.cfg, pattern: "set-var\\(txn.coraza_body_complete\\) bool\\(true\\) if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\} \\{ var\\(txn.coraza_detect_only\\) -m bool \\} \\{ req.hdr\\(Content-Length\\) -m found \\}" }
      - { type: contains, target: haproxy.cfg, pattern: "set-var\\(txn.coraza_body_complete\\) bool\\(true\\) if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\} !\\{ var\\(txn.coraza_detect_only\\) -m bool \\}" }
      - { type: not_contains, target: haproxy.cfg, pattern: "set-var\\(txn.coraza_body_complete\\) bool\\(true\\) if \\{ var\\(txn.coraza_body_mode\\) -m str buffered \\}\n" }
      - { type: contains, target: "file:spoe.conf", pattern: "body-mode=var\\(txn.coraza_body_mode\\) body-complete=var\\(txn.coraza_body_complete\\) detect-only=var\\(txn.coraza_detect_only\\)" }

  test-haptic-waf-body-policy-on-grpc-backend-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      A body-inspecting WAF policy on an Ingress that declares a gRPC backend is
      rejected at admission. Coraza cannot inspect a gRPC stream — the buffered
      wait times out with 408 while unary calls keep working, so the breakage is
      invisible until someone opens a stream.
    extraContext:
      renderMode: admission
      admissionSubject: { stores: { ingresses: true }, namespace: default, name: grpc-api }
      waf: *wafIngresspermissions
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: grpc-api
            annotations: { haproxy-haptic.org/waf-policy: body-inspection, haproxy-haptic.org/backend-protocol: grpc }
          spec:
            ingressClassName: haptic
            rules:
              - host: grpc-api.example.com
                http: *httpRoot
    assertions:
      - type: contains
        target: rendering_error
        pattern: "has a gRPC backend and WAF policy 'body-inspection'"

  test-haptic-waf-body-policy-on-nginx-grpc-backend-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: the same guard sees the nginx-compat spelling of a gRPC backend
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: grpc-api-nginx }
      waf: *wafIngresspermissions
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: grpc-api-nginx
            annotations: { haproxy-haptic.org/waf-policy: body-inspection, nginx.ingress.kubernetes.io/backend-protocol: GRPCS }
          spec:
            ingressClassName: haptic
            rules:
              - host: grpc-api-nginx.example.com
                http: *httpRoot
    assertions:
      - {type: contains, target: rendering_error, pattern: "has a gRPC backend and WAF policy"}

  test-haptic-waf-headers-only-policy-on-grpc-backend-allowed:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      requestBody.mode none is the supported combination for gRPC, so it must
      render cleanly — the guard must not fire on the configuration it tells
      operators to adopt. Plain h2 is likewise untouched: HTTP/2 to the backend
      is equally a REST backend whose bodies ARE bounded.
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: grpc-ok }
      waf: *wafIngresspermissions
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: grpc-ok
            annotations: { haproxy-haptic.org/waf-policy: headers-only, haproxy-haptic.org/backend-protocol: grpc }
          spec:
            ingressClassName: haptic
            rules:
              - host: grpc-ok.example.com
                http: *httpRoot
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: h2-rest
            annotations: { haproxy-haptic.org/waf-policy: body-inspection, haproxy-haptic.org/backend-protocol: h2 }
          spec:
            ingressClassName: haptic
            rules:
              - host: h2-rest.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: "map:coraza-body.map", pattern: "default/grpc-ok none"}
      - {type: contains, target: "map:coraza-body.map", pattern: "default/h2-rest buffered"}

  test-haptic-waf-body-policy-on-grpc-backend-warns-on-reconcile:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      On a live reconcile the same combination must NOT abort the render. A
      pre-existing gRPC Ingress with a body-inspecting policy records a Warning
      Event and keeps serving: unary calls are inspected correctly and the
      streaming ones already fail closed with 408, so there is no fail-open to
      close and no reason to take the fleet down. This is the regression pin
      for the admission-wedge class — a chart upgrade that starts rejecting a
      pre-existing annotation must never break every render.
    extraContext:
      renderMode: reconcile
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          inline: {body-inspection: {requestBody: {mode: any}}}
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: grpc-legacy
            annotations: { haproxy-haptic.org/waf-policy: body-inspection, haproxy-haptic.org/backend-protocol: grpc }
          spec:
            ingressClassName: haptic
            rules:
              - host: grpc-legacy.example.com
                http: *httpRoot
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: unaffected}
          spec:
            ingressClassName: haptic
            rules:
              - host: unaffected.example.com
                http: *httpRoot
    assertions:
      # The render completes at all — that is the property under test.
      - type: haproxy_valid
      # The offending route keeps its policy rather than being 503'd: the
      # runtime already refuses exactly the calls that cannot be inspected.
      - {type: contains, target: "map:coraza-body.map", pattern: "default/grpc-legacy buffered"}
      # And an unrelated Ingress still renders, which is what the wedge class
      # destroyed.
      - {type: contains, target: "map:host.map", pattern: "unaffected\\.example\\.com"}

  test-haptic-waf-policy-seclang-shared:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: policy-authored SecLang compiles once and remains reusable across Ingresses
    extraContext:
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline:
            internal-app:
              requestBody: {mode: json, maxBytes: 2048}
              secLang: |
                SecRuleRemoveById 999999
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: custom-one, annotations: { haproxy-haptic.org/waf-policy: internal-app } }
          spec:
            ingressClassName: haptic
            rules:
              - host: custom-one.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: custom-two, annotations: { haproxy-haptic.org/waf-policy: internal-app } }
          spec:
            ingressClassName: haptic
            rules:
              - host: custom-two.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-body.map", pattern: "default/custom-one buffered json 2048" }
      - { type: contains, target: "map:coraza-app.map", pattern: "default/custom-two policy:internal-app" }
      - { type: match_count, target: "file:spoa-hub-config.toml", pattern: '\[plugins\.params\.applications\."policy:internal-app"\]', expected: "1" }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: "SecRuleRemoveById 999999" }

  test-haptic-waf-policy-invalid-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: under the admission webhook, an unknown exact policy name is rejected with the approved names
    extraContext:
      admissionSubject: { store: ingresses, namespace: default, name: invalid-policy }
      renderMode: admission
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline:
            # __replace__ makes this map REPLACE the deployment's inline
            # catalog instead of deep-merging into it (the sentinel is
            # stripped by the test runner), so the available-policies list
            # below is exact under any Helm values.
            __replace__: true
            approved-policy: {}
          configMapRefs: { __replace__: true }
          selfService: {enabled: false}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: invalid-policy, annotations: { haproxy-haptic.org/waf-policy: typo-policy } }
          spec:
            ingressClassName: haptic
            rules:
              - host: invalid.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      # Test extraContext DEEP-MERGES into the deployment's extraContext
      # (deepMergeMaps in pkg/controller/testrunner), so a live cluster's own
      # inline/configMapRef policies join the pinned `approved-policy` in the
      # available-policies list — the assertion must tolerate neighbors it
      # cannot pin away, or this baked test rejects every
      # HAProxyTemplateConfig update on clusters that define policies.
      - { type: contains, target: rendering_error, pattern: 'Unknown WAF policy ''typo-policy''.*available policies: (.*, )?approved-policy(, |\. ).*exact and case-sensitive' }

  test-haptic-waf-policy-invalid-reconcile-fails-closed:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      On a live reconcile, an unknown policy (e.g. deleted out from under an
      existing Ingress) no longer aborts the whole render: the route fails
      closed with 503, a Warning Event lands on the Ingress, and every other
      route keeps rendering.
    extraContext:
      renderMode: reconcile
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies: {defaultPolicy: "", inline: {approved-policy: {}}, configMapRefs: { __replace__: true }}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: invalid-policy, annotations: { haproxy-haptic.org/waf-policy: typo-policy } }
          spec:
            ingressClassName: haptic
            rules:
              - host: invalid.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "http-request deny deny_status 503 if \\{ var\\(txn.resource_id\\) -m str default/invalid-policy \\}"
      - type: contains
        target: events
        pattern: "Warning WafPolicyUnavailable networking.k8s.io/v1 Ingress default/invalid-policy:.*Unknown WAF policy 'typo-policy'"
      # The broken route must not be dispatched to Coraza with a half-applied policy.
      - {type: not_contains, target: "map:coraza-app.map", pattern: "default/invalid-policy"}

  test-haptic-waf-governance-violation-reconcile-fails-closed:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      On a live reconcile, a governance-gate violation on an existing
      Ingress (waf-mode without allowEnforcementOverride) no longer aborts
      the whole render: the violating route fails closed with 503 and a
      Warning Event, and the rest of the cluster keeps rendering. This is
      the regression pin for the admission-wedge class where a chart
      upgrade that tightens governance against pre-existing annotations
      broke every render and, with it, every config and Ingress update.
    extraContext:
      renderMode: reconcile
      waf:
        ingressPermissions: {allowEnforcementOverride: false, allowPolicySelection: true}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: legacy-mode
            annotations:
              haproxy-haptic.org/waf-policy: baseline
              haproxy-haptic.org/waf-mode: detect
          spec:
            ingressClassName: haptic
            rules:
              - host: legacy-mode.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: healthy
            annotations:
              haproxy-haptic.org/waf-policy: baseline
          spec:
            ingressClassName: haptic
            rules:
              - host: healthy.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "http-request deny deny_status 503 if \\{ var\\(txn.resource_id\\) -m str default/legacy-mode \\}"
      - type: contains
        target: events
        pattern: "Warning WafPermissionDenied networking.k8s.io/v1 Ingress default/legacy-mode:.*allowEnforcementOverride=false"
      # Broken routes must not register Coraza applications or dispatch rows.
      - { type: not_contains, target: "map:coraza-app.map", pattern: "default/legacy-mode" }
      # The untouched route keeps its policy dispatch.
      - { type: contains, target: "map:coraza-app.map", pattern: "default/healthy policy:baseline" }

  test-haptic-waf-governance-violation-unrelated-admission-unblocked:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      Under the admission webhook, a governance violation on an already
      present Ingress must not deny an UNRELATED resource's admission (or a
      HAProxyTemplateConfig update, which renders with no admission
      subject): the violating route fails closed per-route and the render
      succeeds. Only the violating resource's own admission hard-fails
      (pinned by test-haptic-waf-strict-mode-rejected).
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: unrelated }
      waf:
        ingressPermissions: {allowEnforcementOverride: false}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: pre-existing-violator
            annotations:
              haproxy-haptic.org/waf-policy: baseline
              haproxy-haptic.org/waf-mode: detect
          spec:
            ingressClassName: haptic
            rules:
              - host: violator.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: unrelated}
          spec:
            ingressClassName: haptic
            rules:
              - host: unrelated.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "http-request deny deny_status 503 if \\{ var\\(txn.resource_id\\) -m str default/pre-existing-violator \\}"
      - { type: not_contains, target: "map:coraza-app.map", pattern: "default/pre-existing-violator" }

  test-haptic-waf-policy-structured-crs-tuning:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      Structured CRS tuning fields (allowedMethods, paranoiaLevel,
      anomalyThreshold) lower to SETUP-position SecActions spliced BEFORE
      the CRS rules include in the app directives — the position raw policy
      secLang structurally cannot reach (it lands after the include, where
      CRS has already read the tx variables).
    extraContext:
      # Pin the coraza plugin (list leaf → replaces the deployment's plugin
      # set wholesale) so the splice-anchor and base directives are exactly
      # known — a deployment may run a CRS-less custom ruleset (the e2e
      # profile does), which would otherwise hard-fail these policies at
      # the load gate.
      spoaHub: *spoahubCoraza
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline:
            tuned-api:
              enforcement: deny
              allowedMethods: [GET, HEAD, POST, OPTIONS, PUT, PATCH, DELETE]
              paranoiaLevel: 2
              anomalyThreshold: {inbound: 10}
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: api, annotations: { haproxy-haptic.org/waf-policy: tuned-api } }
          spec:
            ingressClassName: haptic
            rules:
              - host: api.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: api, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)applications\."policy:tuned-api"\].*?tx\.allowed_methods=GET HEAD POST OPTIONS PUT PATCH DELETE.*?Include @owasp_crs'
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)applications\."policy:tuned-api"\].*?tx\.blocking_paranoia_level=2.*?Include @owasp_crs'
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)applications\."policy:tuned-api"\].*?tx\.inbound_anomaly_score_threshold=10.*?Include @owasp_crs'

  test-haptic-waf-policy-crs-settings-tuning:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      crsSettings fine-tunes a rule's INPUTS instead of disabling it: the git
      content-type case allowlists application/x-git-upload-pack-request (rather
      than excluding CRS 920420). mergeList emits the chart-owned CRS default
      list PLUS the additions as a full REPLACE (each type |type|-wrapped, the
      way 920420 matches — a runtime %{tx.<var>} append was the earlier bug,
      because the crs-setup default is commented out and unset). boundedInt
      entries replace a scalar. All lower to one setup-position SecAction
      spliced BEFORE the CRS include.
    extraContext:
      spoaHub: *spoahubCoraza
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline:
            git-host:
              enforcement: deny
              requestBody: { mode: none }
              crsSettings:
                allowedRequestContentTypes: [application/x-git-upload-pack-request, application/x-git-receive-pack-request]
                maxFileSize: 1048576
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: git, annotations: { haproxy-haptic.org/waf-policy: git-host } }
          spec:
            ingressClassName: haptic
            rules:
              - host: git.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: git, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      # mergeList REPLACES the variable with the chart-owned CRS default list PLUS
      # the git types, each |type|-wrapped (how 920420 matches |content-type|
      # @within), spliced before the CRS include. The default types are present,
      # then both git types wrapped.
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)applications\."policy:git-host"\].*?tx\.allowed_request_content_type=\|application/x-www-form-urlencoded\|.*?\|application/json\| \|application/x-git-upload-pack-request\| \|application/x-git-receive-pack-request\|.*?Include @owasp_crs'
      # Regression guard: it must NOT be a runtime %{tx...} append (the bug that
      # 403'd git clones because the crs-setup default is unset).
      - type: not_contains
        target: "file:spoa-hub-config.toml"
        pattern: 'allowed_request_content_type=%\{tx'
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)applications\."policy:git-host"\].*?tx\.max_file_size=1048576.*?Include @owasp_crs'

  test-haptic-waf-policy-crs-settings-unknown-field-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: crsSettings is a curated allowlist — an unknown variable name fails closed
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline: {bad-crs: {crsSettings: {notATunable: 5}}}
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - {type: contains, target: rendering_error, pattern: "crsSettings contains unknown field 'notATunable'"}

  test-haptic-waf-policy-crs-settings-invalid-value-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: crsSettings appendList tokens are charset-validated (blocks delimiter/quote injection into the setvar)
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline:
            bad-crs:
              crsSettings:
                allowedRequestContentTypes: ["not a type"]
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - type: contains
        target: rendering_error
        pattern: "crsSettings.allowedRequestContentTypes entry 'not a type' is invalid"

  test-haptic-waf-policy-crs-settings-bounded-value-in-error:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: an out-of-range boundedInt names the offending value, not just the expected range
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline: {bad-crs: {crsSettings: {maxFileSize: 999999999}}}
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - type: contains
        target: rendering_error
        pattern: "crsSettings.maxFileSize value '999999999' must be a byte count between 1 and 536870912"

  test-haptic-waf-policy-crs-settings-dedup:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: mergeList dedupes — a type already in the CRS default, and a type listed twice, each appear once in the emitted allowlist
    extraContext:
      spoaHub: *spoahubCoraza
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline:
            dup:
              enforcement: deny
              requestBody: { mode: none }
              crsSettings:
                allowedRequestContentTypes: [application/json, application/x-git-upload-pack-request, application/x-git-upload-pack-request]
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: dup, annotations: { haproxy-haptic.org/waf-policy: dup } }
          spec:
            ingressClassName: haptic
            rules:
              - host: dup.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: dup, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      # the git type (not in the CRS default) is added once...
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '\|application/x-git-upload-pack-request\|'
      # ...not doubled, even though it was listed twice (dedup among additions)...
      - type: not_contains
        target: "file:spoa-hub-config.toml"
        pattern: '\|application/x-git-upload-pack-request\| \|application/x-git-upload-pack-request\|'
      # ...and application/json (already in the CRS default) is not re-added.
      - type: not_contains
        target: "file:spoa-hub-config.toml"
        pattern: '\|application/json\| \|application/json\|'

  test-haptic-waf-policy-rule-exclusions-lower-correctly:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      ruleExclusions lower to the right CRS directives at the right position:
      path-scoped disable -> a chart-owned SecRule + ctl:ruleRemoveById BEFORE
      the CRS include; path-scoped target exclude -> ctl:ruleRemoveTargetById;
      global (no-path) disable/target -> SecRuleRemoveById / SecRuleUpdateTargetById
      AFTER the CRS include. The tenant never writes a SecRule or a regex.
    extraContext:
      spoaHub: *spoahubCoraza
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline:
            tuned:
              enforcement: deny
              ruleExclusions:
                - rules: [930130]
                  onPathContains: ".git/"
                - rules: [941320]
                  excludeTarget: "ARGS:wp_post"
                  onPathPrefix: "/wp-admin/"
                - rules: [913100]
                - rules: [942150]
                  excludeTarget: "ARGS:query"
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: app, annotations: { haproxy-haptic.org/waf-policy: tuned } }
          spec:
            ingressClassName: haptic
            rules:
              - host: app.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      # Path-scoped disable: a chart-generated SecRule with a literal @contains and a ctl, before the CRS include.
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)SecRule REQUEST_URI "@contains \.git/".*ctl:ruleRemoveById=930130.*Include @owasp_crs'
      # Path-scoped target exclusion via ctl:ruleRemoveTargetById.
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)SecRule REQUEST_URI "@beginsWith /wp-admin/".*ctl:ruleRemoveTargetById=941320;ARGS:wp_post.*Include @owasp_crs'
      # Global (no-path) forms land AFTER the CRS include.
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)Include @owasp_crs.*SecRuleRemoveById 913100'
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)Include @owasp_crs.*SecRuleUpdateTargetById 942150 "!ARGS:query"'

  test-haptic-waf-policy-rule-exclusions-selfservice:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      A self-service catalog (no secLang) can author ruleExclusions — the
      capability that lets a namespace tune its own CRS false positives with
      no admin involvement. Regression pin for the forgejo git-host case
      (disable 930130 on .git paths so git-over-HTTP stops false-positiving).
    extraContext:
      spoaHub: *spoahubCoraza
      waf: *wafPoliciesRoot
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              app:
                enforcement: deny
                ruleExclusions:
                  - rules: [930130]
                    onPathContains: ".git/"
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: git, annotations: { haproxy-haptic.org/waf-policy: app } }
          spec:
            ingressClassName: haptic
            rules:
              - host: git.team-a.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: git, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-app.map", pattern: "team-a/git policy:team-a/app" }
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)applications\."policy:team-a/app".*SecRule REQUEST_URI "@contains \.git/".*ctl:ruleRemoveById=930130'

  test-haptic-waf-policy-rule-exclusions-denylist-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: ruleExclusions may not disable the CRS anomaly-scoring / setup / correlation rules
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline:
            bad:
              ruleExclusions:
                - rules: [949110]
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - {type: contains, target: rendering_error, pattern: "may not target rule '949110'"}

  test-haptic-waf-policy-rule-exclusions-regex-target-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: ruleExclusions excludeTarget rejects the /regex/ collection-key form (the Coraza ReDoS surface)
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline:
            bad:
              ruleExclusions:
                - rules: [942150]
                  excludeTarget: "ARGS:/^inject.*/"
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - {type: contains, target: rendering_error, pattern: "regex collection keys are not allowed"}

  test-haptic-waf-policy-structured-fields-selfservice:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      Self-service catalogs may use the structured CRS tuning fields —
      they are validated and lowered by the chart, unlike raw secLang,
      which self-service refuses. This is what lets a namespace team widen
      the method allowlist for its own REST API without any admin grant.
    extraContext:
      # Pin the coraza plugin (list leaf → replaces the deployment's plugin
      # set wholesale) so the splice-anchor and base directives are exactly
      # known — a deployment may run a CRS-less custom ruleset (the e2e
      # profile does), which would otherwise hard-fail these policies at
      # the load gate.
      spoaHub: *spoahubCoraza
      waf: *wafPoliciesRoot
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              rest-api:
                enforcement: deny
                allowedMethods: [GET, POST, PUT, DELETE]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: rest-api } }
          spec: *specRulesHost
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-app.map", pattern: "team-a/app policy:team-a/rest-api" }
      - type: contains
        target: "file:spoa-hub-config.toml"
        pattern: '(?s)applications\."policy:team-a/rest-api"\].*?tx\.allowed_methods=GET POST PUT DELETE.*?Include @owasp_crs'

  test-haptic-waf-policy-structured-fields-invalid-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: invalid structured-field values are rejected with exact messages
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline:
            bad-methods:
              allowedMethods: ["get"]
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - {type: contains, target: rendering_error, pattern: "invalid allowedMethods entry 'get'"}

  test-haptic-waf-policy-paranoia-out-of-range-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: paranoiaLevel outside 1-4 is rejected
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline: {bad-paranoia: {paranoiaLevel: 5}}
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - {type: contains, target: rendering_error, pattern: "paranoiaLevel must be an integer between 1 and 4"}

  test-haptic-waf-policy-threshold-unknown-field-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: anomalyThreshold rejects unknown sub-fields and non-positive values
    extraContext:
      renderMode: admission
      waf:
        policies:
          defaultPolicy: ""
          inline: {bad-threshold: {anomalyThreshold: {sideways: 3}}}
          configMapRefs: { __replace__: true }
    fixtures: {}
    assertions:
      - type: contains
        target: rendering_error
        pattern: "anomalyThreshold contains unknown field 'sideways'"

  test-haptic-waf-policy-setup-fields-need-crs-include:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      A ROUTE that selects a setup-fields policy fails its admission when the
      coraza directives have no 'Include @owasp_crs' anchor to splice the
      SecActions before — silently accepting them would emit setvars CRS
      never reads. Checked per-route at application time (not at policy
      registration), so a catalog policy no route selects never trips it.
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: tuned-route }
      spoaHub:
        enabled: true
        plugins:
          - name: coraza
            library: libcoraza_plugin.so
            messages: [check-request]
            timeoutMs: 15
            params: ""
            dependsOn: []
            directives: |
              SecRuleEngine On
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline:
            tuned:
              allowedMethods: [GET, POST]
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: tuned-route, annotations: { haproxy-haptic.org/waf-policy: tuned } }
          spec:
            ingressClassName: haptic
            rules:
              - host: tuned.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: contains
        target: rendering_error
        pattern: "no 'Include @owasp_crs' line to insert them before"

  test-haptic-waf-policy-setup-fields-unused-with-crsless-coraza-renders:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      A setup-fields policy defined in the catalog but selected by NO route
      must not fail the render even when coraza runs a CRS-less ruleset —
      the anchor requirement is route-scoped. Regression pin for the
      load-gate crash where a deployment-wide allowedMethods policy merged
      into baked tests whose coraza directives lack the CRS include.
    extraContext:
      spoaHub:
        enabled: true
        plugins:
          - name: coraza
            library: libcoraza_plugin.so
            messages: [check-request]
            timeoutMs: 15
            params: ""
            dependsOn: []
            directives: |
              SecRuleEngine On
      waf:
        policies:
          defaultPolicy: ""
          inline:
            tuned:
              allowedMethods: [GET, POST]
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: plain, annotations: {} }
          spec:
            ingressClassName: haptic
            rules:
              - host: plain.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid

  test-haptic-waf-policy-body-limit-without-inspection-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: a body cap cannot silently do nothing under requestBody mode none
    extraContext:
      waf:
        ingressPermissions: {allowPolicySelection: true}
        policies:
          defaultPolicy: ""
          inline: {misleading-body-policy: {requestBody: {mode: none, maxBytes: 1024}}}
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses: []
    assertions:
      - { type: contains, target: rendering_error, pattern: "sets requestBody.maxBytes while requestBody.mode is none.*mode to any or json" }

  test-haptic-waf-policy-vendor-mixing-rejected:
    # Mixing a native HAPTIC WAF annotation with a vendor (nginx) WAF annotation
    # on one Ingress is rejected by the cross-family feature-collision guard in
    # the ingress-annotations-compat library (features-950), which renders
    # before the coraza map files. allowCustomRules is pinned true so a loaded
    # nginx-ingress library accepts the snippet and the ONLY failure is the
    # cross-family rejection.
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: under the admission webhook, a vendor WAF annotation cannot be mixed with a HAPTIC WAF policy on one Ingress
    extraContext:
      # renderMode admission = the webhook dry-run, where a cross-family conflict
      # fail()s so the API server denies the Ingress. (During a live reconcile it
      # would warn via a Kubernetes Event instead — see the reconcile-mode tests
      # in ingress-annotations-compat.yaml.)
      renderMode: admission
      # Pin nginx as an enabled family so the cross-family guard evaluates the
      # nginx annotation regardless of the deployment's library set (the e2e
      # profile, for instance, leaves nginx-ingress off).
      annotationLibraries: {haptic: true, nginx: true, haproxyIngress: false, haproxytech: false}
      waf:
        ingressPermissions: {allowPolicySelection: true, allowCustomRules: true}
        policies: {defaultPolicy: "", inline: {approved-policy: {}}, configMapRefs: { __replace__: true }}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: mixed-waf
            annotations:
              haproxy-haptic.org/waf-policy: approved-policy
              nginx.ingress.kubernetes.io/modsecurity-snippet: 'SecRuleEngine Off'
          spec:
            ingressClassName: haptic
            rules:
              - host: mixed.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "configures the WAF feature through annotations from multiple families" }
      - { type: contains, target: rendering_error, pattern: "haproxy-haptic.org/waf-policy" }
      - { type: contains, target: rendering_error, pattern: "nginx.ingress.kubernetes.io/modsecurity-snippet" }

  test-haptic-waf-vendor-rule-size-limit:
    # The byte-bound rejection is raised by nginx-ingress's
    # util-nginx-ingress-coraza-scan, so that library must be loaded. No policy
    # catalog is configured: the custom-rule bounds apply independently of
    # policy governance.
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) (include "haptic.nginxIngress.disabled" .) }}true{{ end }}'
    description: nginx compatibility SecLang obeys the custom-rule byte bound independently of policy governance
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: oversized-nginx-rules }
      waf:
        ingressPermissions: {allowCustomRules: true}
        customRules: {limits: {maxBytesPerIngress: 256}}
        policies: {defaultPolicy: "", configMapRefs: { __replace__: true }}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: oversized-nginx-rules
            annotations:
              nginx.ingress.kubernetes.io/modsecurity-snippet: >-
                SecRule REQUEST_HEADERS:X-One "@streq one" "id:910001,phase:1,deny"
                SecRule REQUEST_HEADERS:X-Two "@streq two" "id:910002,phase:1,deny"
                SecRule REQUEST_HEADERS:X-Three "@streq three" "id:910003,phase:1,deny"
                SecRule REQUEST_HEADERS:X-Four "@streq four" "id:910004,phase:1,deny"
          spec:
            ingressClassName: haptic
            rules:
              - host: oversized.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "modsecurity-snippet.*is [0-9]+ bytes.*limit is 256" }

  test-haptic-waf-vendor-rule-app-limit:
    # Requires nginx-ingress: the compatibility modsecurity-snippet path is the
    # only per-Ingress private-Coraza-app path, and a second such Ingress
    # overflows the shared custom-rule count bound.
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) (include "haptic.nginxIngress.disabled" .) }}true{{ end }}'
    description: compatibility private Coraza applications are bounded by one shared count
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: nginx-private-rules-2 }
      waf:
        ingressPermissions: {allowPolicySelection: true, allowCustomRules: true}
        customRules: {limits: {maxIngresses: 1}}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: nginx-private-rules
            annotations:
              nginx.ingress.kubernetes.io/modsecurity-snippet: 'SecRule REQUEST_HEADERS:X-Nginx "@streq yes" "id:910011,phase:1,deny"'
          spec:
            ingressClassName: haptic
            rules:
              - host: nginx-private.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: nginx-private-rules-2
            annotations:
              nginx.ingress.kubernetes.io/modsecurity-snippet: 'SecRule REQUEST_HEADERS:X-Nginx2 "@streq yes" "id:910012,phase:1,deny"'
          spec:
            ingressClassName: haptic
            rules:
              - host: nginx-private-2.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "custom-rule Ingress limit 1" }

  # Strict-baseline tests: each pins an immutable admin default policy and/or
  # the specific ingressPermissions denial it asserts.
  test-haptic-waf-strict-default-cannot-be-removed:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: an annotation-free Ingress still receives the immutable admin default policy
    extraContext:
      waf:
        policies:
          defaultPolicy: baseline
          inline: {baseline: {enforcement: deny}}
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: no-annotation }
          spec:
            ingressClassName: haptic
            rules:
              - host: baseline.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:haproxy-ingress-waf.map", pattern: "default/no-annotation deny" }
      - { type: contains, target: "map:coraza-app.map", pattern: "default/no-annotation policy:baseline" }

  test-haptic-waf-strict-policy-selection-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: Ingress owner cannot replace immutable default with another approved policy
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: replace-policy }
      waf:
        ingressPermissions: {allowPolicySelection: false}
        policies:
          defaultPolicy: baseline
          inline: {baseline: {}, alternate: {}}
          configMapRefs: { __replace__: true }
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: replace-policy, annotations: { haproxy-haptic.org/waf-policy: alternate } }
          spec:
            ingressClassName: haptic
            rules:
              - host: replace.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "disabled Ingress policy selection.*default policy 'baseline' applies automatically" }

  test-haptic-waf-strict-nginx-rules-rejected:
    # Requires nginx-ingress: the rejection is raised by that library's scan
    # of nginx.ingress.kubernetes.io/modsecurity-snippet.
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) (include "haptic.nginxIngress.disabled" .) }}true{{ end }}'
    description: nginx compatibility annotation cannot inject SecLang when HAPTIC admin disabled it
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: inject-nginx }
      waf:
        ingressPermissions: {allowPolicySelection: true, allowCustomRules: false}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: inject-nginx
            annotations: { nginx.ingress.kubernetes.io/modsecurity-snippet: "SecRuleEngine Off" }
          spec:
            ingressClassName: haptic
            rules:
              - host: nginx-inject.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "disabled Ingress-authored custom WAF rules.*allowCustomRules=false" }

  test-haptic-waf-strict-mode-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: HAPTIC mode override cannot turn immutable deny baseline into detect mode
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: detect-haptic }
      waf:
        ingressPermissions: {allowEnforcementOverride: false}
        policies: {defaultPolicy: baseline, inline: {baseline: {}}, configMapRefs: { __replace__: true }}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: detect-haptic, annotations: { haproxy-haptic.org/waf-mode: detect } }
          spec:
            ingressClassName: haptic
            rules:
              - host: detect.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "disabled Ingress enforcement overrides.*allowEnforcementOverride=false" }

  test-haptic-waf-strict-vendor-mode-rejected:
    # Requires haproxy-ingress: the rejection is raised by that library's
    # haproxy-ingress.github.io/waf-mode map template.
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) (not .Values.controller.templateLibraries.haproxyIngress.enabled) }}true{{ end }}'
    description: vendor detect mode cannot bypass immutable HAPTIC enforcement
    extraContext:
      waf:
        ingressPermissions: {allowPolicySelection: true, allowEnforcementOverride: false}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: detect-vendor
            annotations:
              haproxy-ingress.github.io/waf: modsecurity
              haproxy-ingress.github.io/waf-mode: detect
          spec:
            ingressClassName: haptic
            rules:
              - host: detect-vendor.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "disabled Ingress enforcement overrides.*allowEnforcementOverride=false" }

  test-haptic-waf-strict-nginx-disable-rejected:
    # Requires nginx-ingress: the rejection is raised by that library's scan
    # of nginx.ingress.kubernetes.io/enable-modsecurity.
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) (include "haptic.nginxIngress.disabled" .) }}true{{ end }}'
    description: nginx compatibility opt-out cannot disable immutable HAPTIC default policy
    extraContext:
      renderMode: admission
      admissionSubject: { store: ingresses, namespace: default, name: disable-nginx }
      waf:
        ingressPermissions: {allowPolicySelection: true, allowWafDisable: false}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: disable-nginx
            annotations: { nginx.ingress.kubernetes.io/enable-modsecurity: "false" }
          spec:
            ingressClassName: haptic
            rules:
              - host: disable.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "disables WAF.*disabled Ingress WAF opt-outs.*allowWafDisable=false" }

  test-haptic-waf-strict-native-raw-config-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: native raw frontend injection cannot short-circuit an immutable WAF baseline
    extraContext:
      waf:
        ingressPermissions: {allowPolicySelection: true, allowRawHAProxyConfig: false}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: native-raw-bypass
            annotations:
              haproxy-haptic.org/config-frontend: "http-request allow"
          spec:
            ingressClassName: haptic
            rules:
              - host: native-raw.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "config-frontend.*allowRawHAProxyConfig=false.*bypass WAF enforcement" }

  test-haptic-waf-strict-vendor-raw-config-rejected:
    # The raw-annotation rejection is raised by the native scan (it checks the
    # vendor config-* annotations directly), so no vendor library is required.
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: vendor raw defaults injection cannot affect an immutable WAF baseline
    extraContext:
      waf:
        ingressPermissions: {allowPolicySelection: true, allowRawHAProxyConfig: false}
        policies: *policiesRootInline
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: vendor-raw-bypass
            annotations:
              haproxy-ingress.github.io/config-defaults: "http-request allow"
          spec:
            ingressClassName: haptic
            rules:
              - host: vendor-raw.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "config-defaults.*allowRawHAProxyConfig=false.*bypass WAF enforcement" }

  # ---------------------------------------------------------------------------
  # Namespaced self-service policy authoring (waf.policies.selfService).
  # ---------------------------------------------------------------------------

  test-haptic-waf-selfservice-policy-applied:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: a namespace's own waf-policies ConfigMap is discovered and its policy is selectable by bare name from that namespace, registered under the namespace-scoped app key
    extraContext:
      waf: *wafPoliciesRoot
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              app-baseline:
                description: Team A baseline, metadata only
                enforcement: detect
                requestBody:
                  mode: none
                ruleExclusions:
                  - tags: [attack-sqli]
                    excludeTarget: "ARGS:q"
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: app-baseline } }
          spec: *specRulesHost
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-app.map", pattern: "team-a/app policy:team-a/app-baseline" }
      - { type: contains, target: "map:haproxy-ingress-waf.map", pattern: "team-a/app detect" }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: '\[plugins\.params\.applications\."policy:team-a/app-baseline"\]' }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: 'SecRuleUpdateTargetByTag "attack-sqli" "!ARGS:q"' }

  test-haptic-waf-selfservice-foreign-namespace-invisible:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: another namespace's self-service policy is invisible — selecting it from a different namespace is an unknown policy (admission)
    extraContext:
      admissionSubject: { store: ingresses, namespace: team-b, name: freeloader }
      renderMode: admission
      waf: *wafPoliciesRoot
    fixtures:
      waf_selfservice_catalogs: *wafSelfserviceCatalogs
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-b, name: freeloader, annotations: { haproxy-haptic.org/waf-policy: app-baseline } }
          spec:
            ingressClassName: haptic
            rules:
              - host: b.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "Unknown WAF policy 'app-baseline' on Ingress team-b/freeloader" }

  test-haptic-waf-selfservice-cross-namespace-address-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: explicit cross-namespace addressing (ns/name) in waf-policy is rejected — self-service selection is bare-name, own-namespace only
    extraContext:
      admissionSubject: { store: ingresses, namespace: team-b, name: sneaky }
      renderMode: admission
      waf: *wafPoliciesRoot
    fixtures:
      waf_selfservice_catalogs: *wafSelfserviceCatalogs
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-b, name: sneaky, annotations: { haproxy-haptic.org/waf-policy: team-a/app-baseline } }
          spec:
            ingressClassName: haptic
            rules:
              - host: sneaky.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - { type: contains, target: rendering_error, pattern: "policy names must not contain '/'" }

  test-haptic-waf-selfservice-broken-catalog-scoped:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      A broken self-service catalog is SCOPED on a live reconcile: the render
      succeeds, a Warning Event lands on the ConfigMap, the selecting route in
      that namespace fails closed with 503 — and a trusted-catalog route in
      another namespace renders untouched.
    extraContext:
      renderMode: reconcile
      waf:
        policies:
          defaultPolicy: ""
          configMapRefs: { __replace__: true }
          inline: {approved-policy: {requestBody: {mode: none}}}
          selfService: {enabled: true}
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              this: [is: not, valid yaml
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: app-baseline } }
          spec: *specRulesHost
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: trusted-user, annotations: { haproxy-haptic.org/waf-policy: approved-policy } }
          spec:
            ingressClassName: haptic
            rules:
              - host: trusted.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - { type: contains, target: events, pattern: "Warning WafPolicyCatalogInvalid v1 ConfigMap team-a/waf-policies:.*invalid YAML" }
      - { type: contains, target: events, pattern: "Warning WafPolicyUnavailable networking.k8s.io/v1 Ingress team-a/app:.*self-service catalog is broken" }
      - { type: contains, target: haproxy.cfg, pattern: "http-request deny deny_status 503 if \\{ var\\(txn.resource_id\\) -m str team-a/app \\}" }
      - { type: not_contains, target: "map:coraza-app.map", pattern: "team-a/app" }
      - { type: contains, target: "map:coraza-app.map", pattern: "default/trusted-user policy:approved-policy" }

  test-haptic-waf-selfservice-broken-catalog-admission-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: under the admission webhook, a broken self-service catalog is rejected loudly so a proposed change cannot introduce it
    extraContext:
      renderMode: admission
      waf: *wafPoliciesRoot
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              this: [is: not, valid yaml
    assertions:
      - { type: contains, target: rendering_error, pattern: "self-service WAF ConfigMap team-a/waf-policies key policies.yaml is invalid YAML" }

  test-haptic-waf-selfservice-seclang-denied-by-default:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: self-service policies may not carry secLang unless allowSecLang=true — the policy is scoped-broken (event + 503 for selectors), not silently loaded
    extraContext:
      renderMode: reconcile
      waf: *wafPoliciesRoot
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              rule-smuggler:
                requestBody:
                  mode: none
                secLang: |
                  SecRuleEngine Off
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: rule-smuggler } }
          spec: *specRulesHost
    assertions:
      - type: haproxy_valid
      - { type: contains, target: events, pattern: "Warning WafPolicyInvalid v1 ConfigMap team-a/waf-policies:.*uses secLang, which is disabled for self-service catalogs" }
      - { type: contains, target: haproxy.cfg, pattern: "http-request deny deny_status 503 if \\{ var\\(txn.resource_id\\) -m str team-a/app \\}" }
      - { type: not_contains, target: "file:spoa-hub-config.toml", pattern: "SecRuleEngine Off" }

  test-haptic-waf-selfservice-seclang-allowed-when-opted-in:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: with allowSecLang=true a self-service policy may carry bounded secLang
    extraContext:
      waf:
        policies:
          defaultPolicy: ""
          configMapRefs: { __replace__: true }
          selfService: {enabled: true, allowSecLang: true}
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              tuned:
                requestBody:
                  mode: none
                secLang: |
                  SecRuleRemoveById 941320
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: tuned } }
          spec: *specRulesHost
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-app.map", pattern: "team-a/app policy:team-a/tuned" }
      - { type: contains, target: "file:spoa-hub-config.toml", pattern: "SecRuleRemoveById 941320" }

  test-haptic-waf-selfservice-trusted-collision-never-silent:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: >-
      A self-service policy whose name collides with a trusted-catalog policy
      is never resolved silently in either direction: the colliding namespace's
      selector fails closed with an event, while another namespace selecting
      the same name still gets the trusted policy.
    extraContext:
      renderMode: reconcile
      waf:
        policies:
          defaultPolicy: ""
          configMapRefs: { __replace__: true }
          inline: {shared-name: {requestBody: {mode: none}}}
          selfService: {enabled: true}
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              shared-name:
                enforcement: detect
                requestBody:
                  mode: none
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: shared-name } }
          spec: *specRulesHost
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: default, name: other, annotations: { haproxy-haptic.org/waf-policy: shared-name } }
          spec:
            ingressClassName: haptic
            rules:
              - host: other.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - { type: contains, target: events, pattern: "Warning WafPolicyInvalid v1 ConfigMap team-a/waf-policies:.*collides with the trusted-catalog policy" }
      - { type: contains, target: haproxy.cfg, pattern: "http-request deny deny_status 503 if \\{ var\\(txn.resource_id\\) -m str team-a/app \\}" }
      - { type: contains, target: "map:coraza-app.map", pattern: "default/other policy:shared-name" }
      - { type: not_contains, target: "map:coraza-app.map", pattern: "team-a/app" }

  test-haptic-waf-selfservice-per-namespace-budget:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: the per-namespace policy budget cuts deterministically (sorted names) — policies beyond the limit are scoped-broken, the rest stay usable
    extraContext:
      renderMode: reconcile
      waf:
        policies:
          defaultPolicy: ""
          configMapRefs: { __replace__: true }
          selfService: {enabled: true, limits: {maxPoliciesPerNamespace: 2}}
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              p-alpha:
                requestBody:
                  mode: none
              p-beta:
                requestBody:
                  mode: none
              p-gamma:
                requestBody:
                  mode: none
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: first, annotations: { haproxy-haptic.org/waf-policy: p-alpha } }
          spec:
            ingressClassName: haptic
            rules:
              - host: first.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: over, annotations: { haproxy-haptic.org/waf-policy: p-gamma } }
          spec:
            ingressClassName: haptic
            rules:
              - host: over.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: app, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-app.map", pattern: "team-a/first policy:team-a/p-alpha" }
      - { type: contains, target: events, pattern: "Warning WafPolicyInvalid v1 ConfigMap team-a/waf-policies:.*more than the per-namespace self-service limit 2.*'p-gamma'" }
      - { type: contains, target: haproxy.cfg, pattern: "http-request deny deny_status 503 if \\{ var\\(txn.resource_id\\) -m str team-a/over \\}" }

  test-haptic-waf-selfservice-default-on-weakening-rejected:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: under default-on deny dispatch, a tenant-authored detect policy may not weaken the baseline — rejected at admission
    extraContext:
      admissionSubject: { store: ingresses, namespace: team-a, name: app }
      renderMode: admission
      waf:
        dispatch: {mode: default-on, defaultEnforcement: deny}
        policies: {defaultPolicy: "", configMapRefs: { __replace__: true }, selfService: {enabled: true}}
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              softener:
                enforcement: detect
                requestBody:
                  mode: none
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: softener } }
          spec: *specRulesHost
    assertions:
      - { type: contains, target: rendering_error, pattern: "would weaken the default-on baseline enforcement 'deny'" }

  test-haptic-waf-selfservice-disabled-stays-unknown:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: with selfService disabled — pinned per-test, so the test also holds on deployments that enable it — a namespace waf-policies ConfigMap stays inert and its policy is not resolvable (exact message depends on whether the deployment carries a trusted catalog, which per-test extraContext cannot fully remove)
    extraContext:
      admissionSubject: { store: ingresses, namespace: team-a, name: app }
      renderMode: admission
      waf:
        policies: {defaultPolicy: "", configMapRefs: { __replace__: true }, selfService: {enabled: false}}
    fixtures:
      waf_selfservice_catalogs: *wafSelfserviceCatalogs
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: app-baseline } }
          spec: *specRulesHost
    assertions:
      - { type: contains, target: rendering_error, pattern: "(no reusable WAF policy catalog is configured|Unknown WAF policy 'app-baseline')" }

  test-haptic-waf-selfservice-broken-policy-does-not-consume-budget:
    _helm_skip_test: '{{ if or (include "haptic.spoaHub.disabled" .) (not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.coraza "root" . "name" "coraza"))) }}true{{ end }}'
    description: a broken self-service policy (denied secLang, sorted first) does not consume a per-namespace budget slot — the namespace's valid policy still loads within limit 1
    extraContext:
      renderMode: reconcile
      waf:
        policies:
          defaultPolicy: ""
          configMapRefs: { __replace__: true }
          selfService: {enabled: true, limits: {maxPoliciesPerNamespace: 1}}
    fixtures:
      waf_selfservice_catalogs:
        - apiVersion: v1
          kind: ConfigMap
          metadata: { namespace: team-a, name: waf-policies }
          data:
            policies.yaml: |
              a-broken:
                requestBody:
                  mode: none
                secLang: |
                  SecRuleEngine Off
              b-valid:
                requestBody:
                  mode: none
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: { namespace: team-a, name: app, annotations: { haproxy-haptic.org/waf-policy: b-valid } }
          spec: *specRulesHost
    assertions:
      - type: haproxy_valid
      - { type: contains, target: "map:coraza-app.map", pattern: "team-a/app policy:team-a/b-valid" }
      - { type: contains, target: events, pattern: "Warning WafPolicyInvalid v1 ConfigMap team-a/waf-policies:.*'a-broken'.*uses secLang" }
      - { type: not_contains, target: haproxy.cfg, pattern: "more than the per-namespace self-service limit" }
