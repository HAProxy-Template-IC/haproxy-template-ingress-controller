# Chart-time loading rules (consumed by haptic.prepareLibraries; stripped from output).
_fx:
  specTestExampleComRoot: &specTestExampleComRoot
    rules:
      - host: test.example.com
        http:
          paths:
            - path: /
              pathType: Prefix
              backend: {service: {name: echo, port: {number: 80}}}
  statusReady: &statusReady
    phase: Running
    conditions:
      - {type: Ready, status: "True"}

_helm_load: {enable: '{{ .Values.controller.templateLibraries.haproxytech.enabled }}'}

watchedResources:
  secrets:
    apiVersion: v1
    resources: secrets
    indexBy: ["metadata.namespace", "metadata.name"]

templateSnippets:
  features-099-haproxytech-publications:
    template: |-
      {{- render "haproxytech-forwarded-for-publications" -}}
      {{- render "haproxytech-logging-publications" -}}
      {{- render "haproxytech-ssl-redirect-port-publications" -}}
      {{- render "haproxytech-captured-headers-publications" -}}

  frontend-filters-100-haproxytech-basic-headers:
    template: |
      {%- if incremental_value_count("haproxytech-forwarded-for", "enabled") > 0 %}
      # haproxytech/frontend-filters-haproxytech-basic-headers (forwarded-for)
      option forwardfor
      {%- end -%}

  haproxytech-forwarded-for-publications:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/forwarded-for']"]
      group: haproxytech-forwarded-for
      effects: [publishValue]
    template: |-
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
        {%- var value = ingress.Metadata.Annotations["haproxy.org/forwarded-for"] -%}
        {%- if value != "" && value != "true" && value != "false" -%}
          {{- fail("Invalid value '" + value + "' for annotation 'haproxy.org/forwarded-for' on Ingress '" + key + "'. Valid values: 'true', 'false'") -}}
        {%- end -%}
        {%- if value == "true" -%}{{- shared.Publish("enabled", "global", true) -}}{%- end -%}
      {%- end -%}

  frontend-filters-200-haproxytech-access-control:
    template: |-
      {%- import "util-access-control-lane" for AccessControlLane -%}
      {{- AccessControlLane("haproxytech") -}}

  ingress-access-control-0200-haproxytech:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists:
        - "metadata.annotations['haproxy.org/allow-list']"
        - "metadata.annotations['haproxy.org/whitelist']"
        - "metadata.annotations['haproxy.org/deny-list']"
        - "metadata.annotations['haproxy.org/blacklist']"
      group: ingress-access-control
      effects: [publishValue]
    template: |-
      {#- Rows for the shared map lane (util-access-control-lane), or the legacy
          per-route fragment for a list with an IPv6 entry. whitelist/blacklist
          are the deprecated aliases, ignored when the canonical key is set. -#}
      {%- import "util-publish-access-control" for PublishAccessControl -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var anns = ingress.Metadata.Annotations -%}
        {%- var allowKey = "haproxy.org/allow-list" -%}
        {%- if anns["haproxy.org/allow-list"] == "" && anns["haproxy.org/whitelist"] != "" -%}
          {%- allowKey = "haproxy.org/whitelist" -%}
        {%- end -%}
        {%- var denyKey = "haproxy.org/deny-list" -%}
        {%- if anns["haproxy.org/deny-list"] == "" && anns["haproxy.org/blacklist"] != "" -%}
          {%- denyKey = "haproxy.org/blacklist" -%}
        {%- end -%}
        {%- var fragment = PublishAccessControl(ingress, allowKey, denyKey, "haproxytech", "haproxytech/frontend-filters-haproxytech-access-control (allow-list for {KEY})", "haproxytech/frontend-filters-haproxytech-access-control (deny-list for {KEY})") -%}
        {%- if fragment != "" -%}{{- shared.Publish("fragments:haproxytech", ingress.Metadata.Namespace + "/" + ingress.Metadata.Name, fragment) -}}{%- end -%}
      {%- end -%}

  frontend-filters-300-haproxytech-cors:
    requires: [ingresses]
    template: |-
      {%- import "util-cors-lane" for CorsLane -%}
      {{- CorsLane("haproxytech") -}}

  # Mirrors upstream haproxytech/kubernetes-ingress pkg/annotations/ingress/resSetCORS.go:
  # cors-allow-origin is one regex searched in the request Origin ("*" allows
  # any), the preflight 204 is opt-in and unconditional, and there is no Vary.
  ingress-cors-0300-haproxytech:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/cors-enable']"]
      group: ingress-cors
      effects: [publishValue, recordEvent]
    template: |-
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-cors-publish" for PublishCORS -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil && ingress.Metadata.Annotations["haproxy.org/cors-enable"] == "true" -%}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
        {%- var origin = ingress.Metadata.Annotations["haproxy.org/cors-allow-origin"] -%}
        {%- if origin == "" -%}{%- origin = "*" -%}{%- end -%}
        {%- var methods = ingress.Metadata.Annotations["haproxy.org/cors-allow-methods"] -%}
        {%- if methods == "" -%}{%- methods = "*" -%}{%- end -%}
        {%- var headers = ingress.Metadata.Annotations["haproxy.org/cors-allow-headers"] -%}
        {%- if headers == "" -%}{%- headers = "*" -%}{%- end -%}
        {%- var credentials = ingress.Metadata.Annotations["haproxy.org/cors-allow-credentials"] == "true" -%}
        {%- var maxAge = ingress.Metadata.Annotations["haproxy.org/cors-max-age"] -%}
        {%- if maxAge == "" -%}{%- maxAge = "5" -%}{%- end -%}
        {%- var preflight = 0 -%}
        {%- if ingress.Metadata.Annotations["haproxy.org/cors-respond-to-options"] == "true" -%}{%- preflight = 2 -%}{%- end -%}
        {%- var corsInj = "" -%}
        {%- if origin != "*" -%}{%- corsInj = ConfigInjectionKind(origin, "squote") -%}{%- end -%}
        {%- if corsInj == "" -%}{%- corsInj = ConfigInjectionKind(methods, "line") -%}{%- end -%}
        {%- if corsInj == "" -%}{%- corsInj = ConfigInjectionKind(headers, "line") -%}{%- end -%}
        {%- if corsInj == "" -%}{%- corsInj = ConfigInjectionKind(maxAge, "line") -%}{%- end -%}
        {%- if corsInj != "" -%}
          {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", "A CORS annotation on Ingress '" + key + "' contains " + corsInj + ", which would split the frontend directive; CORS is not applied. Remove control characters from the value.") -}}
        {%- else if credentials && origin == "*" -%}
          {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "CORS configuration error on Ingress '" + key + "': cors-allow-credentials cannot be 'true' when cors-allow-origin is '*'. Specify an explicit origin.") -}}
        {%- else if strings_contains(origin, " ") -%}
          {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "CORS configuration error on Ingress '" + key + "': cors-allow-origin must not contain spaces; the origin regex is one map-file token.") -}}
        {%- else -%}
          {%- var regexes = []string{} -%}
          {%- if origin != "*" -%}
            {#- The regex is searched in the Origin (unanchored -m reg parity); a
                leading ^ keeps its meaning at the start of the Origin. -#}
            {%- if hasPrefix(origin, "^") -%}
              {%- regexes = append(regexes, "(" + origin[1:] + ")") -%}
            {%- else -%}
              {%- regexes = append(regexes, ".*(" + origin + ")") -%}
            {%- end -%}
          {%- end -%}
          {{- PublishCORS(key, "haproxytech", origin == "*", []string{}, regexes, methods, headers, maxAge, credentials, "", false, preflight) -}}
        {%- end -%}
      {%- end -%}

  features-100-haproxytech-ssl-redirect:
    template: |
      {# gf["sslRedirectHosts"] is initialised by ssl.yaml's features-050-ssl-initialization
          and consumed by features-160-ssl-redirect-map. #}

      {%- var gf = shared.Get("globalFeatures").(map[string]any) -%}
      {{- render "haproxytech-ssl-redirect-publications" -}}
      {%- for _, entry := range incremental_values("haproxytech-ssl-redirect-hosts", "hosts") -%}
        {%- var redirectHosts []any = gf["sslRedirectHosts"] -%}
        {%- gf["sslRedirectHosts"] = append(redirectHosts, entry) -%}
      {%- end -%}

  haproxytech-ssl-redirect-publications:
    requires: [ingresses]
    incremental:
      bindingsTemplate: '{{- toJSON(map[string]any{"ingresses": map[string]any{"sslRedirectDefault": extraContext | dig_string("", "ssl_redirect_default")}}) -}}'
      group: haproxytech-ssl-redirect-hosts
      effects: [publishValue]
    template: |-
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var sslRedirect = ingress.Metadata.Annotations["haproxy.org/ssl-redirect"] -%}
        {%- var shouldRedirect = sslRedirect == "true" || (sslRedirect == "" && tostring(props["sslRedirectDefault"]) == "true") -%}
        {%- if shouldRedirect && ingress.Metadata.Annotations["haproxy.org/ssl-redirect-port"] == "" -%}
          {%- var code = ingress.Metadata.Annotations["haproxy.org/ssl-redirect-code"] -%}
          {%- if code == "" -%}{%- code = "302" -%}{%- end -%}
          {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
          {%- for ruleIndex, rule := range ingress.Spec.Rules -%}
            {%- if rule.Host != "" -%}
              {{- shared.Publish("hosts", key + "/" + tostring(ruleIndex), map[string]any{"host": toLower(rule.Host), "code": code}) -}}
            {%- end -%}
          {%- end -%}
        {%- end -%}
      {%- end -%}

  frontend-filters-055-haproxytech-ssl-redirect-port:
    template: |
      {# hdr(host),field(1,:) strips any port already in the Host header. The port comes from
          ssl-redirect-port-<code>.map, so changing it is a reload-free map update.
          features-100 keeps these hosts out of ssl.yaml's scheme-redirect map (no double redirect). #}
      {%- var portByCode = map[string]map[string]string{} -%}
      {%- var codeHosts = map[string][]any{} -%}
      {%- var seen = map[string]bool{} -%}
      {%- for _, entryAny := range incremental_values("haproxytech-ssl-redirect-port-hosts", "codes") %}
        {%- var entry = entryAny.(map[string]any) %}
        {%- var code = tostring(entry["code"]) %}
        {%- if portByCode[code] == nil %}{%- portByCode[code] = map[string]string{} %}{%- end %}
      {%- end %}
      {%- for _, entryAny := range incremental_values("haproxytech-ssl-redirect-port-hosts", "hosts") %}
        {%- var entry = entryAny.(map[string]any) %}
        {%- var code = tostring(entry["code"]) %}
        {%- var host = tostring(entry["host"]) %}
        {%- if host != "" && !seen[host] %}
          {%- seen[host] = true %}
          {%- if portByCode[code] == nil %}{%- portByCode[code] = map[string]string{} %}{%- end %}
          {%- portByCode[code][host] = tostring(entry["port"]) %}
          {%- codeHosts[code] = append(coalesce(codeHosts[code], []any{}).([]any), host) %}
        {%- end %}
      {%- end %}
      {%- for code, hostPort := range portByCode %}
        {%- var sortedHosts = sort_strings(coalesce(codeHosts[code], []any{})) %}
        {%- var lines = []string{} %}
        {%- for _, h := range sortedHosts %}{%- lines = append(lines, tostring(h) + " " + hostPort[tostring(h)]) %}{%- end %}
        {%- var mapContent = join(lines, "\n") + "\n" %}
        {%- var _, mapErr = fileRegistry.Register("map", "ssl-redirect-port-" + code + ".map", mapContent) %}
        {%- if mapErr != nil %}{{ fail("failed to register ssl-redirect-port map: " + tostring(mapErr)) }}{%- end %}
        {%- var m, _ = pathResolver.GetPath("ssl-redirect-port-" + code + ".map", "map") %}
      # haproxytech/ssl-redirect-port (code {{ code }})
      http-request redirect location https://%[hdr(host),field(1,:)]:%[var(txn.host),map({{ m }})]%[capture.req.uri] code {{ code }} if !{ ssl_fc } { var(txn.host),map({{ m }}) -m found }
      {%- end %}

  haproxytech-ssl-redirect-port-publications:
    requires: [ingresses]
    incremental:
      bindingsTemplate: '{{- toJSON(map[string]any{"ingresses": map[string]any{"sslRedirectDefault": extraContext | dig_string("", "ssl_redirect_default")}}) -}}'
      whenAnyPathExists: ["metadata.annotations['haproxy.org/ssl-redirect-port']"]
      group: haproxytech-ssl-redirect-port-hosts
      effects: [publishValue, recordEvent]
    template: |-
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var redirectPort = ingress.Metadata.Annotations["haproxy.org/ssl-redirect-port"] -%}
        {%- if redirectPort != "" -%}
          {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
          {%- if toint(redirectPort) <= 0 -%}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "Invalid value '" + redirectPort + "' for annotation 'haproxy.org/ssl-redirect-port' on Ingress '" + key + "'. Must be a positive integer port.") -}}
          {%- else -%}
            {%- var sslRedirect = ingress.Metadata.Annotations["haproxy.org/ssl-redirect"] -%}
            {%- var shouldRedirect = sslRedirect == "true" || (sslRedirect == "" && tostring(props["sslRedirectDefault"]) == "true") -%}
            {%- if shouldRedirect -%}
              {%- var code = ingress.Metadata.Annotations["haproxy.org/ssl-redirect-code"] -%}
              {%- if code == "" -%}{%- code = "302" -%}{%- end -%}
              {{- shared.Publish("codes", key, map[string]any{"code": code}) -}}
              {%- for ruleIndex, rule := range ingress.Spec.Rules -%}
                {%- if rule.Host != "" -%}
                  {{- shared.Publish("hosts", key + "/" + tostring(ruleIndex), map[string]any{
                    "host": toLower(rule.Host), "code": code, "port": redirectPort,
                  }) -}}
                {%- end -%}
              {%- end -%}
            {%- end -%}
          {%- end -%}
        {%- end -%}
      {%- end -%}

  features-130-haproxytech-request-redirect:
    template: |
      {# base.yaml's frontend-filters-060-redirect-location emits the rule from redirect-loc-<code>.map,
          so a redirect change is a reload-free map update. No first_seen gate — features-170 dedups hosts. #}
      {%- var gf = shared.Get("globalFeatures").(map[string]any) -%}
      {{- render "haproxytech-request-redirect-publications" -}}
      {%- for _, entry := range incremental_values("haproxytech-request-redirect-hosts", "hosts") -%}
        {%- var rh []any = coalesce(gf["redirectHosts"], []any{}) -%}
        {%- gf["redirectHosts"] = append(rh, entry) -%}
      {%- end -%}

  haproxytech-request-redirect-publications:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/request-redirect']"]
      group: haproxytech-request-redirect-hosts
      effects: [publishValue, recordEvent]
    template: |-
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var target = ingress.Metadata.Annotations["haproxy.org/request-redirect"] -%}
        {%- if target != "" -%}
          {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
          {%- if regex_search(target, "[[:cntrl:]]") -%}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", "Invalid value for annotation 'haproxy.org/request-redirect' on Ingress '" + key + "': must not contain control characters — config-injection guard.") -}}
          {%- else -%}
            {%- var code = ingress.Metadata.Annotations["haproxy.org/request-redirect-code"] -%}
            {%- if code == "" -%}{%- code = "302" -%}{%- end -%}
            {%- var validCodes = []string{"301", "302", "303", "307", "308"} -%}
            {%- var codeValid = false -%}
            {%- for _, validCode := range validCodes -%}{%- if code == validCode -%}{%- codeValid = true -%}{%- end -%}{%- end -%}
            {%- if !codeValid -%}
              {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "Invalid value '" + code + "' for annotation 'haproxy.org/request-redirect-code' on Ingress '" + key + "'. Valid values: 301, 302, 303, 307, 308") -}}
            {%- else -%}
              {%- for ruleIndex, rule := range ingress.Spec.Rules -%}
                {%- if rule.Host != "" -%}
                  {{- shared.Publish("hosts", key + "/" + tostring(ruleIndex), map[string]any{
                    "host": toLower(rule.Host), "location": target, "code": code,
                  }) -}}
                {%- end -%}
              {%- end -%}
            {%- end -%}
          {%- end -%}
        {%- end -%}
      {%- end -%}

  frontend-filters-500-haproxytech-logging:
    requires: [ingresses]
    template: |-
      {%- import "util-src-ip-lane" for SrcIPHeaderLane -%}
      {{- SrcIPHeaderLane("haproxytech") -}}
      {%- if incremental_value_count("haproxytech-logging", "any") > 0 -%}
      {%%
        var pairs = []any{}
        for _, v := range incremental_values("haproxytech-logging", "pair") {
          pairs = append(pairs, tostring(v))
        }
      %%}
      {{ "\n" }}# haproxytech/request-capture
      {%- for _, p := range sort_strings(pairs) %}
      capture request header {{ p }}
      {%- end %}
      {%- end %}

  # src-ip-header from the shared map lane; a bad value must fail closed, not
  # skip: emitting nothing would leave downstream trust ACLs judging the
  # proxy's address. The header name is a bare token.
  ingress-src-ip-0500-haproxytech:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/src-ip-header']"]
      group: ingress-src-ip
      effects: [publishValue]
    template: |-
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-src-ip-publish" for PublishSrcIPHeader -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil && ingress.Metadata.Annotations["haproxy.org/src-ip-header"] != "" -%}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
        {%- var srcIpHeader = ingress.Metadata.Annotations["haproxy.org/src-ip-header"] -%}
        {%- var srcInj = ConfigInjectionKind(srcIpHeader, "token") -%}
        {%- if srcInj != "" -%}
          {{- fail("Invalid value for annotation 'haproxy.org/src-ip-header' on Ingress '" + key + "': contains " + srcInj + ", which would break the set-src directive. Use a plain header name.") -}}
        {%- end -%}
        {{- PublishSrcIPHeader(key, srcIpHeader, "haproxytech") -}}
      {%- end -%}

  haproxytech-logging-publications:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/request-capture']"]
      group: haproxytech-logging
      effects: [publishValue, recordEvent]
    template: |-
      {#- Capture slots are frontend-wide and positional: one (name, len) pair
          is published per header and the block emits each distinct pair once. -#}
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil && ingress.Metadata.Annotations["haproxy.org/request-capture"] != "" -%}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
        {%- var captureLen = ingress.Metadata.Annotations["haproxy.org/request-capture-len"] -%}
        {%- if captureLen == "" -%}{%- captureLen = "128" -%}{%- end -%}
        {#- captureLen is a bare positive integer; toint() rejects any trailing
            garbage (a newline, a second word) by returning 0, so this one check
            is both the sanity gate and the injection guard for the len operand. -#}
        {%- if toint(captureLen) <= 0 -%}
          {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "Invalid value '" + captureLen + "' for annotation 'haproxy.org/request-capture-len' on Ingress '" + key + "'. Must be a positive integer; request capture is skipped.") -}}
        {%- else -%}
          {%- for _, line := range split(ingress.Metadata.Annotations["haproxy.org/request-capture"], "\n") -%}
            {%- var t = trimSpace(line) -%}
            {%- if t == "" -%}{%- continue -%}{%- end -%}
            {%- var capInj = ConfigInjectionKind(t, "token") -%}
            {%- if capInj != "" -%}
              {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", "Annotation 'haproxy.org/request-capture' on Ingress '" + key + "' names a header containing " + capInj + ", which would break the capture directive; that entry is skipped.") -}}
            {%- else -%}
              {{- shared.Publish("any", "any", "any") -}}
              {{- shared.Publish("pair", t + " len " + captureLen, t + " len " + captureLen) -}}
            {%- end -%}
          {%- end -%}
        {%- end -%}
      {%- end -%}

  global-top-500-haproxytech-ingress-auth:
    template: |
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {{- render "haproxytech-auth-userlist-publications" -}}
      {%- for _, entryAny := range incremental_values("haproxytech-auth-userlists", "userlists") %}
        {%- var entry = entryAny.(map[string]any) %}
        {%- var key = tostring(entry["key"]) %}
        {%- var secretNs = tostring(entry["secretNamespace"]) %}
        {%- var secretName = tostring(entry["secretName"]) %}
        {%- var userlistName = tostring(entry["userlistName"]) %}
      # haproxytech/global-top-haproxytech-ingress-auth
      userlist {{ userlistName }}
        {%- var hashRegex = extraContext | dig_string("^.*$", "annotationCompatibility", "basicAuth", "passwordHashValidation", "regex") %}
        {%- var hashErrMsg = extraContext | dig_string("Invalid password hash", "annotationCompatibility", "basicAuth", "passwordHashValidation", "errorMessage") %}
        {%- for _, userAny := range toSlice(entry["users"]) %}
          {%- var user = userAny.(map[string]any) %}
          {%- var username = tostring(user["username"]) %}
          {%- var passwordHash = tostring(user["passwordHash"]) %}
          {%- var pwInj = ConfigInjectionKind(passwordHash, "token") %}
          {%- if !regex_search(passwordHash, hashRegex) %}
            {{- fail(hashErrMsg + " for user '" + username + "' in Secret '" + secretNs + "/" + secretName + "' (pattern \"" + hashRegex + "\")") -}}
          {%- else if pwInj != "" %}
            {{- fail("Basic-auth hash for user '" + username + "' in Secret '" + secretNs + "/" + secretName + "' referenced on Ingress '" + key + "' contains " + pwInj + ", which would break out of the userlist line. Remove whitespace, quotes and control characters from the hash.") -}}
          {%- end %}
        user {{ username }} password {{ passwordHash }}
        {% end -%}
      {%- end -%}

  haproxytech-auth-userlist-publications:
    requires: [ingresses, secrets]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/auth-type']"]
      group: haproxytech-auth-userlists
      effects: [publishValue]
    template: |-
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var ns = ingress.Metadata.Namespace -%}
        {%- var key = ns + "/" + ingress.Metadata.Name -%}
        {%- var authType = ingress.Metadata.Annotations["haproxy.org/auth-type"] -%}
        {%- if authType != "" && authType != "basic-auth" -%}
          {{- fail("Invalid value '" + authType + "' for annotation 'haproxy.org/auth-type' on Ingress '" + key + "'. Valid values: 'basic-auth'") -}}
        {%- end -%}
        {%- if authType == "basic-auth" -%}
          {%- var authSecret = ingress.Metadata.Annotations["haproxy.org/auth-secret"] -%}
          {%- if authSecret != "" -%}
            {%- var secretNs = ns -%}
            {%- var secretName = authSecret -%}
            {%- if strings_contains(authSecret, "/") -%}
              {%- var parts = split(authSecret, "/") -%}
              {%- secretNs = parts[0] -%}
              {%- secretName = parts[1] -%}
            {%- end -%}
            {%- var secret = resources.secrets.GetSingle(secretNs, secretName) -%}
            {%- if secret != nil -%}
              {%- var users = []any{} -%}
              {%- for _, username := range keys(secret.Data) -%}
                {%- users = append(users, map[string]any{
                  "username": username,
                  "passwordHash": secret.Data[username] | b64decode() | tostring(),
                }) -%}
              {%- end -%}
              {%- var userlistName = "auth_" + secretNs + "_" + secretName -%}
              {{- shared.PublishRanked("userlists", userlistName, key, map[string]any{
                "key": key,
                "secretNamespace": secretNs,
                "secretName": secretName,
                "userlistName": userlistName,
                "users": users,
              }) -}}
            {%- end -%}
          {%- end -%}
        {%- end -%}
      {%- end -%}

  backend-directives-100-haproxytech-pod-maxconn:
    template: |
      {%- if ingress != nil %}
        {%- var ns = ingress.Metadata.Namespace %}
        {%- var name = ingress.Metadata.Name %}
        {%- var key = ns + "/" + name %}
        {%- var podMaxconn = ingress.Metadata.Annotations["haproxy.org/pod-maxconn"] %}
        {%- if podMaxconn != "" %}
          {%- var val = toint(podMaxconn) %}
          {%- if val <= 0 %}
            {{- fail("Invalid value '" + podMaxconn + "' for annotation 'haproxy.org/pod-maxconn' on Ingress '" + key + "'. Must be a positive integer.") -}}
          {%- end %}
          {%- var haproxyPodCount = 0 %}
          {%- if controller != nil %}
            {%- var pods = controller.haproxy_pods %}
            {%- if pods != nil %}
              {%- for _, pod := range pods.List() %}
                {%- if (pod | dig("status", "phase") | fallback("")) == "Running" %}
                  {%- for _, cond := range (pod | dig("status", "conditions") | fallback([]any{}) | toSlice()) %}
                    {%- if (cond | dig("type") | fallback("")) == "Ready" && (cond | dig("status") | fallback("")) == "True" %}
                      {%- haproxyPodCount = haproxyPodCount + 1 %}
                    {%- end %}
                  {%- end %}
                {%- end %}
              {%- end %}
            {%- end %}
          {%- end %}
          {%- if haproxyPodCount > 0 %}
            {#- Power of 2, not the raw pod count: otherwise every scale event rewrites maxconn and reloads. -#}
            {%- var effectiveCount = 1 %}
            {%- for effectiveCount < haproxyPodCount %}
              {%- effectiveCount = effectiveCount * 2 %}
            {%- end %}
            {%- var perPod = ((val | float64()) / (effectiveCount | float64())) | ceil() %}
      # haproxytech/backend-directives-haproxytech-pod-maxconn
      # pod-maxconn: {{ podMaxconn }} total / {{ haproxyPodCount }} ready pods (effective: {{ effectiveCount }}) = {{ perPod }} per pod
            {% serverOpts["podMaxconnValue"] = perPod -%}
          {%- else %}
      # haproxytech/backend-directives-haproxytech-pod-maxconn
      # pod-maxconn: {{ podMaxconn }} (no ready HAProxy pods discovered yet, using full value)
            {% serverOpts["podMaxconnValue"] = val -%}
          {%- end %}
        {%- end %}
      {%- end -%}

  backend-directives-100-haproxytech-timeouts:
    template: |
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- if ingress != nil %}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name %}
        {%- var emitted = false %}
        {#- timeout-server and timeout-tunnel are the settable timeouts: they move to
            backend-timeouts.map via map-backend-timeout-100-haproxytech-timeouts
            (reload-free). connect/queue/check stay per-value here. -#}
        {%- for _, t := range [][]string{ {"haproxy.org/timeout-connect", "connect"}, {"haproxy.org/timeout-queue", "queue"}, {"haproxy.org/timeout-check", "check"} } %}
          {%- var ann = t[0] %}
          {%- var n = t[1] %}
          {%- var v = ingress.Metadata.Annotations[ann] %}
          {%- if v != "" %}
            {%- var inj = ConfigInjectionKind(v, "token") %}
            {%- if inj != "" %}
              {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "Invalid value for annotation '" + ann + "' on Ingress '" + key + "': contains " + inj + ", which would split the backend directive; the timeout is skipped.") -}}
            {%- else %}
              {%- if !emitted %}
      # haproxytech/backend-directives-haproxytech-timeouts
                {%- emitted = true %}
              {%- end %}
      timeout {{ n }} {{ v }}
            {%- end %}
          {%- end %}
        {%- end %}
      {%- end -%}

  # Reload-free settable timeouts: timeout-server / timeout-tunnel become
  # `<backend>|server` / `<backend>|tunnel` entries in backend-timeouts.map.
  map-backend-timeout-100-haproxytech-timeouts:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists:
        - "metadata.annotations['haproxy.org/timeout-server']"
        - "metadata.annotations['haproxy.org/timeout-tunnel']"
      group: map-backend-timeout-ingress
      effects: [recordEvent]
    template: |
      {%- import "util-ingress-backend-timeout" for IngressBackendTimeoutMapEntries -%}
      {{- IngressBackendTimeoutMapEntries(item, "haproxy.org/timeout-server", "haproxy.org/timeout-tunnel") -}}

  backend-directives-150-haproxytech-load-balance:
    template: |
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- if ingress != nil %}
        {%- var ns = ingress.Metadata.Namespace %}
        {%- var name = ingress.Metadata.Name %}
        {%- var key = ns + "/" + name %}
        {%- var lb = ingress.Metadata.Annotations["haproxy.org/load-balance"] %}
        {%- if lb != "" %}
          {%- var exactValid = []string{"roundrobin", "static-rr", "leastconn", "first", "source", "random"} %}
          {%- var prefixValid = []string{"uri", "url_param", "hdr", "rdp-cookie"} %}
          {%- var isValid = false %}
          {%- for _, v := range exactValid %}{%- if lb == v %}{%- isValid = true %}{%- end %}{%- end %}
          {%- if !isValid %}
            {%- for _, v := range prefixValid %}
              {%- if lb | hasPrefix(v) %}{%- isValid = true %}{%- end %}
            {%- end %}
          {%- end %}
          {%- if !isValid %}
            {{- fail("Invalid value '" + lb + "' for annotation 'haproxy.org/load-balance' on Ingress '" + key + "'. Valid algorithms: " + (exactValid | join(", ")) + ", or parameterized: " + (prefixValid | join("(...), ")) + "(...)") -}}
          {%- end %}
          {#- The parameterized forms accept an unbounded suffix (hdr(<v>), url_param(<v>)),
              so a control char or space there would split the backend directive; the
              exact algorithms can't carry one. -#}
          {%- var lbInj = ConfigInjectionKind(lb, "token") %}
          {%- if lbInj != "" %}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "Invalid value for annotation 'haproxy.org/load-balance' on Ingress '" + key + "': contains " + lbInj + ", which would split the backend directive; load-balancing is left at the default.") -}}
          {%- else %}
          {#- Structured Backend() arg (profile), not a body line, so the macro
              adds `hash-type consistent` for a hash-family algorithm. -#}
          {%- serverOpts["balance"] = lb %}
          {%- end %}
        {%- end %}
      {%- end -%}

  backend-directives-200-haproxytech-health-checks:
    template: |
      {%- if ingress != nil %}
        {%- var ns = ingress.Metadata.Namespace %}
        {%- var name = ingress.Metadata.Name %}
        {%- var key = ns + "/" + name %}
        {%- var check = ingress.Metadata.Annotations["haproxy.org/check"] %}
        {%- if check != "" %}
          {%- if check != "true" && check != "false" %}
            {{- fail("Invalid value '" + check + "' for annotation 'haproxy.org/check' on Ingress '" + key + "'. Valid values: 'true', 'false'") -}}
          {%- end %}
          {#- ingress.yaml emits `default-server <checkDirective>`, defaulting to "check". -#}
          {%- if check == "false" %}
            {%- serverOpts["checkDirective"] = "no-check" %}
          {%- end %}
        {%- end %}
      {%- end -%}

  backend-directives-210-haproxytech-advanced-health-checks:
    template: |
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- if ingress != nil %}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name %}
        {%- var checkHttp = ingress.Metadata.Annotations["haproxy.org/check-http"] %}
        {%- var checkInterval = ingress.Metadata.Annotations["haproxy.org/check-interval"] %}
        {%- if checkHttp != "" %}
          {#- httpchk legitimately holds spaces (method + uri + version), so only
              control chars are rejected here. -#}
          {%- var httpInj = ConfigInjectionKind(checkHttp, "line") %}
          {%- if httpInj != "" %}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", "Invalid value for annotation 'haproxy.org/check-http' on Ingress '" + key + "': contains " + httpInj + ", which would split the backend directive; the HTTP health check is skipped.") -}}
          {%- else %}
      # haproxytech/backend-directives-haproxytech-advanced-health-checks
            {%- if strings_contains(checkHttp, " ") %}
      option httpchk {{ checkHttp }}
            {%- else %}
      option httpchk GET {{ checkHttp }}
            {%- end %}
          {%- end %}
        {%- end %}
        {#- 200-health-checks already ran, so checkDirective is set; inter is meaningless under no-check. -#}
        {%- if checkInterval != "" && (serverOpts | dig("checkDirective") | fallback("check") | tostring()) != "no-check" %}
          {%- var intInj = ConfigInjectionKind(checkInterval, "token") %}
          {%- if intInj != "" %}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotationValue", "Invalid value for annotation 'haproxy.org/check-interval' on Ingress '" + key + "': contains " + intInj + ", which would split the server directive; the check interval is skipped.") -}}
          {%- else %}
          {%- serverOpts["flags"] = append(serverOpts | dig("flags") | toSlice(), "inter " + checkInterval) %}
          {%- end %}
        {%- end %}
      {%- end -%}

  # rate-limit-requests per rate-limit-period (default 1s), table sized by
  # rate-limit-size (default 100k), deny rate-limit-status-code (default 403),
  # rate-limit-whitelist exempt. Rate limiting is a security control: an
  # injected value fails closed rather than shipping the route without its
  # limit. Each field is a bare token.
  ingress-rate-limit-0250-haproxytech:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists:
        - "metadata.annotations['haproxy.org/rate-limit-requests']"
        - "metadata.annotations['haproxy.org/rate-limit-period']"
        - "metadata.annotations['haproxy.org/rate-limit-size']"
        - "metadata.annotations['haproxy.org/rate-limit-status-code']"
        - "metadata.annotations['haproxy.org/rate-limit-whitelist']"
      group: ingress-rate-limit
      effects: [publishValue]
    template: |-
      {%- import "util-validate-cidr-list" for ValidateCidrList -%}
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-rate-limit-publish" for PublishRateLimit -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil && ingress.Metadata.Annotations["haproxy.org/rate-limit-requests"] != "" -%}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
        {%- var requests = ingress.Metadata.Annotations["haproxy.org/rate-limit-requests"] -%}
        {%- var period = ingress.Metadata.Annotations["haproxy.org/rate-limit-period"] -%}
        {%- if period == "" -%}{%- period = "1s" -%}{%- end -%}
        {%- var size = ingress.Metadata.Annotations["haproxy.org/rate-limit-size"] -%}
        {%- if size == "" -%}{%- size = "100k" -%}{%- end -%}
        {%- var status = ingress.Metadata.Annotations["haproxy.org/rate-limit-status-code"] -%}
        {%- if status == "" -%}{%- status = "403" -%}{%- end -%}
        {%- var injected = "" -%}
        {%- var injectedField = "" -%}
        {%- var c = ConfigInjectionKind(requests, "token") -%}
        {%- if c != "" -%}{%- injectedField = "rate-limit-requests" -%}{%- injected = c -%}{%- end -%}
        {%- if injected == "" -%}
          {%- c = ConfigInjectionKind(period, "token") -%}
          {%- if c != "" -%}{%- injectedField = "rate-limit-period" -%}{%- injected = c -%}{%- end -%}
        {%- end -%}
        {%- if injected == "" -%}
          {%- c = ConfigInjectionKind(size, "token") -%}
          {%- if c != "" -%}{%- injectedField = "rate-limit-size" -%}{%- injected = c -%}{%- end -%}
        {%- end -%}
        {%- if injected == "" -%}
          {%- c = ConfigInjectionKind(status, "token") -%}
          {%- if c != "" -%}{%- injectedField = "rate-limit-status-code" -%}{%- injected = c -%}{%- end -%}
        {%- end -%}
        {%- if injected != "" -%}
          {{- fail("Invalid value for annotation 'haproxy.org/" + injectedField + "' on Ingress '" + key + "': contains " + injected + ", which would split the rate-limit directive. Rate limiting must stay enforceable, so the config is rejected.") -}}
        {%- end -%}
        {%- if toint(requests) <= 0 -%}
          {{- fail("Invalid value '" + requests + "' for annotation 'haproxy.org/rate-limit-requests' on Ingress '" + key + "'. Must be a positive integer.") -}}
        {%- end -%}
        {%- if !regex_search(period, "^[0-9]+(us|ms|s|m|h|d)?$") -%}
          {{- fail("Invalid value '" + period + "' for annotation 'haproxy.org/rate-limit-period' on Ingress '" + key + "'. Expected a HAProxy time value like '1s', '10s', '1m'.") -}}
        {%- end -%}
        {%- if !regex_search(size, "^[0-9]+[kKmMgG]?$") -%}
          {{- fail("Invalid value '" + size + "' for annotation 'haproxy.org/rate-limit-size' on Ingress '" + key + "'. Expected a size like '100k', '1m'.") -}}
        {%- end -%}
        {%- if !regex_search(status, "^(200|400|401|403|404|405|407|408|410|413|414|425|429|431|500|501|502|503|504)$") -%}
          {{- fail("Invalid value '" + status + "' for annotation 'haproxy.org/rate-limit-status-code' on Ingress '" + key + "'. HAProxy can only deny with a status that has a built-in error page: 200, 400, 401, 403, 404, 405, 407, 408, 410, 413, 414, 425, 429, 431, 500, 501, 502, 503 or 504.") -}}
        {%- end -%}
        {%- var digits = size -%}
        {%- if !regex_search(size, "^[0-9]+$") -%}{%- digits = size[0:len(size)-1] -%}{%- end -%}
        {%- var bytes = toint(digits) -%}
        {%- if hasSuffix(size, "k") || hasSuffix(size, "K") -%}{%- bytes = bytes * 1024 -%}{%- end -%}
        {%- if hasSuffix(size, "m") || hasSuffix(size, "M") -%}{%- bytes = bytes * 1048576 -%}{%- end -%}
        {%- if hasSuffix(size, "g") || hasSuffix(size, "G") -%}{%- bytes = bytes * 1073741824 -%}{%- end -%}
        {#- A zero magnitude passes the format check and would reach the shared
            table as `size 0`, which HAProxy rejects only at config parse, far
            from the annotation that caused it. -#}
        {%- if bytes <= 0 -%}
          {{- fail("Invalid value '" + size + "' for annotation 'haproxy.org/rate-limit-size' on Ingress '" + key + "'. Must be a positive size like '100k', '1m'.") -}}
        {%- end -%}
        {%- var allow = "" -%}
        {%- var whitelist = ingress.Metadata.Annotations["haproxy.org/rate-limit-whitelist"] -%}
        {%- if whitelist != "" -%}{%- allow = ValidateCidrList(whitelist, "haproxy.org/rate-limit-whitelist", key) -%}{%- end -%}
        {{- PublishRateLimit(key, "req", period, tostring(bytes), tostring(toint(requests)), status, allow) -}}
      {%- end -%}

  map-reqhdr-host-250-haproxytech:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/set-host']"]
      group: map-reqhdr-host
    template: |
      {# Emits `<backend> <host>` for base.yaml's frontend-filters-260-request-set-host — a reload-free
          alternative to a per-backend set-header Host. The shared group gives this 250 component precedence
          over nginx's 760 and HAPTIC's 850 emitters when annotations target the same backend. #}
      {%- import "util-backend-name-ingress" for BackendNameIngress -%}
      {%- import "util-validate-config-value" for ValidateConfigValue -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var host = ingress.Metadata.Annotations["haproxy.org/set-host"] -%}
        {%- if host != "" -%}
          {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
          {%- var seen = map[string]bool{} -%}
          {%- for _, rule := range ingress.Spec.Rules -%}
            {%- for _, path := range rule.Http.Paths -%}
              {%- var be = BackendNameIngress(ingress, path) -%}
              {%- if !seen[be] -%}
                {%- seen[be] = true -%}
                {{- shared.Unique("map-reqhdr-host", be, "\n" + be + " " +
                  queryEscape(ValidateConfigValue(host, "haproxy.org/set-host", key, true))) -}}
              {%- end -%}
            {%- end -%}
          {%- end -%}
        {%- end -%}
      {%- end -%}

  # Reload-free header manipulation: haproxy.org/request-set-header and
  # response-set-header become entries in ing-reqhdr.map / ing-reshdr.map (built
  # by ingress.yaml frontend-filters-263-ingress-headers), keyed on the resolved
  # backend, so changing a header value is a map-only update. Contributes in the
  # features phase (before the frontend builder reads the shared list).
  features-300-haproxytech-header-mods:
    template: '{{- "" -}}'

  ingress-header-mods-0300-haproxytech:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists:
        - "metadata.annotations['haproxy.org/request-set-header']"
        - "metadata.annotations['haproxy.org/response-set-header']"
      group: ingress-header-mods
      effects: [publishValue, recordEvent]
    template: |-
      {%- import "util-ingress-header-publish" for PublishIngressHeaderContributions -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {{- PublishIngressHeaderContributions(ingress,
          "haproxy.org/request-set-header", "haproxy.org/response-set-header", "\n", " ", "0300-haproxytech") -}}
      {%- end -%}

  # Resolves one Ingress's haproxy.org/path-rewrite: result["lane"] is ""
  # (no rewrite), "invalid" (a control character; result["message"] says so),
  # "map" or "backend" (see util-path-rewrite-shape in ingress-annotations-compat).
  util-haproxytech-path-rewrite:
    template: |-
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-path-rewrite-shape" for PathRewriteShape -%}
      {%- macro HaproxytechPathRewrite(ingress *resources.ingresses.T, result map[string]any) -%}
        {%- result["lane"] = "" -%}
        {%- var rewrite = ingress.Metadata.Annotations["haproxy.org/path-rewrite"] -%}
        {%- if rewrite != "" -%}
          {%- var inj = ConfigInjectionKind(rewrite, "line") -%}
          {%- if inj != "" -%}
            {%- result["lane"] = "invalid" -%}
            {%- result["message"] = "Invalid value for annotation 'haproxy.org/path-rewrite' on Ingress '" + ingress.Metadata.Namespace + "/" + ingress.Metadata.Name + "': contains " + inj + ", which would split the backend directive; the path rewrite is skipped." -%}
          {%- else -%}
            {{- PathRewriteShape(rewrite, result) -}}
          {%- end -%}
        {%- end -%}
      {%- end macro -%}

  # Gates the frontend block and feeds its maps. An invalid value is reported
  # by the backend component, which can emit text.
  features-350-haproxytech-rewrite-routes:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/path-rewrite']"]
      group: haproxytech-rewrite-routes
      effects: [publishValue]
    template: |-
      {%- import "util-haproxytech-path-rewrite" for HaproxytechPathRewrite -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var rw = map[string]any{} -%}
        {{- HaproxytechPathRewrite(ingress, rw) -}}
        {%- if rw["lane"] == "map" -%}
          {%- var rid = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
          {%- var row = rid + "|" + tostring(rw["prefix"]) + " " + tostring(rw["offset"]) -%}
          {%- if tostring(rw["newPrefix"]) != "" -%}{%- row = row + " " + tostring(rw["newPrefix"]) -%}{%- end -%}
          {{- shared.Publish("any", "any", "any") -}}
          {{- shared.Publish("route", rid, rid + " 1") -}}
          {{- shared.Publish("prefix", rid, row) -}}
        {%- end -%}
      {%- end -%}

  backend-directives-350-haproxytech-path-rewrite:
    template: |
      {#- Regex escapes and \N backreferences are legitimate here, so the value is
          emitted raw and guarded for control chars only (EscapeLogFormatValue would
          double the backslashes). Prefix strips and the bare form ride the frontend
          map instead (frontend-filters-995-haproxytech-path-rewrite). -#}
      {%- import "util-haproxytech-path-rewrite" for HaproxytechPathRewrite -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- if ingress != nil %}
        {%- var rw = map[string]any{} %}
        {{- HaproxytechPathRewrite(ingress, rw) -}}
        {%- if rw["lane"] == "invalid" %}
          {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", tostring(rw["message"])) -}}
        {%- else if rw["lane"] == "backend" %}
      # haproxytech/backend-directives-haproxytech-path-rewrite
      http-request replace-path {{ rw["from"] }} {{ rw["to"] }}
        {%- end %}
      {%- end -%}

  backend-directives-400-haproxytech-session-persistence:
    template: |
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- if ingress != nil %}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name %}
        {%- var cookie = ingress.Metadata.Annotations["haproxy.org/cookie-persistence"] %}
        {%- if cookie != "" %}
          {%- var cookieInj = ConfigInjectionKind(cookie, "token") %}
          {%- if cookieInj != "" %}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", "Invalid value for annotation 'haproxy.org/cookie-persistence' on Ingress '" + key + "': contains " + cookieInj + ", which would split the backend directive; session persistence is skipped.") -}}
          {%- else %}
      # haproxytech/backend-directives-haproxytech-session-persistence
      cookie {{ cookie }} insert indirect nocache dynamic
          {%- end %}
        {%- end %}
      {%- end -%}

  backend-directives-401-haproxytech-session-persistence-no-dynamic:
    template: |
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- if ingress != nil %}
        {%- var ns = ingress.Metadata.Namespace %}
        {%- var name = ingress.Metadata.Name %}
        {%- var key = ns + "/" + name %}
        {%- var dynamic = ingress.Metadata.Annotations["haproxy.org/cookie-persistence"] %}
        {%- var static = ingress.Metadata.Annotations["haproxy.org/cookie-persistence-no-dynamic"] %}
        {%- if dynamic != "" && static != "" %}
          {{- fail("Cannot use both 'haproxy.org/cookie-persistence' and 'haproxy.org/cookie-persistence-no-dynamic' on Ingress '" + key + "'. Use only one cookie persistence method.") -}}
        {%- end %}
        {%- if static != "" %}
          {%- var staticInj = ConfigInjectionKind(static, "token") %}
          {%- if staticInj != "" %}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", "Invalid value for annotation 'haproxy.org/cookie-persistence-no-dynamic' on Ingress '" + key + "': contains " + staticInj + ", which would split the backend directive; session persistence is skipped.") -}}
          {%- else %}
      # haproxytech/backend-directives-haproxytech-session-persistence-no-dynamic
      cookie {{ static }} insert indirect nocache
          {%- end %}
        {%- end %}
      {%- end -%}

  # Resolves one Ingress's basic auth. result["lane"] is "" (no basic-auth or
  # no auth-secret), "map" (state "ok" with userlist and realm, or "missing")
  # or "backend" (a realm that needs escaping, which a map value cannot carry).
  util-haproxytech-basic-auth-route:
    template: |-
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- macro HaproxytechBasicAuthRoute(ingress *resources.ingresses.T, result map[string]any) -%}
        {%- result["lane"] = "" -%}
        {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
        {%- var authType = ingress.Metadata.Annotations["haproxy.org/auth-type"] -%}
        {%- if authType != "" && authType != "basic-auth" -%}
          {{- fail("Invalid value '" + authType + "' for annotation 'haproxy.org/auth-type' on Ingress '" + key + "'. Valid values: 'basic-auth'") -}}
        {%- end -%}
        {%- if authType == "basic-auth" -%}
          {%- var authSecret = ingress.Metadata.Annotations["haproxy.org/auth-secret"] -%}
          {%- var authRealm = ingress.Metadata.Annotations["haproxy.org/auth-realm"] -%}
          {%- if authRealm == "" -%}{%- authRealm = "Protected-Content" -%}{%- end -%}
          {#- Upstream dashes spaces in the realm; match it rather than fail. -#}
          {%- authRealm = replace(authRealm, " ", "-") -%}
          {%- var realmInj = ConfigInjectionKind(authRealm, "line") -%}
          {%- if realmInj != "" -%}
            {{- fail("Invalid value for annotation 'haproxy.org/auth-realm' on Ingress '" + key + "': contains " + realmInj + ". The realm must not contain control characters.") -}}
          {%- end -%}
          {%- if authSecret != "" -%}
            {%- var secretNs = ingress.Metadata.Namespace -%}
            {%- var secretName = authSecret -%}
            {%- if strings_contains(authSecret, "/") -%}
              {%- var parts = split(authSecret, "/") -%}
              {%- secretNs = parts[0] -%}
              {%- secretName = parts[1] -%}
            {%- end -%}
            {%- result["userlist"] = "auth_" + secretNs + "_" + secretName -%}
            {%- result["realm"] = authRealm -%}
            {%- if resources.secrets.GetSingle(secretNs, secretName) == nil -%}
              {%- result["lane"] = "map" -%}
              {%- result["state"] = "missing" -%}
            {%- else -%}
              {%- result["lane"] = "backend" -%}
              {#- The realm is a literal in a quoted config token and in a map
                  value: one that needs escaping stays on the backend rule. -#}
              {%- if regex_search(authRealm, "^[^\"\\\\$\\t]*$") && authRealm == strip(authRealm) -%}
                {%- result["lane"] = "map" -%}
                {%- result["state"] = "ok" -%}
              {%- end -%}
            {%- end -%}
          {%- end -%}
        {%- end -%}
      {%- end macro -%}

  backend-directives-500-haproxytech-ingress-auth:
    template: |
      {%- import "util-escape-dquote-value" for EscapeDquoteValue -%}
      {%- import "util-haproxytech-basic-auth-route" for HaproxytechBasicAuthRoute -%}
      {#- A realm that needs escaping keeps the challenge here, which makes the
          backend structural; every other route rides the frontend map lane
          (frontend-filters-900-haproxytech-basic-auth). -#}
      {%- if ingress != nil %}
        {%- var rw = map[string]any{} %}
        {{- HaproxytechBasicAuthRoute(ingress, rw) -}}
        {%- if rw["lane"] == "backend" %}
      # haproxytech/backend-directives-haproxytech-ingress-auth: the realm needs escaping, so it stays a backend rule
      http-request set-var(txn.denied_by) str(basic_auth) unless { http_auth({{ rw["userlist"] }}) }
      http-request auth realm "{{ EscapeDquoteValue(tostring(rw["realm"])) }}" unless { http_auth({{ rw["userlist"] }}) }
        {%- end %}
      {%- end -%}

  # Rows, userlists and (userlist, realm) pairs for the map lane; also the
  # fail-closed event. A component cannot both publish and emit text, hence
  # the split from the backend snippet.
  features-500-haproxytech-basic-auth-routes:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists:
        - "metadata.annotations['haproxy.org/auth-type']"
        - "metadata.annotations['haproxy.org/auth-secret']"
        - "metadata.annotations['haproxy.org/auth-realm']"
      group: haproxytech-basic-auth-routes
      effects: [publishValue, recordEvent]
    template: |-
      {%- import "util-haproxytech-basic-auth-route" for HaproxytechBasicAuthRoute -%}
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil -%}
        {%- var rw = map[string]any{} -%}
        {{- HaproxytechBasicAuthRoute(ingress, rw) -}}
        {%- if rw["lane"] == "map" -%}
          {%- var key = ingress.Metadata.Namespace + "/" + ingress.Metadata.Name -%}
          {{- shared.Publish("any", "any", "any") -}}
          {%- if rw["state"] == "ok" -%}
            {%- var pair = tostring(rw["userlist"]) + " " + tostring(rw["realm"]) -%}
            {{- shared.Publish("row", key, key + " ok " + pair) -}}
            {{- shared.Publish("userlist", tostring(rw["userlist"]), tostring(rw["userlist"])) -}}
            {{- shared.Publish("pair", pair, pair) -}}
          {%- else -%}
            {{- shared.Publish("row", key, key + " missing") -}}
            {{- recordEvent(ingress, "AuthSecretMissing", "Ingress " + key + " sets 'haproxy.org/auth-secret', but that Secret could not be resolved. Basic auth cannot be enforced without it, so this route fails closed with 503. Create the Secret, or remove the auth annotations.") -}}
          {%- end -%}
        {%- end -%}
      {%- end -%}

  # Basic-auth challenge, map lane: one block per HTTP frontend at band 900,
  # the last request-phase band, where the backend rule used to run.
  frontend-filters-900-haproxytech-basic-auth:
    requires: [ingresses]
    template: |
      {#
        haproxytech-basic-auth-routes.map: "<ns>/<name>" → "ok <userlist> <realm>" | "missing",
        built from the rows features-*-haproxytech-basic-auth-routes publishes. `auth
        realm` and http_auth() take literals only, so there is one rule per
        distinct (userlist, realm) pair, selected by comparing the whole map
        value; a route whose Secret is absent is denied 503. Emitted only while
        a route uses the lane; the first one reloads once, as does removing the
        last or adding a pair.
      #}
      {%- import "util-register-map" for RegisterMap -%}
      {%- if incremental_value_count("haproxytech-basic-auth-routes", "any") > 0 -%}
      {%%
        var routesVal, _ = shared.ComputeIfAbsent("haproxytechBasicAuthMap", func() any {
          var rows = []any{}
          for _, v := range incremental_values("haproxytech-basic-auth-routes", "row") {
            rows = append(rows, tostring(v))
          }
          var lines = []string{}
          for _, r := range sort_strings(rows) {
            lines = append(lines, tostring(r))
          }
          return RegisterMap("haproxytech-basic-auth-routes.map", lines, map[string]any{"ordered": false})
        })
        var routes = tostring(routesVal)
        var userlists = []any{}
        for _, v := range incremental_values("haproxytech-basic-auth-routes", "userlist") {
          userlists = append(userlists, tostring(v))
        }
        var pairs = []any{}
        for _, v := range incremental_values("haproxytech-basic-auth-routes", "pair") {
          pairs = append(pairs, tostring(v))
        }
      %%}
      # haproxytech/basic-auth
      http-request set-var(txn.ht_ba) var(txn.resource_id),map({{ routes }})
      http-request set-var(txn.denied_by) str(basic_auth_unavailable) if { var(txn.ht_ba) -m str missing }
      http-request deny deny_status 503 if { var(txn.ht_ba) -m str missing }
      {%- for _, u := range sort_strings(userlists) %}
      http-request set-var(txn.denied_by) str(basic_auth) if { var(txn.ht_ba) -m beg "ok {{ u }} " } !{ http_auth({{ u }}) }
      {%- end %}
      {%- for _, p := range sort_strings(pairs) %}
      {%- var pair = strings_splitn(tostring(p), " ", 2) %}
      http-request auth realm "{{ pair[1] }}" if { var(txn.ht_ba) -m str "ok {{ p }}" } !{ http_auth({{ pair[0] }}) }
      {%- end %}
      {%- end %}

  # Path rewrite, map lane: one block per HTTP frontend at band 995, the last
  # request-phase band, where the backend rule used to run.
  frontend-filters-995-haproxytech-path-rewrite:
    requires: [ingresses]
    template: |
      {#
        Annotation: haproxy.org/path-rewrite (prefix strips and the bare form;
        see util-haproxytech-path-rewrite). haproxytech-rewrite-routes.map and
        haproxytech-rewrite-prefix.map are built from the rows
        features-350-haproxytech-rewrite-routes publishes and read by the shared
        util-path-rewrite-lane. Emitted only while a route uses the lane; the
        first one reloads once, as does removing the last.
      #}
      {%- import "util-register-map" for RegisterMap -%}
      {%- import "util-path-rewrite-lane" for PathRewriteLane -%}
      {%- if incremental_value_count("haproxytech-rewrite-routes", "any") > 0 -%}
      {%%
        var mapsVal, _ = shared.ComputeIfAbsent("haproxytechRewriteMaps", func() any {
          var lines = func(key string) []string {
            var rows = []any{}
            for _, v := range incremental_values("haproxytech-rewrite-routes", key) {
              rows = append(rows, tostring(v))
            }
            var out = []string{}
            for _, r := range sort_strings(rows) {
              out = append(out, tostring(r))
            }
            return out
          }
          return map[string]any{
            "routes": RegisterMap("haproxytech-rewrite-routes.map", lines("route"), map[string]any{"ordered": false}),
            "prefix": RegisterMap("haproxytech-rewrite-prefix.map", lines("prefix"), map[string]any{"ordered": false}),
          }
        })
        var maps = mapsVal.(map[string]any)
      %%}
      # haproxytech/path-rewrite
      {{ PathRewriteLane(tostring(maps["routes"]), tostring(maps["prefix"]), "ht_rw") }}
      {%- end %}

  backend-directives-900-haproxytech-advanced:
    template: |
      {%- import "util-config-injection-kind" for ConfigInjectionKind -%}
      {%- import "util-webhook-reject-or-warn" for WebhookRejectOrWarn -%}
      {%- if ingress != nil %}
        {%- var ns = ingress.Metadata.Namespace %}
        {%- var name = ingress.Metadata.Name %}
        {%- var key = ns + "/" + name %}

        {%- var sendProxy = ingress.Metadata.Annotations["haproxy.org/send-proxy-protocol"] %}
        {%- if sendProxy != "" %}
          {%- if sendProxy == "proxy" || sendProxy == "proxy-v1" %}
            {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "send-proxy") %}
          {%- else if sendProxy == "proxy-v2" %}
            {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "send-proxy-v2") %}
          {%- else if sendProxy == "proxy-v2-ssl" %}
            {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "send-proxy-v2-ssl") %}
          {%- else if sendProxy == "proxy-v2-ssl-cn" %}
            {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "send-proxy-v2-ssl-cn") %}
          {%- end %}
        {%- end %}

        {%- var serverSsl = ingress.Metadata.Annotations["haproxy.org/server-ssl"] %}
        {%- if serverSsl == "true" %}
          {#- One keyword per entry so the plan record carries each structurally. -#}
          {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "ssl") %}
          {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "alpn h2,http/1.1") %}
          {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "verify none") %}
        {%- end %}

        {%- var serverProto = ingress.Metadata.Annotations["haproxy.org/server-proto"] %}
        {%- if serverProto != "" %}
          {%- var protoInj = ConfigInjectionKind(serverProto, "token") %}
          {%- if protoInj != "" %}
            {{- WebhookRejectOrWarn(ingress, "InvalidAnnotation", "Invalid value for annotation 'haproxy.org/server-proto' on Ingress '" + key + "': contains " + protoInj + ", which would split the server directive; the upstream protocol is skipped.") -}}
          {%- else %}
          {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "proto " + serverProto) %}
          {%- end %}
        {%- end %}

        {%- var serverCa = ingress.Metadata.Annotations["haproxy.org/server-ca"] %}
        {%- if serverCa != "" %}
          {%- var caNs = ns %}
          {%- var caName = serverCa %}
          {%- if strings_contains(serverCa, "/") %}
            {%- var parts = split(serverCa, "/") %}
            {%- caNs = parts[0] %}
            {%- caName = parts[1] %}
          {%- end %}
          {%- var caSecret = resources.secrets.GetSingle(caNs, caName) %}
          {%- if caSecret != nil %}
            {%- var caContent = caSecret.Data["ca.crt"] | b64decode() %}
            {%- if caContent != "" %}
              {%- var caFilename = caNs + "-" + caName + "-ca.pem" %}
              {#- "ca-file": a content-only CA rotation applies at runtime, no reload on v3.2+. -#}
              {%- var caPath, caRegErr = RegisterBackendFile("ca-file", caFilename, caContent) -%}
              {%- if caRegErr != nil -%}{{ fail("failed to register CA file " + caFilename + ": " + tostring(caRegErr)) }}{%- end -%}
              {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "ssl") %}
              {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "verify required") %}
              {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "ca-file " + caPath) %}
            {%- else %}
              {#- Name-free comment: the tenant-controlled Secret name goes only into the
                  Kubernetes Event, never into the rendered config, so a newline in it can't
                  smuggle a directive past the # comment. -#}
              {{- recordEvent(ingress, "ServerCASecretMissingCACert", "Ingress " + key + " references Secret '" + serverCa + "' in 'haproxy.org/server-ca', but it has no ca.crt. Upstream certificate verification is skipped.") -}}
            # haproxy.org/server-ca: referenced Secret has no ca.crt; upstream verification skipped
            {%- end %}
          {%- else %}
            {{- recordEvent(ingress, "ServerCASecretMissing", "Ingress " + key + " references Secret '" + serverCa + "' in 'haproxy.org/server-ca', but it could not be resolved. Upstream certificate verification is skipped.") -}}
            # haproxy.org/server-ca: referenced Secret not found; upstream verification skipped
          {%- end %}
        {%- end %}

        {%- var serverCrt = ingress.Metadata.Annotations["haproxy.org/server-crt"] %}
        {%- if serverCrt != "" %}
          {%- var crtNs = ns %}
          {%- var crtName = serverCrt %}
          {%- if strings_contains(serverCrt, "/") %}
            {%- var parts = split(serverCrt, "/") %}
            {%- crtNs = parts[0] %}
            {%- crtName = parts[1] %}
          {%- end %}
          {%- var crtSecret = resources.secrets.GetSingle(crtNs, crtName) %}
          {%- if crtSecret != nil %}
            {%- var tlsCrt = crtSecret.Data["tls.crt"] | b64decode() %}
            {%- var tlsKey = crtSecret.Data["tls.key"] | b64decode() %}
            {%- if tlsCrt != "" && tlsKey != "" %}
              {%- var combinedPem = tlsCrt + "\n" + tlsKey %}
              {%- var crtFilename = crtNs + "-" + crtName + "-client.pem" %}
              {%- var _, crtRegErr = RegisterBackendFile("cert", crtFilename, combinedPem) -%}
              {%- if crtRegErr != nil -%}{{ fail("failed to register client cert " + crtFilename + ": " + tostring(crtRegErr)) }}{%- end -%}
              {#- Bare filename: crt-base joins it, on the config line and on add server alike. -#}
              {%- serverOpts["flags"] = append(serverOpts["flags"].([]any), "crt " + crtFilename) %}
            {%- else %}
              {{- recordEvent(ingress, "ServerCrtSecretIncomplete", "Ingress " + key + " references Secret '" + serverCrt + "' in 'haproxy.org/server-crt', but it has no tls.crt/tls.key. No client certificate is presented upstream.") -}}
            # haproxy.org/server-crt: referenced Secret missing tls.crt/tls.key; no client certificate presented
            {%- end %}
          {%- else %}
            {{- recordEvent(ingress, "ServerCrtSecretMissing", "Ingress " + key + " references Secret '" + serverCrt + "' in 'haproxy.org/server-crt', but it could not be resolved. No client certificate is presented upstream.") -}}
            # haproxy.org/server-crt: referenced Secret not found; no client certificate presented
          {%- end %}
        {%- end %}

        {#- haproxy.org/scale-server-slots no longer has any effect: servers are
            named after their pods (no slot pool, ADR-0011), so a backend always
            holds the current pods and scaling never reserves slots or reloads. -#}
        {%- if ingress.Metadata.Annotations["haproxy.org/scale-server-slots"] != "" %}
          {%- _ = WebhookRejectOrWarn(ingress, "UnsupportedAnnotation", "Ingress " + key + " sets 'haproxy.org/scale-server-slots', which no longer has any effect: servers are named after pods and scale with no reserved slots and no reload. Remove the annotation.") %}
        {%- end %}

        {%- var snippet = ingress.Metadata.Annotations["haproxy.org/backend-config-snippet"] %}
        {%- if snippet != "" %}
      # haproxytech/backend-config-snippet
      {{ snippet }}
        {%- end %}
      {%- end -%}

  backends-501-haproxytech-ssl-passthrough:
    requires: [ingresses, services, endpoints]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/ssl-passthrough']"]
      group: ingress-ssl-passthrough
      effects: [publishValue, backendPlan]
    template: |-
      {%- import "util-annotation-ssl-passthrough-component" for RenderAnnotationSSLPassthrough -%}
      {{- RenderAnnotationSSLPassthrough(item,
        "haproxy.org/ssl-passthrough",
        "ssl-passthrough-", "haproxytech", "haproxytech") -}}

  log-fields-530-haproxytech-captured-headers:
    template: |-
      {# %hr is the positional capture list — the only consumer of haproxy.org/request-capture.
          haptic-annotations emits the identical item for the native vocabulary; base deduplicates. #}
      {%- if incremental_value_count("haproxytech-captured-headers", "enabled") > 0 %}
      %(captured_headers)hr
      {%- end %}

  haproxytech-captured-headers-publications:
    requires: [ingresses]
    incremental:
      source: ingresses
      whenAnyPathExists: ["metadata.annotations['haproxy.org/request-capture']"]
      group: haproxytech-captured-headers
      effects: [publishValue]
    template: |-
      {%- var ingress = resources.ingresses.GetSingle(
        dig_string(item, "", "metadata", "namespace"), dig_string(item, "", "metadata", "name")) -%}
      {%- if ingress != nil && ingress.Metadata.Annotations["haproxy.org/request-capture"] != "" -%}
        {{- shared.Publish("enabled", "global", true) -}}
      {%- end -%}

validationTests:
  test-haproxytech-set-host:
    description: haproxy.org/set-host relocates the upstream Host override to reqhdr-host.map (reload-free)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: sethost-svc}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: sethost-ing
            annotations:
              haproxy.org/set-host: "internal.svc.local"
          spec:
            ingressClassName: haproxy
            rules:
              - host: sethost.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: sethost-svc, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: map:reqhdr-host.map
        pattern: "default_sethost-ing_svc_sethost-svc_80 internal.svc.local"
      - {type: not_contains, target: haproxy.cfg, pattern: "set-header Host 'internal.svc.local'"}

  test-auth-basic-cross-namespace:
    description: Basic authentication with secret in different namespace
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: auth-system, name: shared-auth}
          type: Opaque
          data: {admin: JDJ5JDA1JG1OMVdWazVRbmJnNFF3ZEFkWGJmei44YjNjZUg2UTVLT1ZDS3hSMklrTkFmSmdMaTVwSUtX}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: production
            name: api-ingress
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: auth-system/shared-auth
          spec:
            rules:
              - host: api.production.example.com
                http:
                  paths:
                    - path: /api
                      pathType: Prefix
                      backend: {service: {name: api-service, port: {number: 443}}}
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: production, name: api-service}
          spec:
            ports:
              - {port: 443, targetPort: 8443}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "userlist auth_auth-system_shared-auth"}
      - {type: contains, target: haproxy.cfg, pattern: "user admin password"}
      - {type: contains, target: haproxy.cfg, pattern: "http_auth\\(auth_auth-system_shared-auth\\)"}

  test-haproxytech-basic-auth-map-lane:
    description: Basic auth rides the frontend map lane — routes on one Secret share its userlist, one auth rule per (userlist, realm) pair selected on the whole map value (spaces dashed as upstream does), one denied_by rule per userlist, and no backend challenge
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: api-auth}
          type: Opaque
          data: {apiuser: JDJ5JDA1JDJlV1VoQVU5Z0VlZ2FDZkY4Uk4xZHV1WUNrdlhoUGtpS1BDL3pkTFBvN2xNYlkxcWlUQXRP}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ht-one
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: api-auth
              haproxy.org/auth-realm: "Zone A"
          spec:
            rules:
              - host: one.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: api-service, port: {number: 8080}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ht-two
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: api-auth
          spec:
            rules:
              - host: two.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: api-service, port: {number: 8080}}}
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: api-service}
          spec:
            ports:
              - port: 8080
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: 'http-request set-var\(txn\.ht_ba\) var\(txn\.resource_id\),map\(\S*/haproxytech-basic-auth-routes.map\)'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request deny deny_status 503 if \{ var\(txn\.ht_ba\) -m str missing \}'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request set-var\(txn\.denied_by\) str\(basic_auth\) if \{ var\(txn\.ht_ba\) -m beg "ok auth_default_api-auth " \} !\{ http_auth\(auth_default_api-auth\) \}'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request auth realm "Zone-A" if \{ var\(txn\.ht_ba\) -m str "ok auth_default_api-auth Zone-A" \} !\{ http_auth\(auth_default_api-auth\) \}'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request auth realm "Protected-Content" if \{ var\(txn\.ht_ba\) -m str "ok auth_default_api-auth Protected-Content" \} !\{ http_auth\(auth_default_api-auth\) \}'}
      - {type: not_contains, target: haproxy.cfg, pattern: 'unless \{ http_auth\(auth_default_api-auth\) \}'}
      - {type: match_count, target: haproxy.cfg, pattern: "userlist auth_default_api-auth", expected: "1"}
      - {type: contains, target: "map:haproxytech-basic-auth-routes.map", pattern: '(?m)^default/ht-one ok auth_default_api-auth Zone-A$'}
      - {type: contains, target: "map:haproxytech-basic-auth-routes.map", pattern: '(?m)^default/ht-two ok auth_default_api-auth Protected-Content$'}

  test-auth-custom-realm:
    description: Basic authentication with custom realm
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: api-auth}
          type: Opaque
          data: {apiuser: JDJ5JDA1JDJlV1VoQVU5Z0VlZ2FDZkY4Uk4xZHV1WUNrdlhoUGtpS1BDL3pkTFBvN2xNYlkxcWlUQXRP}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: secure-api-ingress
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: api-auth
              haproxy.org/auth-realm: "API-Access"
          spec:
            rules:
              - host: secure-api.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: api-service, port: {number: 8080}}}
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: api-service}
          spec:
            ports:
              - port: 8080
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: 'http-request auth realm "API-Access"'}
      - {type: not_contains, target: haproxy.cfg, pattern: 'realm "Protected-Content"'}

  test-haproxytech-basic-auth-missing-secret:
    description: >-
      haproxy.org/auth-secret referencing a Secret absent from the render
      snapshot (informer lag / cascade-delete) must skip the userlist + auth ACL
      cleanly, not panic. Regression for the typed-nil gotcha (GetSingle returns a
      typed-nil *resources.secrets.T when absent; boxing it through
      shared.ComputeIfAbsent's func() any made `!= nil` pass, then `.Data`
      dereferenced nil).
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: api-service}
          spec:
            ports:
              - port: 8080
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: secure-api-missing-secret
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: absent-secret
              haproxy.org/auth-realm: "API-Access"
          spec:
            rules:
              - host: secure-api-missing.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: api-service, port: {number: 8080}}}
    assertions:
      - type: haproxy_valid
      - {type: not_contains, target: haproxy.cfg, pattern: "userlist auth_default_absent-secret"}
      - {type: not_contains, target: haproxy.cfg, pattern: "http_auth(auth_default_absent-secret)"}

  test-haproxytech-basic-auth-hash-injection-fails-closed:
    description: A space in a basic-auth password hash (accepted by the default hash regex) fails the render closed instead of appending extra tokens to the userlist line (#157).
    extraContext: {renderMode: admission}
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: basic-auth-secret}
          data: {admin: eDJ5MDV2YWxpZGhhc2ggZ3JvdXBzIGFkbWlu}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: hash-inj
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: basic-auth-secret
          spec:
            rules:
              - host: hash-inj.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: api-service, port: {number: 8080}}}
    assertions:
      - {type: contains, target: rendering_error, pattern: "would break out of the userlist line"}

  test-auth-realm-with-spaces:
    description: Auth realm with spaces is normalized to dashes (matching upstream), not a render failure
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: api-auth}
          type: Opaque
          data: {user1: JDJ5JDA1JDJlV1VoQVU5Z0VlZ2FDZkY4Uk4xZHV1WUNrdlhoUGtpS1BDL3pkTFBvN2xNYlkxcWlUQXRP}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: space-realm-ingress
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: api-auth
              haproxy.org/auth-realm: "DeepL demo"
          spec:
            rules:
              - host: api.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: 'realm "DeepL-demo"'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request auth realm "DeepL-demo"'}

  test-auth-per-secret-userlists:
    description: Ingresses sharing the same secret share the same userlist (deduplication for performance)
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: shared-credentials}
          type: Opaque
          data: {admin: JDJ5JDA1JG1OMVdWazVRbmJnNFF3ZEFkWGJmei44YjNjZUg2UTVLT1ZDS3hSMklrTkFmSmdMaTVwSUtX}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: app1-ingress
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: shared-credentials
          spec:
            rules:
              - host: app1.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: app1-service, port: {number: 80}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: app2-ingress
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: shared-credentials
          spec:
            rules:
              - host: app2.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: app2-service, port: {number: 80}}}
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: app1-service}
          spec:
            ports:
              - port: 80
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: app2-service}
          spec:
            ports:
              - port: 80
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "userlist auth_default_shared-credentials"}
      - {type: not_contains, target: haproxy.cfg, pattern: "userlist default-app1-ingress"}
      - {type: not_contains, target: haproxy.cfg, pattern: "userlist default-app2-ingress"}
      - {type: contains, target: haproxy.cfg, pattern: "http_auth\\(auth_default_shared-credentials\\)"}

  test-load-balance-hdr-parameter:
    description: Load balance with hdr(header-name) parameter for header-based routing
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: sticky-ingress
            annotations:
              haproxy.org/load-balance: "hdr(x-session-id)"
          spec:
            rules:
              - host: api.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "balance hdr\\(x-session-id\\)"}

  test-haproxytech-cors-regex-anchor:
    description: CORS non-wildcard origin regex-matches the request Origin and echoes it back (upstream model), and a regex $ anchor must not cause a parse error
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cors-regex-ingress
            annotations:
              haproxy.org/cors-enable: "true"
              haproxy.org/cors-allow-origin: "^https://(.+\\.)?(dev\\.|test\\.)?example\\.com$"
              haproxy.org/cors-allow-methods: "GET, POST, OPTIONS"
          spec:
            ingressClassName: haproxy
            rules:
              - host: api.example.com
                http:
                  paths:
                    - path: /api
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      # The regex origin becomes one regex row keyed by route, its leading ^ kept
      # at the start of the Origin and its escaped dots intact.
      - {type: contains, target: "map:ing-cors-regex.map", pattern: '(?m)\\\|\(https://\(\.\+\\\.\)\?\(dev\\\.\|test\\\.\)\?example\\\.com\$\) 1$'}
      - {type: contains, target: haproxy.cfg, pattern: 'concat\(\|,txn\.haproxytech_cors_origin\),map_reg\(\S*/ing-cors-regex\.map\) -m found'}
      - type: contains
        target: haproxy.cfg
        pattern: "Access-Control-Allow-Origin '%\\[var\\(txn\\.haproxytech_cors_origin\\)\\]'"
      - type: contains
        target: haproxy.cfg
        pattern: "http-after-response set-header Access-Control-Allow-Origin"

  test-haproxytech-cors-respond-to-options:
    description: cors-respond-to-options makes HAProxy answer the OPTIONS preflight with a 204
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cors-preflight-ingress
            annotations:
              haproxy.org/cors-enable: "true"
              haproxy.org/cors-allow-origin: "https://app.example.com"
              haproxy.org/cors-allow-credentials: "true"
              haproxy.org/cors-respond-to-options: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: api.example.com
                http:
                  paths:
                    - path: /api
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "http-request return status 204 if .* METH_OPTIONS"}
      - {type: contains, target: "map:ing-cors-routes.map", pattern: '(?m)^default/cors-preflight-ingress haproxytech:list 1 0 1 2$'}
      - type: contains
        target: haproxy.cfg
        pattern: "http-after-response set-header Access-Control-Allow-Credentials 'true'"

  test-haproxytech-auth-password-hash-validation-reject:
    description: Password hash validation rejects non-matching hashes
    extraContext:
      annotationCompatibility:
        basicAuth: {passwordHashValidation: {regex: "^\\$apr1\\$", errorMessage: "Bcrypt hashes not allowed"}}
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: auth-secret}
          type: Opaque
          data: {admin: JDJ5JDEwJGJjcnlwdGhhc2g=}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: auth-test
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: auth-secret
          spec: *specTestExampleComRoot
    assertions:
      - {type: contains, target: rendering_error, pattern: "Bcrypt hashes not allowed"}
      - {type: contains, target: rendering_error, pattern: "admin"}
      - {type: contains, target: rendering_error, pattern: "pattern"}

  test-haproxytech-auth-password-hash-validation-allow:
    description: Password hash validation allows matching hashes
    extraContext: {annotationCompatibility: {basicAuth: {passwordHashValidation: {regex: "^\\$2[ayb]\\$"}}}}
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: auth-secret}
          type: Opaque
          data: {admin: JDJ5JDA1JG1OMVdWazVRbmJnNFF3ZEFkWGJmei44YjNjZUg2UTVLT1ZDS3hSMklrTkFmSmdMaTVwSUtX}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: auth-test
            annotations:
              haproxy.org/auth-type: basic-auth
              haproxy.org/auth-secret: auth-secret
          spec: *specTestExampleComRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "user admin password"}

  test-haproxytech-ssl-redirect-default-enabled:
    description: SSL redirect applies when annotation absent and ssl_redirect_default is "true"
    extraContext: {ssl_redirect_default: "true"}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: no-annotation}
          spec: *specTestExampleComRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "http-request redirect scheme https"}

  # Two DIFFERENT redirect codes on purpose. The rule block is emitted one
  # rule per code, and with a single code Go cannot reorder a one-element map,
  # so a one-code fixture passes whether or not the template sorts. This is the
  # fixture that gives the runner's automatic determinism check something to
  # see at this site.
  test-haproxytech-ssl-redirect-two-codes-ordered:
    description: Two ssl-redirect codes emit their rules in a stable order
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: redirect-301
            annotations:
              haproxy.org/ssl-redirect: "true"
              haproxy.org/ssl-redirect-code: "301"
          spec:
            rules:
              - host: moved.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: redirect-302
            annotations:
              haproxy.org/ssl-redirect: "true"
              haproxy.org/ssl-redirect-code: "302"
          spec:
            rules:
              - host: temp.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: match_order
        target: haproxy.cfg
        patterns:
          - "ssl-redirect \\(code 301\\)"
          - "ssl-redirect \\(code 302\\)"
        description: Rules emit in ascending code order, not map order

  test-haproxytech-ssl-redirect-default-opt-out:
    description: SSL redirect does not apply when annotation is "false" even with default
    extraContext: {ssl_redirect_default: "true"}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: opt-out
            annotations:
              haproxy.org/ssl-redirect: "false"
          spec: *specTestExampleComRoot
    assertions:
      - type: haproxy_valid
      - {type: not_contains, target: haproxy.cfg, pattern: "http-request redirect scheme https"}

  test-haproxytech-ssl-redirect-no-default:
    description: SSL redirect does not apply when annotation absent and no default
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: no-annotation}
          spec: *specTestExampleComRoot
    assertions:
      - type: haproxy_valid
      - {type: not_contains, target: haproxy.cfg, pattern: "http-request redirect scheme https"}

  test-haproxytech-ssl-redirect-many-hosts:
    description: SSL redirect works with many hosts using map-based matching
    extraContext: {ssl_redirect_default: "true"}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: many-hosts}
          spec:
            rules:
              - host: host01.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: echo, port: { number: 80 } } } }
              - host: host02.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: echo, port: { number: 80 } } } }
              - host: host03.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: echo, port: { number: 80 } } } }
              - host: host04.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: echo, port: { number: 80 } } } }
              - host: host05.example.com
                http:
                  paths:
                    - { path: /, pathType: Prefix, backend: { service: { name: echo, port: { number: 80 } } } }
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "http-request redirect scheme https code 302 if.*map_str.*ssl-redirect-302\\.map.*-m found"
      - {type: contains, target: map:ssl-redirect-302.map, pattern: "host01\\.example\\.com 1"}

  test-haproxytech-rate-limit-whitelist:
    description: >-
      haproxy.org/rate-limit-whitelist appends a trailing '!{ src <cidrs> }' guard
      to the rate-limit deny line so whitelisted sources are never denied.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rl-whitelist-ingress
            annotations:
              haproxy.org/rate-limit-requests: "10"
              haproxy.org/rate-limit-period: "10s"
              haproxy.org/rate-limit-whitelist: "10.0.0.0/8, 192.168.1.5"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rl.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "stick-table type string len 340 size 102400 expire 10s store http_req_rate\\(10s\\) peers localinstance"
      - {type: contains, target: "map:ing-rl-routes.map", pattern: '(?m)^default/rl-whitelist-ingress req 10s 10 403$'}
      - {type: contains, target: "map:ing-rl-allow-members.map", pattern: '(?m)^10\.0\.0\.0/8\|default/rl-whitelist-ingress 1$'}
      - {type: contains, target: "map:ing-rl-allow-members.map", pattern: '(?m)^192\.168\.1\.5/32\|default/rl-whitelist-ingress 1$'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request deny deny_status 403 if \{ var\(txn\.vrl_status\) -m str 403 \}'}
  test-haproxytech-rate-limit-size-zero-fails:
    description: >-
      A zero-magnitude rate-limit-size passes the format check but would reach
      the shared table as `size 0`, which HAProxy rejects only at config parse.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rl-zero-size
            annotations:
              haproxy.org/rate-limit-requests: "10"
              haproxy.org/rate-limit-size: "0"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rl-zero.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
    assertions:
      - {type: contains, target: rendering_error, pattern: "rate-limit-size' on Ingress 'default/rl-zero-size'. Must be a positive size"}

  test-haproxytech-rate-limit-no-whitelist:
    description: >-
      Without haproxy.org/rate-limit-whitelist the deny line must not carry a src guard
      (regression guard for the whitelist extension).
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rl-plain-ingress
            annotations:
              haproxy.org/rate-limit-requests: "10"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rl-plain.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: "map:ing-rl-routes.map", pattern: '(?m)^default/rl-plain-ingress req 1s 10 403$'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request deny deny_status 403 if \{ var\(txn\.vrl_status\) -m str 403 \}'}
      # Without a whitelist the allowlist block is not emitted at all.
      - {type: not_contains, target: haproxy.cfg, pattern: 'rate-limit-allowlist'}
  test-haproxytech-access-control-allow-list:
    description: >-
      haproxy.org/allow-list (the canonical upstream key) emits a src allow ACL.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ac-allow
            annotations:
              haproxy.org/allow-list: "10.0.0.0/8"
          spec:
            ingressClassName: haproxy
            rules:
              - host: ac-allow.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: "map:ing-ac-routes.map", pattern: '(?m)^default/ac-allow haproxytech:allow$'}
      - {type: contains, target: "map:ing-ac-allow.map", pattern: '(?m)^10\.0\.0\.0/8\|default/ac-allow 1$'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request deny if \{ var\(txn\.ac\) -m str "haproxytech:allow" "haproxytech:both" \} !\{ var\(txn\.ac_block\),concat\(\|,txn\.resource_id\),map\(\S*/ing-ac-allow\.map\) -m found \}'}

  test-haproxytech-access-control-deny-list:
    description: >-
      haproxy.org/deny-list (the canonical upstream key) emits a src deny ACL.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ac-deny
            annotations:
              haproxy.org/deny-list: "192.168.0.0/16"
          spec:
            ingressClassName: haproxy
            rules:
              - host: ac-deny.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: "map:ing-ac-routes.map", pattern: '(?m)^default/ac-deny haproxytech:deny$'}
      - {type: contains, target: "map:ing-ac-deny.map", pattern: '(?m)^192\.168\.0\.0/16\|default/ac-deny 1$'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request deny if \{ var\(txn\.ac\) -m str "haproxytech:deny" "haproxytech:both" \} \{ var\(txn\.ac_block\),concat\(\|,txn\.resource_id\),map\(\S*/ing-ac-deny\.map\) -m found \}'}

  test-haproxytech-access-control-deprecated-alias:
    description: >-
      The deprecated haproxy.org/whitelist alias still emits a src allow ACL when
      the canonical allow-list is absent (upstream parity).
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ac-wl
            annotations:
              haproxy.org/whitelist: "172.16.0.0/12"
          spec:
            ingressClassName: haproxy
            rules:
              - host: ac-wl.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: "map:ing-ac-routes.map", pattern: '(?m)^default/ac-wl haproxytech:allow$'}
      - {type: contains, target: "map:ing-ac-allow.map", pattern: '(?m)^172\.16\.0\.0/12\|default/ac-wl 1$'}

  test-haproxytech-ssl-redirect-port:
    description: >-
      haproxy.org/ssl-redirect-port redirects to https on the explicit port instead of
      the default scheme redirect.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ssl-redirect-port-ingress
            annotations:
              haproxy.org/ssl-redirect: "true"
              haproxy.org/ssl-redirect-port: "8443"
          spec:
            ingressClassName: haproxy
            rules:
              - host: secure.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: map:ssl-redirect-port-302.map, pattern: "secure.example.com 8443"}
      - type: contains
        target: haproxy.cfg
        pattern: "http-request redirect location https://%\\[hdr\\(host\\),field\\(1,:\\)\\]:%\\[var\\(txn.host\\),map\\([^)]*ssl-redirect-port-302\\.map\\)\\]%\\[capture.req.uri\\] code 302 if !\\{ ssl_fc \\}"
      - type: not_contains
        target: haproxy.cfg
        pattern: "redirect scheme https code 302 if.*ssl-redirect-302\\.map"

  test-haproxytech-ssl-redirect-no-port-default:
    description: >-
      Without haproxy.org/ssl-redirect-port the default scheme redirect path is used
      (regression guard for the port variant).
    extraContext: {ssl_redirect_default: "true"}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: ssl-redirect-no-port-ingress}
          spec:
            ingressClassName: haproxy
            rules:
              - host: plain.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "http-request redirect scheme https code 302 if"}
      - {type: not_contains, target: haproxy.cfg, pattern: "redirect location https://%\\[hdr\\(host\\)"}

  test-pod-maxconn-basic:
    description: pod-maxconn divides total by ready HAProxy pod count with power-of-2 quantization
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: test-ingress
            annotations:
              haproxy.org/pod-maxconn: "100"
          spec:
            rules:
              - host: example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
      haproxy-pods:
        - apiVersion: v1
          kind: Pod
          metadata: {namespace: haptic-system, name: haproxy-pod-1}
          status: *statusReady
        - apiVersion: v1
          kind: Pod
          metadata: {namespace: haptic-system, name: haproxy-pod-2}
          status: *statusReady
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "maxconn 50"}
      - {type: contains, target: haproxy.cfg, pattern: "pod-maxconn: 100 total / 2 ready pods"}

  test-pod-maxconn-only-counts-ready-pods:
    description: pod-maxconn ignores non-Running and non-Ready pods
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: test-ingress
            annotations:
              haproxy.org/pod-maxconn: "100"
          spec:
            rules:
              - host: example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
      haproxy-pods:
        - apiVersion: v1
          kind: Pod
          metadata: {namespace: haptic-system, name: haproxy-pod-1}
          status: *statusReady
        - apiVersion: v1
          kind: Pod
          metadata: {namespace: haptic-system, name: haproxy-pod-2}
          status: *statusReady
        - apiVersion: v1
          kind: Pod
          metadata: {namespace: haptic-system, name: haproxy-pod-3-pending}
          status: {phase: Pending}
        - apiVersion: v1
          kind: Pod
          metadata: {namespace: haptic-system, name: haproxy-pod-4-not-ready}
          status:
            phase: Running
            conditions:
              - {type: Ready, status: "False"}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "maxconn 50"}
      - {type: contains, target: haproxy.cfg, pattern: "2 ready pods"}
      - {type: not_contains, target: haproxy.cfg, pattern: "4 ready pods"}

  test-haproxytech-ssl-passthrough-named-port:
    description: haproxy.org/ssl-passthrough annotation must resolve a backend referenced by port.name to the correct numeric pod port (mirrors the original unifi-controller bug scenario)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {name: unifi, namespace: default}
          spec:
            ports:
              - {name: https-gui, port: 8443, targetPort: https-gui}
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: unifi-xyz
            namespace: default
            labels:
              kubernetes.io/service-name: unifi
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.42"]
              targetRef: {kind: Pod, name: unifi-0}
          ports:
            - {name: https-gui, port: 8443, protocol: TCP}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            name: unifi
            namespace: default
            annotations:
              haproxy.org/ssl-passthrough: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: unifi.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: unifi, port: {name: https-gui}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "server unifi-0 10.0.0.42:8443 "}
      - {type: not_contains, target: haproxy.cfg, pattern: "server unifi-0 10.0.0.42:80 "}

  test-haproxytech-check-disable:
    description: haproxy.org/check=false disables health checks via default-server no-check
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: nocheck-ingress
            annotations:
              haproxy.org/check: "false"
          spec:
            ingressClassName: haproxy
            rules:
              - host: nocheck.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "default-server no-check"}
      # Scoped to this test's own backend. A bare `default-server check` matches
      # any OTHER backend that legitimately health-checks — the Varnish cache
      # tier does — which made this assertion true only while the cache tier
      # happened to be disabled, and a load-gate crash-loop once it wasn't.
      - type: not_contains
        target: haproxy.cfg
        pattern: 'backend default_nocheck-ingress_svc_echo_80 from haptic-be-\w+\n\s+guid [^\n]*\n\s+default-server check'

  test-haproxytech-check-interval:
    description: haproxy.org/check-interval sets the inter parameter on default-server
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: interval-ingress
            annotations:
              haproxy.org/check-interval: "10s"
          spec:
            ingressClassName: haproxy
            rules:
              - host: interval.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "default-server check inter 10s"}

  test-haproxytech-scale-server-slots-tolerated:
    description: >-
      haproxy.org/scale-server-slots no longer has any effect (servers are named
      after pods, no slot pool). The annotation is tolerated — the render
      succeeds and emits no slot placeholders — and warned (a chart fail on a
      tenant value would abort the whole render).
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: slots-ingress
            annotations:
              haproxy.org/scale-server-slots: "5"
          spec:
            ingressClassName: haproxy
            rules:
              - host: slots.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: echo, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "backend default_slots-ingress_svc_echo_80 from haptic-be-"}
      - {type: not_contains, target: haproxy.cfg, pattern: "server SRV_[0-9]"}

  # ---------------------------------------------------------------------------
  # Config-injection guards (#157, phase 2). The centralized utilities are behavior-
  # tested once in base/library.yaml (phase 1); these are wiring checks. One hostile
  # fixture drives every routing guard at once (a shared ZZINJECT marker must never
  # reach the config); positive fixtures prove the load-bearing special cases
  # (single-quoted regex origin, %→%% header value, \N path backref, escaped realm);
  # the security sites fail closed.
  # ---------------------------------------------------------------------------

  test-haproxytech-injection-routing-warns-and-skips:
    description: Every haproxy.org routing value carrying an injection payload is skipped (reconcile), so the ZZINJECT marker never reaches the config; a single unguarded site would leak it.
    extraContext: {renderMode: reconcile}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: inj-a
            annotations:
              haproxy.org/cors-enable: "true"
              haproxy.org/cors-allow-origin: "https://ok.example.com"
              haproxy.org/cors-allow-methods: "GET\nZZINJECT"
              haproxy.org/timeout-server: "30s\nZZINJECT"
              haproxy.org/load-balance: "hdr(x-session-id)\nZZINJECT"
              haproxy.org/check-http: "/h\nZZINJECT"
              haproxy.org/check-interval: "10s\nZZINJECT"
              haproxy.org/cookie-persistence: "SID\nZZINJECT"
              haproxy.org/path-rewrite: "/a\nZZINJECT"
              haproxy.org/server-proto: "h2\nZZINJECT"
              haproxy.org/request-set-header: "X-Inj ZZINJECT\tval"
              haproxy.org/response-set-header: "Y-Inj ZZINJECT\tval"
              haproxy.org/request-capture: "X-Cap ZZINJECT"
          spec: *specTestExampleComRoot
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: inj-b
            annotations:
              haproxy.org/cookie-persistence-no-dynamic: "SID\nZZINJECT"
              haproxy.org/request-capture: "X-ValidCap"
              haproxy.org/request-capture-len: "5\nZZINJECT"
          spec:
            ingressClassName: haproxy
            rules:
              - host: inj-b.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
    assertions:
      - type: haproxy_valid
      - {type: not_contains, target: haproxy.cfg, pattern: "ZZINJECT"}

  test-haproxytech-guarded-values-render:
    description: Legit values still render correctly through the new guards, including the load-bearing cases — %→%% in a header value, a \N path backreference, and a regex CORS origin whose backslashes reach its map row intact.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ok-be
            annotations:
              haproxy.org/timeout-server: "30s"
              haproxy.org/load-balance: "leastconn"
              haproxy.org/check-interval: "10s"
              haproxy.org/cookie-persistence: "SESSIONID"
              haproxy.org/server-proto: "h2"
              haproxy.org/check-http: "GET /healthz HTTP/1.1"
              haproxy.org/src-ip-header: "X-Real-IP"
              haproxy.org/request-capture: "X-Forwarded-For"
              haproxy.org/request-capture-len: "256"
              haproxy.org/request-set-header: "X-Pct a%b"
              haproxy.org/path-rewrite: "/api/(.*) /v2/\\1"
          spec: *specTestExampleComRoot
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ok-cors
            annotations:
              haproxy.org/cors-enable: "true"
              haproxy.org/cors-allow-origin: "https://.*\\.example\\.com"
              haproxy.org/cors-allow-methods: "GET, POST"
              haproxy.org/cors-allow-headers: "X-H%K"
          spec:
            ingressClassName: haproxy
            rules:
              - host: ok-cors.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
    assertions:
      - type: haproxy_valid
      # The settable server timeout lives as ms in backend-timeouts.map (reload-free).
      - {type: contains, target: 'map:backend-timeouts.map', pattern: 'default_ok-be_svc_echo_80\|server 30000'}
      - {type: not_contains, target: haproxy.cfg, pattern: "timeout server 30s"}
      - {type: contains, target: haproxy.cfg, pattern: "balance leastconn"}
      - {type: contains, target: haproxy.cfg, pattern: "default-server check inter 10s"}
      - {type: contains, target: haproxy.cfg, pattern: "cookie SESSIONID insert indirect nocache dynamic"}
      - {type: contains, target: haproxy.cfg, pattern: "proto h2"}
      - {type: contains, target: haproxy.cfg, pattern: "option httpchk GET /healthz HTTP/1.1"}
      - {type: contains, target: "map:ing-src-ip-routes.map", pattern: '(?m)^default/ok-be haproxytech:X-Real-IP$'}
      - {type: contains, target: haproxy.cfg, pattern: 'set-src hdr\(X-Real-IP\) if \{ var\(txn\.resource_id\),map\(\S*/ing-src-ip-routes\.map\) -m str "haproxytech:X-Real-IP" \}'}
      - {type: contains, target: haproxy.cfg, pattern: "capture request header X-Forwarded-For len 256"}
      # Header manipulation is reload-free: the value lives URL-encoded in
      # ing-reqhdr.map (a%b -> a%25b), read by one static line keyed on the backend.
      - {type: contains, target: 'map:ing-reqhdr.map', pattern: 'default_ok-be_svc_echo_80\|set\|x-pct a%25b'}
      - {type: contains, target: haproxy.cfg, pattern: 'set-header X-Pct %\[var\(txn\.backend_name\),concat\(\|set\|x-pct\),map\([^)]*ing-reqhdr\.map\),url_dec\(1\)\]'}
      - {type: not_contains, target: haproxy.cfg, pattern: 'set-header X-Pct "a'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request replace-path /api/\(\.\*\) /v2/\\1'}
      - {type: contains, target: "map:ing-cors-regex.map", pattern: '(?m)^\^default/ok-cors\\\|\.\*\(https://\.\*\\\.example\\\.com\) 1$'}
      - {type: contains, target: "map:ing-cors-methods.map", pattern: '(?m)^default/ok-cors GET%2c%20POST$'}
      - {type: contains, target: "map:ing-cors-headers.map", pattern: '(?m)^default/ok-cors X-H%25K$'}

  test-haproxytech-request-capture-shared-pair-emitted-once:
    description: Two Ingresses capturing the same header with the same length yield one capture slot per frontend; a second length is a second slot.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cap-a
            annotations:
              haproxy.org/request-capture: "X-Trace-Id"
              haproxy.org/request-capture-len: "64"
          spec:
            ingressClassName: haproxy
            rules:
              - host: cap-a.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cap-b
            annotations:
              haproxy.org/request-capture: |
                X-Trace-Id
                X-Tenant
              haproxy.org/request-capture-len: "64"
          spec:
            ingressClassName: haproxy
            rules:
              - host: cap-b.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cap-c
            annotations:
              haproxy.org/request-capture: "X-Trace-Id"
              haproxy.org/request-capture-len: "128"
          spec:
            ingressClassName: haproxy
            rules:
              - host: cap-c.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "capture request header X-Trace-Id len 64"}
      - {type: contains, target: haproxy.cfg, pattern: "capture request header X-Tenant len 64"}
      - {type: contains, target: haproxy.cfg, pattern: "capture request header X-Trace-Id len 128"}
      # The shared pair is one slot per frontend: the sorted slots never repeat a line.
      - {type: not_contains, target: haproxy.cfg, pattern: 'capture request header X-Trace-Id len 64\n\s*capture request header X-Trace-Id len 64'}

  test-haproxytech-path-rewrite-map-lane:
    description: haproxy.org/path-rewrite prefix strips and the bare form ride the frontend maps; a pattern that is not a prefix strip keeps its backend replace-path.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: pr-strip
            annotations:
              haproxy.org/path-rewrite: "^/api/v1/(.*) /\\1"
          spec:
            ingressClassName: haproxy
            rules:
              - host: strip.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: pr-bare
            annotations:
              haproxy.org/path-rewrite: "/backend"
          spec:
            ingressClassName: haproxy
            rules:
              - host: bare.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: pr-regex
            annotations:
              haproxy.org/path-rewrite: "/api/(.*) /v2/\\1"
          spec:
            ingressClassName: haproxy
            rules:
              - host: regex.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: 'http-request set-var\(txn\.ht_rw_in\) path if \{ var\(txn\.resource_id\),map\(\S*/haproxytech-rewrite-routes\.map\) -m found \}'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request set-var\(txn\.ht_rw_v\) var\(txn\.resource_id\),concat\(\|,txn\.ht_rw_in\),map_beg\(\S*/haproxytech-rewrite-prefix\.map\) if \{ var\(txn\.ht_rw_in\) -m found \}'}
      - {type: contains, target: haproxy.cfg, pattern: "http-request set-path %\\[var\\(txn\\.ht_rw_v\\),field\\(2,' ',0\\)\\]%\\[var\\(txn\\.ht_rw_rest\\)\\] if \\{ var\\(txn\\.ht_rw_v\\) -m found \\}"}
      - {type: contains, target: "map:haproxytech-rewrite-routes.map", pattern: '(?m)^default/pr-strip 1$'}
      - {type: contains, target: "map:haproxytech-rewrite-routes.map", pattern: '(?m)^default/pr-bare 1$'}
      - {type: not_contains, target: "map:haproxytech-rewrite-routes.map", pattern: 'pr-regex'}
      - {type: contains, target: "map:haproxytech-rewrite-prefix.map", pattern: '(?m)^default/pr-strip\|/api/v1/ 8 /$'}
      - {type: contains, target: "map:haproxytech-rewrite-prefix.map", pattern: '(?m)^default/pr-bare\| 1048576 /backend$'}
      - {type: contains, target: haproxy.cfg, pattern: 'http-request replace-path /api/\(\.\*\) /v2/\\1'}
      - {type: not_contains, target: haproxy.cfg, pattern: 'replace-path \^/api/v1/'}
      - {type: not_contains, target: haproxy.cfg, pattern: 'replace-path \(\.\*\) /backend'}

  test-haproxytech-cors-origin-single-quote-rejected:
    description: A single quote in the regex cors-allow-origin is denied at admission (it would close the strong-quoted -m reg operand).
    extraContext: {renderMode: admission}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cors-origin-quote
            annotations:
              haproxy.org/cors-enable: "true"
              haproxy.org/cors-allow-origin: "https://ok.example.com' or { always_true } '"
          spec: *specTestExampleComRoot
    assertions:
      - {type: contains, target: rendering_error, pattern: "would split the frontend directive"}

  test-haproxytech-src-ip-header-fails-closed:
    description: A control character in src-ip-header fails closed (set-src rewrites the trusted client address; skipping is not an option).
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: srcip-bad, annotations: {haproxy.org/src-ip-header: "X-Real-IP\nZZINJECT"}}
          spec: *specTestExampleComRoot
    assertions:
      - {type: contains, target: rendering_error, pattern: "haproxy.org/src-ip-header"}
      - {type: contains, target: rendering_error, pattern: "break the set-src directive"}

  test-haproxytech-rate-limit-fails-closed:
    description: A control character in a rate-limit field fails closed (rate limiting must stay enforceable).
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rl-bad
            annotations:
              haproxy.org/rate-limit-requests: "10"
              haproxy.org/rate-limit-period: "10s\nZZINJECT"
          spec: *specTestExampleComRoot
    assertions:
      - {type: contains, target: rendering_error, pattern: "haproxy.org/rate-limit-period"}
      - {type: contains, target: rendering_error, pattern: "Rate limiting must stay enforceable"}

  test-haproxytech-auth-realm-fails-closed:
    description: A control character in auth-realm fails closed (auth is security-critical).
    fixtures:
      secrets:
        - {apiVersion: v1, kind: Secret, metadata: {namespace: default, name: realm-auth}, type: Opaque, data: {admin: JDJ5JDA1JG1OMVdWazVRbmJnNFF3ZEFkWGJmei44YjNjZUg2UTVLT1ZDS3hSMklrTkFmSmdMaTVwSUtX}}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: realm-bad
            annotations: {haproxy.org/auth-type: basic-auth, haproxy.org/auth-secret: realm-auth, haproxy.org/auth-realm: "Prod\nZZINJECT"}
          spec: *specTestExampleComRoot
    assertions:
      - {type: contains, target: rendering_error, pattern: "haproxy.org/auth-realm"}

  test-haproxytech-auth-realm-quote-escaped:
    description: A double quote in auth-realm is escaped inside the quoted realm string.
    fixtures:
      secrets:
        - {apiVersion: v1, kind: Secret, metadata: {namespace: default, name: realm-auth2}, type: Opaque, data: {admin: JDJ5JDA1JG1OMVdWazVRbmJnNFF3ZEFkWGJmei44YjNjZUg2UTVLT1ZDS3hSMklrTkFmSmdMaTVwSUtX}}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: realm-quote
            annotations: {haproxy.org/auth-type: basic-auth, haproxy.org/auth-secret: realm-auth2, haproxy.org/auth-realm: "My\"Realm"}
          spec: *specTestExampleComRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: 'auth realm "My\\"Realm"'}

  test-haproxytech-server-tls-warning-name-free:
    description: Missing server-ca/server-crt Secrets render name-free warning comments (the tenant Secret name goes only to the Event), so a newline in the reference cannot inject a directive.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: ca-missing, annotations: {haproxy.org/server-ca: "absent-ca\nZZINJECT"}}
          spec: *specTestExampleComRoot
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata: {namespace: default, name: crt-missing, annotations: {haproxy.org/server-crt: "absent-crt\nZZINJECT"}}
          spec:
            ingressClassName: haproxy
            rules:
              - host: crt.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: echo, port: {number: 80}}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "server-ca: referenced Secret not found; upstream verification skipped"}
      - {type: contains, target: haproxy.cfg, pattern: "server-crt: referenced Secret not found; no client certificate presented"}
      - {type: not_contains, target: haproxy.cfg, pattern: "absent-ca"}
      - {type: not_contains, target: haproxy.cfg, pattern: "absent-crt"}
      - {type: not_contains, target: haproxy.cfg, pattern: "ZZINJECT"}

# Consumed by the playground's migration report and the generated tables in
# docs/site/docs/migrating.md; scripts/check-migration-coverage.sh pins drift.
# Upstream haproxytech/kubernetes-ingress also reads many of these on Services and
# ConfigMaps — HAPTIC reads Ingress annotations only, so those don't carry over.
_migrationCoverage:
  - source: haproxytech
    detect:
      ingressClasses: ["haproxy"]
      annotationPrefixes: ["haproxy.org/"]
    annotations:
      haproxy.org/timeout-server:
        status: supported
        note: Mapped to HAProxy's timeout server.
      haproxy.org/timeout-connect:
        status: supported
        note: Mapped to HAProxy's timeout connect.
      haproxy.org/timeout-queue:
        status: supported
        note: Mapped to HAProxy's timeout queue.
      haproxy.org/timeout-tunnel:
        status: supported
        note: Mapped to HAProxy's timeout tunnel.
      haproxy.org/timeout-check:
        status: supported
        note: Mapped to HAProxy's timeout check.
      haproxy.org/load-balance:
        status: supported
        note: roundrobin, static-rr, leastconn, first, source and random, plus parameterized uri/url_param/hdr/rdp-cookie, are accepted; other values fail the render.
      haproxy.org/pod-maxconn:
        status: different
        note: Divided across the number of ready HAProxy pods (quantized to a power of two) rather than applied per-server verbatim; must be a positive integer.
        doc: annotation-support
      haproxy.org/check:
        status: supported
        note: '"true"/"false" toggle health checks; other values fail the render.'
      haproxy.org/check-http:
        status: supported
        note: Emits option httpchk (a bare path becomes GET <path>).
      haproxy.org/check-interval:
        status: supported
        note: Sets the check interval (inter); ignored when check is "false".
      haproxy.org/rate-limit-requests:
        status: supported
        note: Deny threshold that enables rate limiting; exceeding it returns the rate-limit-status-code.
      haproxy.org/rate-limit-period:
        status: supported
        note: Stick-table window (default 1s).
      haproxy.org/rate-limit-size:
        status: supported
        note: Stick-table size (default 100k).
      haproxy.org/rate-limit-status-code:
        status: supported
        note: Deny status (default 403).
      haproxy.org/rate-limit-whitelist:
        status: supported
        note: Comma-separated CIDRs never rate-limited; invalid CIDRs fail the render.
      haproxy.org/ssl-redirect:
        status: supported
        note: '"true" redirects HTTP to HTTPS; the default can be flipped via extraContext.ssl_redirect_default.'
      haproxy.org/ssl-redirect-code:
        status: supported
        note: Redirect status code (default 302).
      haproxy.org/ssl-redirect-port:
        status: supported
        note: Redirects to HTTPS on an explicit port, preserving the request URI, via a reload-free map.
      haproxy.org/request-redirect:
        status: supported
        note: Host-scoped redirect to a URL via a reload-free map.
      haproxy.org/request-redirect-code:
        status: different
        note: Default 302; an invalid code fails the render.
        doc: annotation-support
      haproxy.org/set-host:
        status: supported
        note: Overrides the upstream Host header via a reload-free map.
      haproxy.org/request-set-header:
        status: supported
        note: Newline-separated "name value" pairs set on requests.
      haproxy.org/response-set-header:
        status: supported
        note: Newline-separated "name value" pairs set on responses.
      haproxy.org/path-rewrite:
        status: supported
        note: A single "<from> <to>" pair maps to replace-path; a bare value rewrites the whole path.
      haproxy.org/src-ip-header:
        status: different
        note: Rewrites the source IP from the named header (set-src) for the route, from a per-route map.
        doc: annotation-support
      haproxy.org/request-capture:
        status: supported
        note: Newline-separated fetch expressions captured into the log.
      haproxy.org/request-capture-len:
        status: supported
        note: Capture length for request-capture (default 128).
      haproxy.org/cookie-persistence:
        status: supported
        note: Dynamic cookie-based stickiness.
      haproxy.org/cookie-persistence-no-dynamic:
        status: different
        note: Static (non-dynamic) cookie stickiness; setting it together with cookie-persistence fails the render.
        doc: annotation-support
      haproxy.org/auth-type:
        status: different
        note: Only "basic-auth" is supported; other values fail the render (note the value differs from ingress-nginx's "basic").
        doc: annotation-support
      haproxy.org/auth-secret:
        status: different
        note: Secret format is one key per username with a base64(hash) value — different from ingress-nginx's htpasswd. A missing Secret makes the route serve 503 until it appears, and writing such an Ingress is rejected by the admission webhook.
        doc: annotation-support
      haproxy.org/auth-realm:
        status: different
        note: Default "Protected-Content" (matching the upstream controller); spaces in the realm are replaced with dashes, as upstream does.
        doc: annotation-support
      haproxy.org/send-proxy-protocol:
        status: different
        note: proxy, proxy-v1, proxy-v2, proxy-v2-ssl and proxy-v2-ssl-cn map to the matching send-proxy flags; any other value is silently ignored.
        doc: annotation-support
      haproxy.org/server-ssl:
        status: supported
        note: '"true" enables TLS to the upstream (ssl alpn h2,http/1.1 verify none).'
      haproxy.org/server-proto:
        status: supported
        note: Sets the upstream ALPN/proto (e.g. h2).
      haproxy.org/server-ca:
        status: different
        note: Verifies the upstream certificate against the Secret's ca.crt; a missing Secret or missing ca.crt renders a warning comment and silently skips verification instead of failing.
        doc: annotation-support
      haproxy.org/server-crt:
        status: different
        note: Presents a client certificate to the upstream from the Secret; a missing Secret or missing tls.crt/tls.key renders a warning comment and skips the client cert instead of failing.
        doc: annotation-support
      haproxy.org/scale-server-slots:
        status: dropped
        note: Inert; servers are named after their pods (ADR-0011), so there is no slot pool to size. Setting it emits an UnsupportedAnnotation Warning Event.
      haproxy.org/backend-config-snippet:
        status: supported
        note: Raw HAProxy directives injected into the backend section.
      haproxy.org/ssl-passthrough:
        status: supported
        note: SNI-based TCP passthrough; HTTP-level annotations don't apply to passthrough hosts.
      haproxy.org/forwarded-for:
        status: supported
        note: '"true" enables option forwardfor; "false" disables it; other values fail the render.'
      haproxy.org/allow-list:
        status: different
        note: Host-scoped source-IP allowlist — only gates rules with an explicit host; invalid CIDRs fail the render.
        doc: annotation-support
      haproxy.org/deny-list:
        status: different
        note: Host-scoped source-IP denylist — only gates rules with an explicit host; invalid CIDRs fail the render.
        doc: annotation-support
      haproxy.org/whitelist:
        status: different
        note: Deprecated alias of allow-list, honoured only when allow-list is absent; host-scoped.
        doc: annotation-support
      haproxy.org/blacklist:
        status: different
        note: Deprecated alias of deny-list, honoured only when deny-list is absent; host-scoped.
        doc: annotation-support
      haproxy.org/cors-enable:
        status: supported
        note: Emits the Access-Control-* headers via http-after-response (matching the upstream controller). By default OPTIONS preflights flow to the backend; set cors-respond-to-options "true" to have HAProxy answer them with a 204.
        doc: annotation-support
      haproxy.org/cors-allow-origin:
        status: supported
        note: The value is a regex matched against the request Origin; the Allow-Origin header echoes the matched origin (default "*" allows any). Combining "*" with cors-allow-credentials "true" fails the render.
        doc: annotation-support
      haproxy.org/cors-allow-methods:
        status: supported
        note: Default "*", matching the upstream controller.
        doc: annotation-support
      haproxy.org/cors-allow-headers:
        status: supported
        note: Allowed request headers for preflight responses (default "*").
      haproxy.org/cors-allow-credentials:
        status: supported
        note: Only emitted when "true"; combining it with cors-allow-origin "*" fails the render.
        doc: annotation-support
      haproxy.org/cors-max-age:
        status: supported
        note: Preflight cache duration in seconds; default 5. Unlike the upstream controller, HAPTIC takes a plain integer (seconds), not an HAProxy duration string.
      haproxy.org/cors-respond-to-options:
        status: supported
        note: When "true", HAProxy answers the CORS preflight (OPTIONS) with a 204 instead of forwarding it to the backend, matching the upstream controller.
      haproxy.org/timeout-client:
        status: dropped
        note: A frontend-level timeout owned by HAPTIC's shared frontend, not settable per Ingress.
        doc: annotation-support
      haproxy.org/timeout-http-request:
        status: dropped
        note: A frontend-level timeout owned by HAPTIC's shared frontend, not settable per Ingress.
        doc: annotation-support
      haproxy.org/timeout-http-keep-alive:
        status: dropped
        note: A frontend-level timeout owned by HAPTIC's shared frontend, not settable per Ingress.
        doc: annotation-support
      haproxy.org/standalone-backend:
        status: dropped
        note: Not implemented; HAPTIC always shares the backend model.
        doc: annotation-support
