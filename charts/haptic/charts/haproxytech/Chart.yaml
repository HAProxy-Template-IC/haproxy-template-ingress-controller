apiVersion: v2
name: haproxytech
description: HAProxy (haproxy.org/*) annotation-compat library for HAPTIC. Carries only library YAML; the parent chart merges it into the HAProxyTemplateConfig.
version: 0.1.0
