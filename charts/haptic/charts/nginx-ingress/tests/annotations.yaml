# nginx-ingress library — annotation validation tests.
# Backend directives, frontend filters, session affinity, basic auth, and
# SSL passthrough.
_fx:
  httpRoot: &httpRoot
    paths:
      - path: /
        pathType: Prefix
        backend: {service: {name: echo, port: {number: 80}}}

validationTests:
  # ==========================================================================
  # Backend Directive Tests
  # ==========================================================================

  test-nginx-ingress-timeouts:
    description: Nginx Ingress timeout annotations generate correct config
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: timeout-ingress
            annotations:
              nginx.ingress.kubernetes.io/proxy-connect-timeout: "10"
              nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
          spec:
            ingressClassName: haproxy
            rules:
              - host: timeout.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "timeout connect 10s"}
      # read/send timeout collapses into the settable server timeout, stored as ms
      # in backend-timeouts.map (reload-free) rather than a per-backend directive.
      - {type: contains, target: 'map:backend-timeouts.map', pattern: 'default_timeout-ingress_svc_echo_80\|server 60000'}
      - {type: not_contains, target: haproxy.cfg, pattern: "timeout server 60s"}

  test-nginx-ingress-load-balance:
    description: Nginx Ingress load-balance annotation maps to HAProxy algorithm
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: lb-ingress
            annotations:
              nginx.ingress.kubernetes.io/load-balance: "least_conn"
          spec:
            ingressClassName: haproxy
            rules:
              - host: lb.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "balance leastconn"}

  test-nginx-ingress-ssl-redirect:
    description: Nginx Ingress ssl-redirect annotation generates 308 redirect (matches ingress-nginx)
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: redirect-ingress
            annotations:
              nginx.ingress.kubernetes.io/ssl-redirect: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: redirect.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "redirect scheme https code 308"}
      # Relocated to the shared host map (reload-free host add/remove).
      - {type: contains, target: map:ssl-redirect-308.map, pattern: "redirect.example.com 1"}
      - {type: not_contains, target: haproxy.cfg, pattern: "var\\(txn.host\\) -m str redirect.example.com"}

  test-nginx-ingress-ssl-redirect-code-override:
    description: ssl-redirect honours the nginxHttpRedirectCode extraContext override
    extraContext: {nginxHttpRedirectCode: "301"}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: redirect-ingress
            annotations:
              nginx.ingress.kubernetes.io/ssl-redirect: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: redirect.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "redirect scheme https code 301"}
      - {type: contains, target: map:ssl-redirect-301.map, pattern: "redirect.example.com 1"}

  test-nginx-ingress-force-ssl-redirect:
    description: Nginx Ingress force-ssl-redirect annotation generates 308 redirect
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: force-redirect-ingress
            annotations:
              nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: force-redirect.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "redirect scheme https code 308"}
      - {type: contains, target: map:ssl-redirect-308.map, pattern: "force-redirect.example.com 1"}

  test-nginx-ingress-cors:
    description: Nginx Ingress CORS annotations generate correct headers
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cors-ingress
            annotations:
              nginx.ingress.kubernetes.io/enable-cors: "true"
              nginx.ingress.kubernetes.io/cors-allow-origin: "https://example.com"
          spec:
            ingressClassName: haproxy
            rules:
              - host: cors.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      # The request Origin is captured and matched against the allow-list; the
      # Allow-Origin header echoes the matched origin (never the raw annotation),
      # matching ingress-nginx. Headers ride on http-after-response so they also
      # apply to the synthetic preflight 204. (contains patterns are regex.)
      - type: contains
        target: haproxy.cfg
        pattern: "http-request set-var\\(txn\\.cors_origin\\) req\\.hdr\\(Origin\\)"
      - type: contains
        target: haproxy.cfg
        pattern: "acl cors_origin_\\S+ var\\(txn\\.cors_origin\\) -m str https://example\\.com"
      - type: contains
        target: haproxy.cfg
        pattern: "http-after-response set-header Access-Control-Allow-Origin '%\\[var\\(txn\\.cors_origin\\)\\]'"
      - {type: contains, target: haproxy.cfg, pattern: "http-after-response add-header Vary Origin"}
      - {type: contains, target: haproxy.cfg, pattern: "status 204 .*METH_OPTIONS"}

  test-nginx-ingress-cors-multiorigin:
    description: cors-allow-origin accepts a comma-separated allow-list with single-level wildcards; exact origins go to -m str, wildcards compile to anchored -m reg
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cors-ingress
            annotations:
              nginx.ingress.kubernetes.io/enable-cors: "true"
              nginx.ingress.kubernetes.io/cors-allow-origin: "https://app.example.com, https://*.example.com"
          spec:
            ingressClassName: haproxy
            rules:
              - host: cors.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      # Exact origin -> -m str; single-level wildcard -> anchored -m reg; both on
      # the same named ACL (OR). The matched request Origin is echoed back.
      - type: contains
        target: haproxy.cfg
        pattern: "acl cors_origin_\\S+ var\\(txn\\.cors_origin\\) -m str https://app\\.example\\.com"
      - type: contains
        target: haproxy.cfg
        pattern: "var\\(txn\\.cors_origin\\) -m reg .*A-Za-z0-9.*example"
      - type: contains
        target: haproxy.cfg
        pattern: "http-after-response set-header Access-Control-Allow-Origin '%\\[var\\(txn\\.cors_origin\\)\\]'"

  test-nginx-ingress-access-control:
    description: Nginx Ingress whitelist-source-range creates ACL and deny rule
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: allow-ingress
            annotations:
              nginx.ingress.kubernetes.io/whitelist-source-range: "10.0.0.0/8,192.168.0.0/16"
          spec:
            ingressClassName: haproxy
            rules:
              - host: allow.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "acl ni_allowlist_default_allow-ingress src"}
      - {type: contains, target: haproxy.cfg, pattern: "10.0.0.0/8"}
      - {type: contains, target: haproxy.cfg, pattern: "http-request deny"}

  test-nginx-ingress-hsts:
    description: Nginx Ingress HSTS annotations generate correct header
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: hsts-ingress
            annotations:
              nginx.ingress.kubernetes.io/hsts: "true"
              nginx.ingress.kubernetes.io/hsts-max-age: "31536000"
              nginx.ingress.kubernetes.io/hsts-include-subdomains: "true"
              nginx.ingress.kubernetes.io/hsts-preload: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: hsts.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - type: contains
        target: map:hsts.map
        pattern: "hsts.example.com max-age%3d31536000%3b%20includeSubDomains%3b%20preload"
      - type: contains
        target: haproxy.cfg
        pattern: "http-response set-header Strict-Transport-Security %\\[var\\(txn.host\\),map\\([^)]*hsts\\.map\\),url_dec\\(1\\)\\] if \\{ ssl_fc \\}"

  test-nginx-ingress-backend-protocol-https:
    description: Nginx Ingress backend-protocol HTTPS adds ssl to server options
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: https-service}
          spec:
            ports:
              - port: 443
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: https-service-abc
            namespace: default
            labels:
              kubernetes.io/service-name: https-service
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.1"]
          ports:
            - port: 8443
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: https-ingress
            annotations:
              nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
          spec:
            ingressClassName: haproxy
            rules:
              - host: https.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: https-service, port: {number: 443}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "ssl verify none"}

  test-nginx-ingress-backend-protocol-grpc:
    description: Nginx Ingress backend-protocol GRPC adds proto h2 to server options
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: grpc-service}
          spec:
            ports:
              - port: 50051
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: grpc-service-abc
            namespace: default
            labels:
              kubernetes.io/service-name: grpc-service
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.1"]
          ports:
            - port: 50051
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: grpc-ingress
            annotations:
              nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
          spec:
            ingressClassName: haproxy
            rules:
              - host: grpc.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: grpc-service, port: {number: 50051}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "proto h2"}

  test-nginx-ingress-session-affinity:
    description: Nginx Ingress cookie affinity generates cookie directive
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: affinity-ingress
            annotations:
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-name: "SERVERID"
          spec:
            ingressClassName: haproxy
            rules:
              - host: affinity.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "cookie SERVERID insert indirect nocache dynamic"}

  test-nginx-ingress-session-cookie-hash-ignored:
    description: session-cookie-hash is not configurable in HAProxy and surfaces a warning instead of being silently ignored
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: hash-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: hash-ingress
            annotations:
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-hash: "sha1"
          spec:
            ingressClassName: haproxy
            rules:
              - host: hash.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: hash-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "Warning:.*session-cookie-hash"}

  test-nginx-ingress-session-cookie-domain-maxage:
    description: session-cookie-domain and session-cookie-max-age map to the cookie domain / Max-Age attributes
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: sc-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: sc-ingress
            annotations:
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-name: "SRVID"
              nginx.ingress.kubernetes.io/session-cookie-domain: "example.com"
              nginx.ingress.kubernetes.io/session-cookie-max-age: "3600"
          spec:
            ingressClassName: haproxy
            rules:
              - host: sc.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: sc-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "cookie SRVID insert.* domain example.com"}
      - {type: contains, target: haproxy.cfg, pattern: "cookie SRVID insert.* attr Max-Age=3600"}

  test-nginx-ingress-session-cookie-expires:
    description: legacy session-cookie-expires maps to attr Max-Age when session-cookie-max-age is absent
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: sce-service}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: sce-ingress
            annotations:
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-name: "SRVID"
              nginx.ingress.kubernetes.io/session-cookie-expires: "7200"
          spec:
            ingressClassName: haproxy
            rules:
              - host: sce.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: sce-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "cookie SRVID insert.* attr Max-Age=7200"}

  test-nginx-ingress-session-cookie-maxage-precedence:
    description: session-cookie-max-age takes precedence over session-cookie-expires when both are set
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: scp-service}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: scp-ingress
            annotations:
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-name: "SRVID"
              nginx.ingress.kubernetes.io/session-cookie-max-age: "100"
              nginx.ingress.kubernetes.io/session-cookie-expires: "999"
          spec:
            ingressClassName: haproxy
            rules:
              - host: scp.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: scp-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "cookie SRVID insert.* attr Max-Age=100"}
      - {type: not_contains, target: haproxy.cfg, pattern: "Max-Age=999"}

  test-nginx-ingress-session-cookie-maxage-invalid:
    description: a non-integer session-cookie-max-age aborts the render
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: sci-service}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: sci-ingress
            annotations:
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-max-age: "abc"
          spec:
            ingressClassName: haproxy
            rules:
              - host: sci.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: sci-service, port: {number: 80}}}
    assertions:
      - {type: contains, target: rendering_error, pattern: "Must be a non-negative integer"}

  test-nginx-ingress-session-cookie-secure-samesite:
    description: session-cookie-secure and session-cookie-samesite add Secure / SameSite cookie attrs
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: cookieattr-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: cookieattr-ingress
            annotations:
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-name: "AFFINITY"
              nginx.ingress.kubernetes.io/session-cookie-secure: "true"
              nginx.ingress.kubernetes.io/session-cookie-samesite: "Strict"
          spec:
            ingressClassName: haproxy
            rules:
              - host: cookieattr.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: cookieattr-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "cookie AFFINITY insert indirect nocache dynamic attr Secure attr SameSite=Strict"

  test-nginx-ingress-auth-basic-missing-secret:
    description: >-
      nginx.ingress.kubernetes.io/auth-secret referencing a Secret absent from
      the render snapshot (informer lag / cascade-delete) must skip the userlist
      + auth ACL cleanly, not panic. Regression for the typed-nil gotcha
      (GetSingle returns a typed-nil *resources.secrets.T when absent; boxing it
      through shared.ComputeIfAbsent's func() any made `!= nil` pass, then
      `.Data["auth"]` dereferenced nil).
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: auth-ingress-missing-secret
            annotations:
              nginx.ingress.kubernetes.io/auth-type: "basic"
              nginx.ingress.kubernetes.io/auth-secret: "absent-secret"
          spec:
            ingressClassName: haproxy
            rules:
              - host: auth-missing.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: not_contains, target: haproxy.cfg, pattern: "userlist ni_auth_default_absent-secret"}
      - {type: not_contains, target: haproxy.cfg, pattern: "http_auth(ni_auth_default_absent-secret)"}

  test-nginx-ingress-auth-basic:
    description: Nginx Ingress basic auth generates userlist and auth directive
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: my-auth}
          type: Opaque
          data:
            auth: YWRtaW46JDJ5JDA1JG1PMVdWazVRbmJnNFF3ZEFkWGJmei44YjNjZUg2UTVLT1ZDS3hSMklrTkFmSmdMaTVwSUtXCg==
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: auth-ingress
            annotations:
              nginx.ingress.kubernetes.io/auth-type: "basic"
              nginx.ingress.kubernetes.io/auth-secret: "my-auth"
          spec:
            ingressClassName: haproxy
            rules:
              - host: auth.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "userlist ni_auth_default_my-auth"}
      - {type: contains, target: haproxy.cfg, pattern: "user admin password"}
      - {type: contains, target: haproxy.cfg, pattern: "http-request auth"}

  test-nginx-ingress-auth-secret-type-map:
    description: auth-secret-type=auth-map reads usernames from Secret data keys (values = password hashes)
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: authmap-secret}
          type: Opaque
          data: {admin: JDJ5JDA1JG1PMVdWazVRbmJnNFF3ZEFkWGJmei44YjNjZUg2UTVLT1ZDS3hSMklrTkFmSmdMaTVwSUtX}
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: authmap-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: authmap-ingress
            annotations:
              nginx.ingress.kubernetes.io/auth-type: "basic"
              nginx.ingress.kubernetes.io/auth-secret: "authmap-secret"
              nginx.ingress.kubernetes.io/auth-secret-type: "auth-map"
          spec:
            ingressClassName: haproxy
            rules:
              - host: authmap.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: authmap-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "userlist ni_auth_default_authmap-secret"}
      - {type: contains, target: haproxy.cfg, pattern: "user admin password"}

  test-nginx-ingress-auth-secret-type-map-invalid-hash:
    description: auth-map rejects a Secret value outside the htpasswd-hash charset (config-injection guard)
    fixtures:
      secrets:
        - apiVersion: v1
          kind: Secret
          metadata: {namespace: default, name: badmap-secret}
          type: Opaque
          data: {admin: ZXZpbCd2YWw=}
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: badmap-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: badmap-ingress
            annotations:
              nginx.ingress.kubernetes.io/auth-type: "basic"
              nginx.ingress.kubernetes.io/auth-secret: "badmap-secret"
              nginx.ingress.kubernetes.io/auth-secret-type: "auth-map"
          spec:
            ingressClassName: haproxy
            rules:
              - host: badmap.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: badmap-service, port: {number: 80}}}
    assertions:
      - {type: contains, target: rendering_error, pattern: "invalid password hash for user 'admin'"}

  test-nginx-ingress-proxy-next-upstream-invalid-tries:
    description: a non-integer proxy-next-upstream-tries aborts the render instead of being silently dropped
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: nut-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: nut-ingress
            annotations:
              nginx.ingress.kubernetes.io/proxy-next-upstream-tries: "abc"
          spec:
            ingressClassName: haproxy
            rules:
              - host: nut.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: nut-service, port: {number: 80}}}
    assertions:
      - {type: contains, target: rendering_error, pattern: "Must be a non-negative integer"}

  test-nginx-ingress-proxy-next-upstream:
    description: proxy-next-upstream maps nginx retry conditions to HAProxy retry-on + retries
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: nu-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: nu-ingress
            annotations:
              nginx.ingress.kubernetes.io/proxy-next-upstream: "error timeout http_502 http_503"
              nginx.ingress.kubernetes.io/proxy-next-upstream-tries: "2"
          spec:
            ingressClassName: haproxy
            rules:
              - host: nu.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: nu-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "retry-on conn-failure response-timeout 502 503"}
      - {type: contains, target: haproxy.cfg, pattern: "retries 2"}

  test-nginx-ingress-limit-rate:
    description: limit-rate / limit-rate-after map to a per-stream bwlim-out download throttle
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: lr-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: lr-ingress
            annotations:
              nginx.ingress.kubernetes.io/limit-rate: "100k"
              nginx.ingress.kubernetes.io/limit-rate-after: "1m"
          spec:
            ingressClassName: haproxy
            rules:
              - host: lr.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: lr-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "filter bwlim-out ni_limitrate_default_lr-ingress default-limit 100k default-period 1s min-size 1m"
      - {type: contains, target: haproxy.cfg, pattern: "set-bandwidth-limit ni_limitrate_default_lr-ingress"}

  test-nginx-ingress-proxy-redirect:
    description: proxy-redirect-from/-to rewrites the upstream Location response header
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: pr-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: pr-ingress
            annotations:
              nginx.ingress.kubernetes.io/proxy-redirect-from: "http://backend.local"
              nginx.ingress.kubernetes.io/proxy-redirect-to: "https://public.example.com"
          spec:
            ingressClassName: haproxy
            rules:
              - host: pr.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: pr-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "http-response replace-header Location .* https://public.example.com"

  test-nginx-ingress-rate-limit-rps:
    description: Nginx Ingress limit-rps annotation generates stick-table and deny 429
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: ratelimit-ingress
            annotations:
              nginx.ingress.kubernetes.io/limit-rps: "10"
          spec:
            ingressClassName: haproxy
            rules:
              - host: ratelimit.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "stick-table type ip size 100k expire 1s store http_req_rate\\(1s\\)"
      - {type: contains, target: haproxy.cfg, pattern: "deny_status 429"}

  test-nginx-ingress-rate-limit-rpm:
    description: Nginx Ingress limit-rpm annotation generates per-minute stick-table and deny 429
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: rpm-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rpm-ingress
            annotations:
              nginx.ingress.kubernetes.io/limit-rpm: "120"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rpm.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: rpm-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "stick-table type ip size 100k expire 60s store http_req_rate\\(60s\\)"
      - type: contains
        target: haproxy.cfg
        pattern: "deny deny_status 429 if \\{ sc_http_req_rate\\(0\\) gt 120 \\}"

  test-nginx-ingress-rate-limit-rps-and-rpm:
    description: limit-rps takes precedence over limit-rpm (HAProxy stores http_req_rate once per table)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: rpsrpm-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rpsrpm-ingress
            annotations:
              nginx.ingress.kubernetes.io/limit-rps: "10"
              nginx.ingress.kubernetes.io/limit-rpm: "300"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rpsrpm.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: rpsrpm-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "stick-table type ip size 100k expire 1s store http_req_rate\\(1s\\)"
      - type: contains
        target: haproxy.cfg
        pattern: "deny deny_status 429 if \\{ sc_http_req_rate\\(0\\) gt 10 \\}"
      - {type: not_contains, target: haproxy.cfg, pattern: "gt 300"}
      - {type: contains, target: haproxy.cfg, pattern: "limit-rpm ignored"}

  test-nginx-ingress-rate-limit-whitelist:
    description: Nginx Ingress limit-whitelist exempts CIDRs from rate limiting
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: rlwl-service}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rlwl-ingress
            annotations:
              nginx.ingress.kubernetes.io/limit-rps: "10"
              nginx.ingress.kubernetes.io/limit-whitelist: "10.0.0.0/8, 192.168.1.0/24"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rlwl.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: rlwl-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "deny deny_status 429 if \\{ sc_http_req_rate\\(0\\) gt 10 \\} !\\{ src 10\\.0\\.0\\.0/8 192\\.168\\.1\\.0/24 \\}"

  test-nginx-ingress-config-snippet:
    description: Nginx Ingress configuration-snippet injects raw directives
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: snippet-ingress
            annotations:
              nginx.ingress.kubernetes.io/configuration-snippet: |
                option httpchk GET /healthz
          spec:
            ingressClassName: haproxy
            rules:
              - host: snippet.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "option httpchk GET /healthz"}

  test-nginx-ingress-rewrite-target:
    description: Nginx Ingress rewrite-target translates capture groups
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rewrite-ingress
            annotations:
              nginx.ingress.kubernetes.io/rewrite-target: "/api\\$1"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rewrite.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "replace-path"}

  test-nginx-ingress-rewrite-target-literal:
    description: Nginx rewrite-target with no capture group relocates to path-rewrite.map (reload-free)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: rwlit-svc}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rwlit-ing
            annotations:
              nginx.ingress.kubernetes.io/rewrite-target: "/new"
          spec:
            ingressClassName: nginx
            rules:
              - host: rwlit.example.com
                http:
                  paths:
                    - path: /old
                      pathType: Prefix
                      backend: {service: {name: rwlit-svc, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      # Literal rewrite → path-rewrite.map entry (URL-encoded) + shared frontend set-path.
      - {type: contains, target: map:path-rewrite.map, pattern: "default_rwlit-ing_svc_rwlit-svc_80 %2fnew"}
      # No per-backend replace-path for the literal case (it moved to the map).
      - {type: not_contains, target: haproxy.cfg, pattern: "replace-path \\(\\.\\*\\) /new"}

  test-nginx-ingress-rewrite-target-injection-rejected:
    description: A control character in a literal rewrite-target aborts the render (path-rewrite.map injection guard)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: rwinj-svc}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rwinj-ing
            annotations:
              nginx.ingress.kubernetes.io/rewrite-target: "/new\nevil-backend /pwned"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rwinj.example.com
                http:
                  paths:
                    - path: /old
                      pathType: Prefix
                      backend: {service: {name: rwinj-svc, port: {number: 80}}}
    assertions:
      - {type: contains, target: rendering_error, pattern: "must not contain control characters"}

  test-nginx-ingress-app-root-map-sorted:
    description: app-root.map lines are sorted by host (deterministic output, no reload churn)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: ar-sort-svc}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: zzz-ar-ingress
            annotations:
              nginx.ingress.kubernetes.io/app-root: "/z"
          spec:
            ingressClassName: haproxy
            rules:
              - host: zzz-ar.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: ar-sort-svc, port: {number: 80}}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: aaa-ar-ingress
            annotations:
              nginx.ingress.kubernetes.io/app-root: "/a"
          spec:
            ingressClassName: haproxy
            rules:
              - host: aaa-ar.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: ar-sort-svc, port: {number: 80}}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: map:app-root.map
        pattern: "aaa-ar\\.example\\.com [^\\n]*\\nzzz-ar\\.example\\.com"

  test-nginx-ingress-app-root:
    description: Nginx Ingress app-root redirects root path
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: approot-ingress
            annotations:
              nginx.ingress.kubernetes.io/app-root: "/dashboard"
          spec:
            ingressClassName: haproxy
            rules:
              - host: approot.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: map:app-root.map, pattern: "approot.example.com %2fdashboard"}
      - type: contains
        target: haproxy.cfg
        pattern: "http-request redirect location %\\[var\\(txn.host\\),map\\([^)]*app-root\\.map\\),url_dec\\(1\\)\\] code 302 if \\{ path / \\}"

  test-nginx-ingress-proxy-body-size:
    description: Nginx Ingress proxy-body-size limits request body via body-size.map (relocated from the backend)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: bodysize-service}
          spec:
            ports:
              - port: 80
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: bodysize-service-abc
            namespace: default
            labels:
              kubernetes.io/service-name: bodysize-service
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.1"]
          ports:
            - port: 8080
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: bodysize-ingress
            annotations:
              nginx.ingress.kubernetes.io/proxy-body-size: "10m"
          spec:
            ingressClassName: haproxy
            rules:
              - host: bodysize.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: bodysize-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: map:body-size.map
        pattern: "default_bodysize-ingress_svc_bodysize-service_80 10485760"
      # The old per-backend literal deny must be gone (relocated to the
      # resource-agnostic frontend-filters-250-request-body-size scaffold).
      - {type: not_contains, target: haproxy.cfg, pattern: "req.body_size gt 10485760"}

  test-nginx-ingress-ssl-passthrough:
    description: Nginx Ingress ssl-passthrough creates TCP backend and SNI routing
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: passthrough-service}
          spec:
            ports:
              - port: 443
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: passthrough-service-abc
            namespace: default
            labels:
              kubernetes.io/service-name: passthrough-service
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.1"]
          ports:
            - port: 8443
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: passthrough-ingress
            annotations:
              nginx.ingress.kubernetes.io/ssl-passthrough: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: passthrough.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: passthrough-service, port: {number: 443}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "backend ssl-passthrough-ni-default-passthrough-ingress"
      - {type: contains, target: haproxy.cfg, pattern: "mode tcp"}
      - type: contains
        target: haproxy.cfg
        pattern: "use_backend ssl-passthrough-ni-default-passthrough-ingress if { req_ssl_sni -m str passthrough.example.com }"

  test-nginx-ingress-ssl-passthrough-skips-ingress-without-paths:
    description: "Regression test for the dry-run race that flaked TestIngressAnnotations/nginx-ssl-passthrough on !893. Pre-fix shape: BuildAnnotationSSLPassthrough added an entry to gf[\"sslPassthroughBackends\"] for any (host, ingress) pair where the annotation was truthy. The use_backend in ssl-tcp was emitted from that list. The corresponding `backend X` block came from a separate `resources.ingresses.GetSingle` lookup that walked spec.rules to find a service. If the ingress had a host but no spec.rules[*].http.paths (or those paths had no backend.service), the use_backend was emitted without a matching backend block — `haproxy -c` rejected the config and the webhook denied the unrelated dry-run as collateral damage. Post-fix: the macro itself extracts svcName/svcPort up front and refuses to add an entry without them. This fixture's ingress has the annotation + a host but no http.paths, so neither use_backend nor backend should appear in the rendered config."
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: passthrough-ingress-no-paths
            annotations:
              nginx.ingress.kubernetes.io/ssl-passthrough: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: noservice.example.com
                # Note: no `http:` block, so no paths.
    assertions:
      - type: haproxy_valid
      - type: not_contains
        target: haproxy.cfg
        pattern: "ssl-passthrough-ni-default-passthrough-ingress-no-paths"
  test-nginx-ingress-ssl-passthrough-named-port:
    description: nginx-ingress SSL passthrough annotation must resolve a backend referenced by port.name to the correct numeric pod port (regression guard for the silent fallback-to-not-rendered bug in BuildAnnotationSSLPassthrough)
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {name: secure-app, namespace: default}
          spec:
            ports:
              - {name: https, port: 8443, targetPort: https}
      endpoints:
        - apiVersion: discovery.k8s.io/v1
          kind: EndpointSlice
          metadata:
            name: secure-app-xyz
            namespace: default
            labels:
              kubernetes.io/service-name: secure-app
          addressType: IPv4
          endpoints:
            - addresses: ["10.0.0.50"]
              targetRef: {kind: Pod, name: secure-app-0}
          ports:
            - {name: https, port: 8443, protocol: TCP}
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            name: secure-app
            namespace: default
            annotations:
              nginx.ingress.kubernetes.io/ssl-passthrough: "true"
          spec:
            ingressClassName: haproxy
            rules:
              - host: secure-app.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: secure-app, port: {name: https}}}
    assertions:
      - type: haproxy_valid
      - {type: contains, target: haproxy.cfg, pattern: "server secure-app-0 10.0.0.50:8443 "}
      - {type: not_contains, target: haproxy.cfg, pattern: "server secure-app-0 10.0.0.50:80 "}

  # ==========================================================================
  # Upstream request headers (upstream-vhost / x-forwarded-prefix / connection-proxy-header)
  # ==========================================================================

  test-nginx-ingress-upstream-headers:
    description: upstream-vhost, x-forwarded-prefix, and connection-proxy-header set upstream request headers
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: uh-svc}
          spec:
            ports:
              - port: 80
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: uh-ingress
            annotations:
              nginx.ingress.kubernetes.io/upstream-vhost: "internal.example.com"
              nginx.ingress.kubernetes.io/x-forwarded-prefix: "/app"
              nginx.ingress.kubernetes.io/connection-proxy-header: "keep-alive"
          spec:
            ingressClassName: nginx
            rules:
              - host: uh.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: uh-svc, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      # Relocated to per-header maps keyed on the backend; the shared frontend
      # set-header rules in base.yaml apply them. Adding/changing a value is now
      # a map-only, reload-free update.
      - type: contains
        target: map:reqhdr-host.map
        pattern: "default_uh-ingress_svc_uh-svc_80 internal.example.com"
      - {type: contains, target: map:reqhdr-xfwd-prefix.map, pattern: "default_uh-ingress_svc_uh-svc_80 %2fapp"}
      - type: contains
        target: map:reqhdr-connection.map
        pattern: "default_uh-ingress_svc_uh-svc_80 keep-alive"

  test-nginx-ingress-mirror-target:
    _helm_skip_test: '{{ if not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.mirror "root" .)) }}true{{ end }}'
    description: "mirror-target mirrors the Ingress's requests to the spoa-hub mirror plugin — a host-gated append to txn.gw_mirror_targets + the single send-spoe-group mirror-group, plus a Content-Length-gated http-request wait-for-body. Only the authority of the nginx URL template is used as the target."
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: mt-service}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: mt-ingress
            annotations:
              nginx.ingress.kubernetes.io/mirror-target: "https://mirror.example.com$request_uri"
          spec:
            ingressClassName: haproxy
            rules:
              - host: mirror-app.example.com
                http:
                  paths:
                    - path: /
                      pathType: Prefix
                      backend: {service: {name: mt-service, port: {number: 80}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "set-var\\(txn.gw_mirror_targets\\) str\\(https\\|mirror.example.com:443\\|2000\\|0;\\),concat\\(,txn.gw_mirror_targets,\\) if \\{ var\\(txn.host\\) -m str mirror-app.example.com \\}"
      - type: contains
        target: haproxy.cfg
        pattern: "send-spoe-group spoa-hub-mirror mirror-group if \\{ var\\(txn.gw_mirror_targets\\) -m found \\}"
      - type: contains
        target: haproxy.cfg
        pattern: "http-request wait-for-body time 10s if \\{ req\\.hdr\\(content-length\\) -m found \\} !\\{ var\\(txn\\.backend_name\\),map\\(.*request-buffering\\.map,on\\) -m str off \\}"

  test-nginx-ingress-mirror-target-same-host-both-mirror:
    _helm_skip_test: '{{ if not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.mirror "root" .)) }}true{{ end }}'
    description: "two mirror-target Ingresses sharing a host both append their own entry to txn.gw_mirror_targets, so a request to that host mirrors to BOTH targets from the single NOTIFY (no shared-slot collision — the list carries both, no per-Ingress index)."
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: mh-svc}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: mh-ingress-a
            annotations:
              nginx.ingress.kubernetes.io/mirror-target: "http://mirror-a.example.com$request_uri"
          spec:
            ingressClassName: haproxy
            rules:
              - host: shared.example.com
                http:
                  paths:
                    - {path: /a, pathType: Prefix, backend: {service: {name: mh-svc, port: {number: 80}}}}
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: mh-ingress-b
            annotations:
              nginx.ingress.kubernetes.io/mirror-target: "http://mirror-b.example.com$request_uri"
          spec:
            ingressClassName: haproxy
            rules:
              - host: shared.example.com
                http:
                  paths:
                    - {path: /b, pathType: Prefix, backend: {service: {name: mh-svc, port: {number: 80}}}}
    assertions:
      - type: haproxy_valid
      - type: contains
        target: haproxy.cfg
        pattern: "set-var\\(txn.gw_mirror_targets\\) str\\(http\\|mirror-a.example.com:80\\|2000\\|0;\\),concat\\(,txn.gw_mirror_targets,\\) if \\{ var\\(txn.host\\) -m str shared.example.com \\}"
      - type: contains
        target: haproxy.cfg
        pattern: "set-var\\(txn.gw_mirror_targets\\) str\\(http\\|mirror-b.example.com:80\\|2000\\|0;\\),concat\\(,txn.gw_mirror_targets,\\) if \\{ var\\(txn.host\\) -m str shared.example.com \\}"
      - {type: not_contains, target: haproxy.cfg, pattern: "mirror-2-group"}

  test-nginx-ingress-mirror-target-hostless-rejected:
    _helm_skip_test: '{{ if not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.mirror "root" .)) }}true{{ end }}'
    description: "mirror-target on a host-less Ingress is rejected (no host to gate on)."
    extraContext: {renderMode: admission}
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: mhl-svc}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: mhl-ingress
            annotations:
              nginx.ingress.kubernetes.io/mirror-target: "http://mirror.example.com$request_uri"
          spec:
            ingressClassName: haproxy
            rules:
              - http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: mhl-svc, port: {number: 80}}}}
    assertions:
      - type: contains
        target: rendering_error
        pattern: "host-less / default-backend mirroring is not supported"

  test-nginx-ingress-mirror-target-bad-port-rejected:
    _helm_skip_test: '{{ if not (include "haptic.spoaHub.pluginEnabled" (dict "plugin" .Values.spoaHub.plugins.mirror "root" .)) }}true{{ end }}'
    description: "mirror-target with an out-of-range port is rejected at render time."
    fixtures:
      services:
        - apiVersion: v1
          kind: Service
          metadata: {namespace: default, name: mbp-svc}
          spec:
            ports: [{port: 80}]
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: mbp-ingress
            annotations:
              nginx.ingress.kubernetes.io/mirror-target: "http://mirror.example.com:99999$request_uri"
          spec:
            ingressClassName: haproxy
            rules:
              - host: mbp.example.com
                http:
                  paths:
                    - {path: /, pathType: Prefix, backend: {service: {name: mbp-svc, port: {number: 80}}}}
    assertions:
      - {type: contains, target: rendering_error, pattern: "Port must be 1-65535"}

  # ==========================================================================
  # Config-injection guards (issue #157). Char-class rejection itself is proven
  # by base's ConfigInjectionKind / Escape* utility tests; these are per-site
  # wiring checks that each guard is actually called at its emission point.
  # ==========================================================================
  # One hostile Ingress at once exercises the routing/presentation guards that
  # warn-and-skip: server-alias (host.map key), canary match value (single-quoted
  # ACL operand), custom-request-headers value (frontend directive) and
  # session-cookie-name (backend cookie token). A dropped guard would surface its
  # marker; a valid config still renders (admission denial is base-tested).
  test-nginx-ingress-warn-guards-skip-injection:
    description: A single Ingress with injection payloads in server-alias, canary, custom-request-headers and session-cookie-name renders clean — every guarded value is skipped, no smuggled marker survives.
    extraContext: {renderMode: reconcile}
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: hostile
            annotations:
              nginx.ingress.kubernetes.io/server-alias: "ok.example.com,inj\nALIASWN.example.com"
              nginx.ingress.kubernetes.io/affinity: "cookie"
              nginx.ingress.kubernetes.io/session-cookie-name: "SID COOKIEWN"
              nginx.ingress.kubernetes.io/canary: "true"
              nginx.ingress.kubernetes.io/canary-by-header: "X-Canary"
              nginx.ingress.kubernetes.io/canary-by-header-value: "v1' } { CANARYWN"
              nginx.ingress.kubernetes.io/custom-request-headers: "X-Foo: v1\nHEADERWN"
          spec:
            ingressClassName: haproxy
            rules:
              - host: hostile.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: map:host.map, pattern: "ok\\.example\\.com hostile\\.example\\.com"}
      - {type: not_contains, target: map:host.map, pattern: "ALIASWN"}
      - {type: not_contains, target: haproxy.cfg, pattern: "COOKIEWN"}
      - {type: not_contains, target: haproxy.cfg, pattern: "CANARYWN"}
      - {type: not_contains, target: haproxy.cfg, pattern: "HEADERWN"}

  # Load-bearing special case: a custom-request-headers value is double-quoted and
  # log-format escaped, so a literal `%` is doubled and can't read request state.
  test-nginx-ingress-custom-request-header-value-log-format-escaped:
    description: A literal percent in a custom-request-headers value is URL-encoded (% -> %25) in ing-reqhdr.map and decoded with url_dec(1), so HAProxy can't interpret it as a log-format fetch.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: hdr-escape
            annotations:
              nginx.ingress.kubernetes.io/custom-request-headers: "X-Discount: 50%off"
          spec:
            ingressClassName: haproxy
            rules:
              - host: hdr.example.com
                http: *httpRoot
    assertions:
      - type: haproxy_valid
      - {type: contains, target: 'map:ing-reqhdr.map', pattern: 'default_hdr-escape_svc_echo_80\|set\|x-discount 50%25off'}
      - {type: contains, target: haproxy.cfg, pattern: 'set-header X-Discount %\[var\(txn\.backend_name\),concat\(\|set\|x-discount\),map\([^)]*ing-reqhdr\.map\),url_dec\(1\)\]'}
      - {type: not_contains, target: haproxy.cfg, pattern: 'set-header X-Discount "'}

  # A malformed rate-limit threshold reaches an unquoted ACL operand and must fail
  # closed rather than silently ship without the limit (rpm/connections share the
  # same guard). Wiring check that the guard is called.
  test-nginx-ingress-limit-rps-injection-fails:
    description: A space in limit-rps fails the render — the rate limit is never silently dropped.
    fixtures:
      ingresses:
        - apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            namespace: default
            name: rps-attacker
            annotations:
              nginx.ingress.kubernetes.io/limit-rps: "10 } { sc_conn_cur(0) gt 0"
          spec:
            ingressClassName: haproxy
            rules:
              - host: rps.example.com
                http: *httpRoot
    assertions:
      - {type: contains, target: rendering_error, pattern: "break the rate-limit ACL"}
